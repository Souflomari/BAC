# Proposed reconciliation — `docs/grounding/architecture.md`

**Status:** PROPOSAL ONLY. The grounding doc is **not** edited. This note
lists the specific stale passages and the proposed correction for human
review. Generated by the 2026-06-22 re-establishment audit.

**Why stale:** `architecture.md` was written during the original
reconciliation audit, before migrations 040–047 landed. It also carries a
§5.5 over-generalisation whose correction (Task 5, commit `03671f3`) exists
only on the unmerged `slice-2-prep--architecture-5.5-cleanup` branch.

---

## Proposed corrections

| Loc | Current text (paraphrased) | Proposed correction |
|-----|----------------------------|---------------------|
| §2 "Repository layout" | "`backend/supabase/` — migrations/ (**36** numbered SQL files, gaps at 019 and 035)" | "**45** numbered SQL files (001–047, gaps at 019 and 035), plus `_apply_002_to_006_idempotent.sql`" |
| §2 | "`docs/grounding/`, `docs/decisions/` — **empty before this audit**" | "`docs/grounding/` (4 docs), `docs/decisions/` (ADRs 0003–0014)" |
| §2 | "148 .dart files, ~55k lines" | "152 .dart files, ~55k lines" (tracked count) |
| §4.1 "Migration set" | "**36** numbered migrations" | "**45** numbered migrations (001–047, gaps 019/035)" — keep the "gaps intentional / append-only" note as-is (still correct) |
| §4.2 "Tables (live count: **17**)" | "17" | "**18** — add `user_misconception_states` (mig 043)" |
| §4.2 Curriculum tables: "**RLS not enabled** — see §4.4" | RLS not enabled | "**RLS enabled by migration 040**" — remove the "declared but inert" framing for the 7 curriculum tables |
| §4.4 "RLS coverage — partial … does **not** appear for: subjects, stream_subjects, topics, skills, skill_prerequisites, items, badges" | partial | "**RLS coverage — complete.** Migration 040 enabled RLS on the seven curriculum tables; the previously-inert public-read policies are now active." |
| §4.6 RPC list | (no `record_misconception_exhibited`) | Add: "`record_misconception_exhibited(uuid, uuid, text, …)` (mig 047) — atomic upsert into `user_misconception_states`, service_role-granted, called by the submit-answer edge function." |
| §4.6 `get_user_weak_areas` "broken… dead code or latent error" | broken | **Keep as-is** — verified still unfixed (only in mig 004) and still unreferenced by Dart/edge functions, i.e. confirmed dead. (No change needed; flagged here only to confirm it is *not* stale.) |
| §5.1 prereq table (SMA **0** / SMB **0** edges) | 0 / 0 | "SMA edges seeded (mig 042); 73 SMB + humanities edges recovered (mig 041). PC 21, SVT 9 unchanged." |
| §5.5 "two parallel item banks per scientific subject" / "**For every** scientific-subject skill… produced two skill rows" | universal two-bank claim | **Replace with the three-shape (A/B/C) framing** already authored on `slice-2-prep--architecture-5.5-cleanup` (commit `03671f3`): Shape A = legacy + SMA-prefixed twin (e.g. `limit_calc`/`sma_limit_calc`); Shape B = SMA-only, SMB omits the skill (`sma_limit_ops`); Shape C = SMA-only, SMB folds it into a broader skill (`sma_asymptotes`). The naïve-query failure mode stays, scoped to Shape A only. |
| §7 "What is notably absent → Misconception-tagged distractors… no stable misconception_id… no per-(student, misconception) state" | absent | "**Landed** (mig 043–047): `items.distractor_misconceptions`, the misconception ID format (ADR 0008), and `user_misconception_states`. Tagging coverage: slice 1 (`sma_limit_calc`) done; further skills pending." |
| §1 / §3 (Flutter framing) + the `nextjs-frontend` references | "nextjs-frontend has no codebase" | **Flag, do not silently fix.** The agent kit now implies a hybrid (Next.js shell + Flutter iframe) model and a `flutter-widgets` agent exists, but there is no `nextjs-frontend` agent file and no ADR recording the stack decision. Resolve the decision first (see main report Part 4 §4); then update §1/§3 to match. |

## Recommended sequencing

1. The §5.5 correction is the highest-value fix — it removes a *disproven*
   structural claim from the canonical doc. Easiest path: land
   `slice-2-prep--architecture-5.5-cleanup` (a clean +1 commit) rather than
   re-authoring.
2. The count/RLS/RPC corrections (rows 1–10 above) are mechanical and safe.
3. The frontend-framing row is **blocked on a human decision** — do not edit
   §1/§3 until the stack decision is recorded.
