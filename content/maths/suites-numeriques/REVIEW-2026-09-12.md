# suites-numeriques — revue vague 1 (2026-09-12)

Deux critiques (`bac-fidelity-critic`, `pedagogy-critic`) + recalcul indépendant
croisé, mené AVANT leurs retours.

## Ce que le recalcul indépendant a établi : les maths sont saines

Vérifiés terme à terme et retrouvés justes — les deux critiques concordent, et
la fidelity-critic ne conteste aucun de ces points :

- le réservoir (100 → 60 → 40 → 30 → 25 → 22,5 → 21,25 → 20,625, point fixe 20) ;
- les deux récurrences ($u_n > 20$, $u_n \le 100$) ;
- arithmétiques/géométriques : terme général, sommes, cas $q = 1$ ;
- la monotonie par $0{,}5(20-u_n)$ ; la définition ε–N et l'unicité de la limite ;
- les limites usuelles, les formes indéterminées, les gendarmes sur $\cos(n)/(n+1)$,
  la limite monotone ;
- $v_n = u_n - 20$ géométrique $\Rightarrow u_n = 20 + 80 \times 0{,}5^n$ ;
- **tout le chapitre homographique** (points fixes $L^2 = 1$ ; les deux
  auxiliaires ; la dérivation générale $(a-c\alpha)(u_n-\alpha)/(cu_n+d)$ ;
  le recoupement $u_1 = 5/3$ ; les variantes $b = 0$ et racine double) ;
- les suites adjacentes et **les deux sommets** (2019 SM : $g(\ln 2) = 0{,}095 > 0$,
  $u_{n+1} = 4u_n\,g(u_n)$, la bascule $L = 0$ / $-\infty$, $u_n \le u_0 + n f(u_0)$ ;
  r-variation : $\sqrt{2u_n+3}$, $(x-3)(x+1) \le 0$, $L = 3$) ;
- le `coverage_summary` : 15 comptes, 38 items, plancher 3, 11 clés de
  `ramp_coverage` (Σ = 38) — recomptés depuis les 114 tags de distracteurs par
  les deux critiques **indépendamment**, aucun écart.

Les citations « chapitre N » visent toutes les chapitres 1–9 (zone non décalée
par le titre sans barreau) et sont **justes** : aucune n'a été touchée.

## Appliqué — 12 corrections objectives

Aucun calcul enseigné n'est modifié. Ce sont des **hypothèses manquantes**, des
**promesses non tenues**, un **barème inventé** et des **réponses imprimées
au-dessus de la porte d'essai**.

| # | Où | Défaut | Correction |
|---|---|---|---|
| S5 | `lesson.md` recette en 4 étapes | $f(I) \subset I$ donne $u_n \in I$, mais « donc bornée » exige **$I$ borné**. Contre-exemple que la recette conclut à tort : $u_{n+1} = u_n^2+1$ sur $[1;+\infty[$ — $f(I) \subset I$ ✓, croissante ✓, et elle tend vers $+\infty$ | « intervalle **borné** » + le contre-exemple en une phrase |
| S6 | `lesson.md` « le mécanisme est structurel » | « ne dépend ni des valeurs de $a,b,c,d$ » : faux. Contredit 67 lignes plus bas par la racine double, et le cas $\Delta < 0$ n'est nulle part — alors que le **3ᵉ exemple motivant de la leçon** en est un : $u_{n+1} = \frac{3u_n-1}{u_n+2}$, $L^2-L+1=0$, $\Delta = -3$ | conditionné aux deux points fixes **distincts** ; les deux cas d'échec nommés, dont l'exemple de la leçon |
| S7 | `lesson.md` résultat encadré | le résultat AFFICHÉ (celui qui part sur la fiche) omet $u_n \neq \beta$ ; la leçon le répare 21 lignes plus loin, en prose | hypothèse ajoutée dans la formule affichée |
| S11 | `lesson.md` accroche | promet « un théorème qu'on va **démontrer** » ; le chapitre 8 le refuse ensuite, à juste titre (« hors programme ici ») | « énoncer et comprendre en profondeur — sa démonstration complète est hors programme » |
| S12 | `lesson.md` R10 | « tous les outils du chapitre » omet les **deux chapitres qui précèdent** (homographiques, adjacentes) | les deux ajoutés à la liste |
| F2b | `lesson.md` intro du sommet | imprime les **deux réponses** (« convergence vers 0 dans un cas, divergence vers $-\infty$ dans l'autre ») — verbatim les réponses de q2d et q3c | l'enjeu conservé, les deux valeurs retirées |
| F2 | `exercises.yaml` × 2 | les libellés de partie se rendent **hors de la porte d'essai** (`AttemptFirstExercise.tsx:100`, avant l'énoncé et le bouton, alors que `reasoning` est gardé) : « Partie B — … : convergence vers $0$ » surplombe une question qui demande de **calculer** cette limite | libellés neutres (« Partie B — Le cas $0 \le u_0$ ») |
| S10c | `exercises.yaml` sourcing | « barème 4 points sur l'exercice » : le relevé vérifié ne porte **aucun** chiffre de 4 points pour ce sujet (seulement « exercice complet : 10 points »), contrairement aux entrées SExp du même fichier qui citent la table des composantes. Les neuf barèmes qu'il transcrit somment à 0,5+0,25+0,5+0,5+0,25+0,5+0,5+0,5+0,25 = **3,75** | 3,75 points sur la Partie II ; exercice complet 10 points |
| S13 | `items.yaml` | autocorrection de brouillon laissée en plein champ : « donne $u_n\ldots$ non — c'est … » | phrase refaite ; les maths visées étaient déjà justes |
| F15 | `items.yaml` × 5 | **deux `skill_code` dans une même notion** (5 items en `maths_…`, 34 + 6 checkpoints en `sma_…`) | normalisé sur `sma_suites_numeriques` |
| S10a | `bank.yaml` | la SCOPE NOTE déclare un manque que la leçon a **comblé** : les trois auxiliaires qu'elle dit non enseignées le sont désormais (`lesson.md:528` ×2, `:573-585`) | note remise à jour ; ce qui reste vrai l'est pour une autre raison (le chapitre n'a pas de code de barreau) |

`validate-content --strict` : 0 failure.

### Sur F15, l'arbitrage mérite d'être écrit

Le corpus penche pour `maths_*` (12 notions de maths sur 14) — ce qui ferait des
5 items déviants les **bons**. C'est l'inverse : les ids de misconception du
fichier sont tous `mc.math.**sma_suites_numeriques**.<label>` (ADR 0011 lie ce
préfixe au `skill_code`) et 114 tags de distracteurs les visent. Le fichier est
bâti sur `sma_`. C'est la convention interne, pas la majorité du corpus, qui
tranche.

## Déféré — content-author / pedagogy-architect

- **Le chapitre homographique n'a pas de code de barreau** (S9/F1, les deux
  critiques en tête de liste). ~140 lignes, le plus long bloc de la notion :
  **0 item, 0 checkpoint, 0 figure, aucune carte « à retenir »**, et il est
  invisible au `coverage_summary`. Or c'est la forme la plus fréquente aux
  examens dans ce chapitre (3 des 4 sujets de banque la portent). Lui donner un
  barreau et une place dans la rampe débloque le taguage.
- **F4** : aucun commit entre la définition ε–N et la fin des adjacentes — 400
  lignes, la partie la plus dure, non sondée. Le chapitre 8 (limite monotone)
  n'a **aucune** section « erreur à repérer ».
- **F5** : 5 lignes de grille non réclamées, dont `monotone-hypothese-partielle`
  (5 items, modèle faux jamais énoncé) ; aucun fichier de spec où déléguer.
- **F3/S14** : `r-variation` — 3 questions contre 9, laisse tomber la bascule
  selon $u_0$ (la synthèse niveau 3, 20 % au cadre) et **nomme la méthode**
  avant l'essai, donc il n'y a plus rien à reconnaître.
- **F7** : ε n'est instancié qu'une fois (ε = 3) ; l'alternance des
  quantificateurs n'est jamais éprouvée.
- **F11** : le feedback de l'accroche livre la valeur **et** le mécanisme (20, le
  point fixe) — c'est la révélation du chapitre 8, donnée à la 1ʳᵉ interaction.
- **F8** : l'intuition du chapitre 8 reprend le théorème au lieu de le faire sentir.
- **F14** : `cp-convergence-pointfixe` est posé 200 lignes après la prose qu'il
  rompt, juste avant le sommet.
- **F10** : enchaînements de 4–5 transformations par ligne `$$` dans le chapitre
  homographique (le même fichier fait bien ailleurs).
- **F9** : aucun `retenir.json`, **zéro `\boxed{}`** — la zone « à retenir »
  retombe sur la 1ʳᵉ formule bloc du chapitre et affiche de l'arithmétique
  d'exemple ($u_n > 20$, $u_n \le 100$) sur deux chapitres, **rien** sur les deux
  théorèmes les plus importants (limite monotone, adjacentes — énoncés en puces,
  sans math bloc). Défaut de corpus probable : à mesurer partout.

## Déféré — research-lead / propriétaire (cadre)

- **S1, le plus grave** : `lesson_slug: suites-numeriques` est revendiqué par les
  **deux** cadres, et `maths-sexp.yaml:78` dit explicitement que les suites
  adjacentes sont « hors du cœur SExp ». Le chapitre 11 et 3 items sont donc
  hors-programme pour un lecteur SExp, **sans aucune barrière**. La seule
  déclaration de filière est un commentaire HTML : rien de lisible par machine.
- **S8** : la dérivation générale en lettres et la variante à racine double
  dépassent le `savoir_faire` du cadre (qui demande « résoudre $\ell = f(\ell)$ »).
  Atténué : les deux sont explicitement présentées comme facultatives.

## Déféré — schéma / pipeline

- **S2** : aucun champ `habilete` sur les 38 items → le ratio 40/40/20 du cadre
  est **structurellement inauditable**. Et les 5 checkpoints portent tous
  `habilete: raisonnement`, qui n'est pas un des trois niveaux maths (c'est
  l'axe PC) : seule notion de maths du corpus à 5/0 sur cet axe.
- **S3/S4** : 8 items sur 38 (≈ 21 %) sont de l'application directe contre 40 %
  au cadre ; et 22 items sur 38 emploient le cadrage « un élève affirme… qu'en
  penser ? », qui n'apparaît **dans aucun** des cinq sujets nationaux vérifiés.
- **F16** : la bonne réponse est A dans 4 checkpoints sur 5 (la banque, elle,
  mélange délibérément).
- **F12/F13** : les chapitres 2, 4, 8 et 11 n'ont aucun item de **production** ;
  et les 3 items du dernier barreau se passent le résultat de l'un à l'autre.
