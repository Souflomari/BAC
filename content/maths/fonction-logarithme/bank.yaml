# bank.yaml — la banque « S'entraîner » de fonction-logarithme (BANK-SPEC v1).
#
# Sujets réels d'examen national, cartes attempt-first (BANK-SPEC §2/§3). CE
# N'EST PAS exercises.yaml : aucune entrée ici ne porte de marqueur
# [[exercise:…]] dans lesson.md — c'est la couche de VOLUME derrière le sommet
# de leçon (r-bac + r-variation, [[exercise:]] à R7), pas la rampe embarquée.
#
# `sourcing` est AUTHORING-SIDE UNIQUEMENT (jamais rendu — même contrat que
# exercises.yaml). Les quatre entrées sont des transcriptions FIDÈLES des
# quatre entrées « vérifié » de docs/sujets/maths/fonction-logarithme.md —
# 2019 et 2023 (re-fetch AlloSchool indépendant + diff caractère-par-
# caractère, agent-vérificateur-adversarial, 2026-07-12), puis 2021 et 2024
# (même protocole, agent-vérificateur-adversarial, 2026-08-06 ; 2024 porte le
# statut « vérifié (corrigé sur re-lecture) », deux corrections mineures
# actées lors de la relecture — voir sourcing.note de bk-2024-n-x4) — les
# SEULS sujets vérifiés recensés pour cette notion à ce jour : 2019, 2021,
# 2023 et 2024, tous quatre session normale, filière Sciences Expérimentales,
# tous quatre le « Problème » (4ᵉ composante du sujet NS 22F, après les trois
# exercices) sur leur copie (barème : 11, 9, 11 et 8 points respectivement).
# Aucun autre sujet n'est disponible — introuvable = absent, jamais inventé.
# Raisonnement rédigé à la voix experte de la maison sur 100 % des questions ;
# barème transcrit tel quel dans les énoncés ; conventions KaTeX (block
# scalars pour la prose, KaTeX brut entre apostrophes simples dans
# steps[].math, notes sans backslash NI accent — ASCII simple, même
# convention que les entrées précédentes).
#
# Chaque calcul (limites, dérivées, IPP, aires, points fixes/limites de
# suite, dérivée d'une réciproque) a été refait et vérifié indépendamment par
# l'auteur de ce fichier avant transcription des `reasoning`/`steps` — pour
# les quatre entrées, y compris 2021 et 2024, qui n'avaient aucune solution
# préexistante dans le corpus (contrairement à 2019, dont la résolution
# complète existe déjà comme sommet de leçon r-bac, exercises.yaml).
#
# SCOPE NOTE (divulgation honnête, authoring-side — reprend la note déjà
# posée dans exercises.yaml et dans docs/sujets/maths/fonction-logarithme.md) :
# les quatre problèmes sont des problèmes d'ANALYSE complets et recoupent
# d'autres notions au-delà du socle R1–R6 de cette leçon. `calcul-integral`
# (intégration par parties, pour l'aire — 2019, 2021, 2023, et l'aire de la
# Partie I de 2024) et `suites-numeriques` (récurrence + convergence
# monotone — 2019 et 2023 SEULEMENT ; 2021 et 2024 n'ont PAS de volet suites,
# celui-ci étant porté par l'exercice autonome du même sujet, hors périmètre
# de cette notion). `derivabilite-etude-fonctions` : dérivées de
# produit/quotient (ex. $(x\ln x)'$, $(\ln x / x)'$ — 2019, 2021, 2023) ;
# fonction réciproque et son domaine, corollaire du théorème des valeurs
# intermédiaires (2021 Q7, 2024 Q5) ; dérivée d'une fonction réciproque en un
# point (2024 Q5-b, $(g^{-1})'(1)$) — technique neuve dans ce fichier,
# introduite par 2021 et approfondie par 2024. `fonction-exponentielle` :
# comparaison graphique de $e^x$ et $x$, identité $\ln(e^x)=x$, limites de
# référence de l'exponentielle dont $xe^{-x}\to0$ — Partie I et plusieurs
# questions de la Partie II de 2024 SEULEMENT, notion neuve dans ce fichier.
# Les champs `reasoning` nomment explicitement la technique et la notion
# source chaque fois qu'elle sort du corps R1–R6 de cette leçon.
#
# NOTE ÉDITORIALE (pour la revue) : bk-2019-n-x4 transcrit le MÊME sujet que
# le sommet de leçon r-bac (exercises.yaml, [[exercise:r-bac]] à R7). Il
# figure ici pour que la banque soit le corpus complet des annales vérifiées
# de la notion (BANK-SPEC §1 : « la banque est le volume derrière le sommet,
# jamais son remplacement »). À l'arbitrage de l'owner si l'on préfère
# l'écarter de la banque pour éviter le recoupement sur la même page — même
# décision, prise dans le même sens, sur bk-2021-n-x1 de
# content/pc/reactions-acido-basiques/bank.yaml et bk-2019-n-x2 de
# content/maths/nombres-complexes-1/bank.yaml. bk-2023-n-x4, bk-2021-n-x4 et
# bk-2024-n-x4, en revanche, sont des ajouts NEUFS au corpus (absents
# d'exercises.yaml, dont r-variation est une variation fraîche construite,
# pas un sujet réel).
#
# ID : `bk-<année>-<n|r>-x<position sur la copie>`. Sur un sujet NS 22F
# (Sciences Expérimentales), la composition normalisée est « 3 exercices + 1
# problème » (docs/sujets/maths/README.md §0 ; INDEX.md §2) : Ex1 géométrie de
# l'espace, Ex2 nombres complexes, Ex3 probabilités, puis le Problème — d'où
# la position `x4` pour les quatre entrées de ce fichier.
#
# AJOUT DU 2026-08-27 — bk-2023-n-x1 (2023, session NORMALE, filière SM,
# Exercice 1 — 7,75 points). Converti depuis
# docs/sujets/_incoming/maths-sm-2023-n.md (agent-vérificateur-adversarial,
# 2026-08-27, statut « vérifié » — re-fetch indépendant + re-dérivation,
# AUCUN défaut trouvé dans le corps de l'exercice). NE PAS CONFONDRE avec
# bk-2023-n-x4 ci-dessus : même année (2023), même session (normale), mais
# une épreuve DIFFÉRENTE — bk-2023-n-x4 est le Problème de la filière SExp
# (code NS 22F, element/137482) ; bk-2023-n-x1 est l'Exercice 1 de la filière
# SM (code NS 24F, element/142490). Les deux identifiants ne se ressemblent
# que par coïncidence de position sur des copies différentes ; c'est
# `source.filiere` (« SExp » contre « SM ») qui les sépare réellement dans
# l'assemblage d'épreuves de web/src/lib/examens.ts (groupement sur
# `source.{filiere, year, session}`). Avec l'ajout jumeau de bk-2023-n-x2
# dans content/maths/calcul-integral/bank.yaml, cette conversion fait passer
# l'épreuve SM 2023 normale de 10,00 à 20,00/20 dans Examens blancs.
#
# RÉSERVE DE PORTÉE SUR CETTE VÉRIFICATION — à lire avant de s'appuyer
# dessus. La passe qui a validé cet énoncé PRÉCÈDE la règle du 2026-08-27.
# Une passe ultérieure, sur SM 2025 normale, a découvert qu'AlloSchool peut
# servir depuis son cache CDN un AUTRE sujet que celui demandé — et qu'une
# lecture visuelle, la transcription sous les yeux, « retrouve » alors
# l'énoncé attendu sur des pages qui ne le contiennent pas. Le protocole du
# sas exige depuis quatre contrôles (docs/sujets/_incoming/README.md,
# « la lecture visuelle seule ne suffit pas ») : année imprimée relue sur
# CHAQUE page, recoupement par un instrument NON visuel, MD5 consignés avec
# un second téléchargement, contrôle du `<title>` servi.
#   Ce que cette passe-ci a fait : re-dérivation des URLs d'images depuis le
#   HTML, CONTRÔLE DU `<title>` SERVI (qui nommait la bonne année et la
#   bonne filière), lecture des pages comme images avec recadrages, et
#   re-dérivation mathématique de toutes les identités de l'énoncé.
#   Ce qu'elle n'a pas fait : l'année page par page, le recoupement non
#   visuel, les MD5 avec second téléchargement.
# Le contrôle du `<title>` est un vrai garde-fou — il vient du HTML, pas de
# l'attente du lecteur, et c'est lui qui a mis la passe SM 2025 sur la piste.
# Risque résiduel FAIBLE MAIS NON NUL : le panachage constaté en 2025
# touchait les images alors que la page HTML, elle, était correcte.
# À L'ARBITRAGE DE L'OWNER : re-passer ce sujet sous le protocole complet,
# ou accepter le niveau de preuve ci-dessus. Cette réserve est recopiée ici
# — et pas seulement laissée dans le fichier `_incoming` — parce que c'est
# CE fichier-ci qu'un mainteneur lira le jour où il se demandera d'où sort
# l'énoncé qu'il a sous les yeux.
#
# SCOPE NOTE ADDITIONNELLE (bk-2023-n-x1) : au-delà des outils déjà recensés
# ci-dessus, ce problème mobilise deux outils supplémentaires, absents du
# corps R0–R6 de cette leçon :
#   — le corollaire du théorème des valeurs intermédiaires (continuité +
#     stricte monotonie ⇒ bijection sur l'intervalle image), pour l'existence
#     et l'unicité de alpha en Partie III, question 1 — domaine
#     `derivabilite-etude-fonctions`, déjà utilisé plus haut dans ce fichier
#     (bk-2021-n-x4, q15) ;
#   — le théorème des accroissements finis (TAF), énoncé et appliqué
#     explicitement au point d'usage, pour la majoration géométrique de
#     Partie III, question 2-b — domaine `derivabilite-etude-fonctions`,
#     usage neuf dans ce fichier (déjà pratiqué ailleurs dans le corpus SM,
#     voir content/maths/fonction-exponentielle/bank.yaml, bk-2019-n-x4) ;
#   — `fonction-exponentielle` : le facteur $e^{-x}$ de $f$, ses limites de
#     référence et sa dérivée — deuxième usage dans ce fichier après
#     bk-2024-n-x4 (Partie II tout entière).
# Le raisonnement nomme le domaine à chaque emprunt, sans jamais le supposer
# acquis du socle de cette leçon.
#
# FLAG DE JUMELAGE — bk-2023-n-x1 face à bk-2023-r-x1 et bk-2024-r-x1
# (ci-dessous). Les trois sont des problèmes d'analyse SM à quatre exercices
# indépendants, bâtis sur une suite avec une majoration géométrique. Mais la
# ressemblance s'arrête au vocabulaire ; le mécanisme diffère sur les trois
# points qui comptent :
#   — 2023 R et 2024 R étudient une FAMILLE de fonctions indexée par $n$
#     ($f_n$), avec une suite définie soit par évaluation en un point fixe
#     ($u_n=f_n(\beta)$), soit IMPLICITEMENT par une équation $f_n(x_n)=1$ ou
#     $f_n(\alpha_n)=0$ ; leur limite se détermine par un raisonnement PAR
#     L'ABSURDE (deux expressions de la même limite qui se contredisent).
#   — 2023 N (cette entrée) étudie UNE SEULE fonction fixe $f$ ; le "$n$" de
#     Partie III n'indexe aucune famille de fonctions mais les ITÉRÉS d'une
#     récurrence $u_{n+1}=\tfrac13 f(u_n)$ — une itération de point fixe
#     (méthode de Picard), technique absente des deux autres entrées. Sa
#     limite $\alpha$ n'est pas cherchée par l'absurde : elle est le point
#     fixe de $F=\tfrac13 f$, obtenu par TVI + stricte monotonie dès la
#     Partie III q1, et la convergence de $(u_n)$ vers ce point fixe se
#     démontre par une majoration géométrique EXPLICITE de la vitesse de
#     convergence ($|u_n-\alpha|\le 2^{-n}|\beta-\alpha|$), obtenue par le TAF
#     — un outil et une structure de preuve qu'aucune des deux autres
#     entrées n'emploie.
#   — 2023 N est en outre la seule des trois en session NORMALE (2023 R et
#     2024 R sont toutes deux en rattrapage), et la seule bâtie en deux
#     fonctions superposées : $g(x)=\ln(1+x)/x$ d'abord encadrée (Partie I),
#     puis $f(x)=g(x)e^{-x}$ construite dessus (Partie II) — une construction
#     en cascade que ni 2023 R ni 2024 R ne pratiquent.
# Garder les trois : elles enseignent trois usages différents du même
# vocabulaire (« suite indexée par $n$ », « limite par majoration ou par
# l'absurde »), précisément ce qui prévient la mémorisation d'un seul patron.

notion: maths/fonction-logarithme

entries:
  # ── bk-2019-n-x4 — 2019 N, Problème : étude de f avec ln, IPP, suite ──────
  # Même sujet que le sommet de leçon r-bac (exercises.yaml). Voir note éditoriale en tête.
  - id: bk-2019-n-x4
    title: "Étude d'une fonction avec ln, aire par intégration par parties et suite récurrente"
    source:
      year: 2019
      session: normale
      filiere: "SExp"
      exercise_label: "Problème"
    bareme_total: 11
    duration_min: 75
    sourcing:
      status: sourced
      note: >-
        2019 session normale — examen national SExp BIOF (SVT et Sciences
        Physiques), Mathématiques (3 h, coef 7), code NS 22F, AlloSchool
        NS22F, element/68527 (Problème, 11 points, pages 3-4 du scan).
        Transcription fidèle de l'entrée « vérifié » de
        docs/sujets/maths/fonction-logarithme.md, section « 2019 — session
        normale — Problème » (re-fetch AlloSchool indépendant
        element/68527 → course-438/upload-54971 + diff caractère-par-
        caractère conforme au scan, y compris le tableau de variations et la
        figure, agent-vérificateur-adversarial, 2026-07-12). NB : ce même
        sujet sous-tend aussi le sommet de leçon r-bac (exercises.yaml,
        marqueur [[exercise:r-bac]] à R7) — recoupement assumé, voir note
        éditoriale en tête de fichier. Recoupe aussi `calcul-integral` (Q6,
        IPP) et `suites-numeriques` (deuxième partie).
    intro: |-
      **Problème (examen national, session normale 2019, Sciences Expérimentales).**

      Soit $f$ la fonction numérique définie sur $]0,+\infty[$ par :

      $$f(x) = x + \frac{1}{2} - \ln x + \frac{1}{2}(\ln x)^2$$

      et $(C)$ sa courbe représentative dans un repère orthonormé $(O,\vec{i},\vec{j})$ *(unité : 1 cm)*.

      Point de vigilance qui traverse tout le problème : $\ln$ n'existe que sur $]0,+\infty[$, et $(\ln u)' = \dfrac{u'}{u}$ ne se lit que là où $u>0$. Garde en tête la hiérarchie des croissances vue en cours (R4) : $x$ écrase $\ln x$ à l'infini.

      La **première partie** étudie $f$ (limites, variations, convexité, position par rapport à la droite $(\Delta):y=x$, puis une aire). La **deuxième partie** exploite cette étude pour la suite $(u_n)$ définie par $u_0=1$ et $u_{n+1}=f(u_n)$.
    questions:
      - id: q1
        part: "Première partie — Limites, asymptote et branche parabolique"
        stem: |-
          **(0,5 pt)** Calculer $\displaystyle\lim_{\substack{x \to 0 \\ x > 0}} f(x)$ puis interpréter le résultat géométriquement.
        reasoning: |-
          En $0^+$, on examine chaque morceau de $f$ séparément : $x\to 0$ (fini), mais $\ln x \to -\infty$ (limite de référence, R4), donc $-\ln x\to+\infty$ et $(\ln x)^2\to+\infty$. Tous les termes qui bougent partent vers $+\infty$ — aucune indétermination, la somme suit. Une limite infinie en une borne finie du domaine se lit toujours de la même façon géométriquement : $x=0$ est asymptote verticale à $(C)$.
        steps:
          - math: '\lim_{x \to 0^+} \ln x = -\infty \implies \lim_{x \to 0^+} (\ln x)^2 = +\infty'
            note: "Limite de reference en 0+ (R4), au carre."
          - math: '\lim_{x \to 0^+} f(x) = \lim_{x \to 0^+}\left(x+\tfrac12-\ln x+\tfrac12(\ln x)^2\right) = +\infty'
            note: "Somme de termes qui partent tous vers +infini : pas d'indetermination. Asymptote verticale x=0."
      - id: q2
        stem: |-
          **(0,25 pt)** Vérifier que pour tout $x$ de $]0,+\infty[$, $f(x) = x + \dfrac{1}{2} + \left(\dfrac{1}{2}\ln x - 1\right)\ln x$.
        reasoning: |-
          Une vérification se fait dans le sens le plus sûr : on développe le membre proposé et on retombe sur $f$. Le facteur $\left(\frac12\ln x-1\right)\ln x$ se distribue en $\frac12(\ln x)^2-\ln x$ ; il ne reste qu'à ajouter $x+\frac12$.
        steps:
          - math: '\left(\tfrac12\ln x - 1\right)\ln x = \tfrac12(\ln x)^2 - \ln x'
            note: "On distribue le facteur sur ln x."
          - math: 'x+\tfrac12+\tfrac12(\ln x)^2-\ln x = x+\tfrac12-\ln x+\tfrac12(\ln x)^2 = f(x)'
            note: "On retrouve f(x) : egalite verifiee."
      - id: q3
        stem: |-
          **(0,5 pt)** En déduire que $\displaystyle\lim_{x \to +\infty} f(x) = +\infty$.
        reasoning: |-
          La forme factorisée de la question précédente sert exactement à lever l'indétermination $\infty-\infty$ qui apparaîtrait sinon. Quand $x\to+\infty$, $\ln x\to+\infty$ (R4), donc $\frac12\ln x - 1 \to+\infty$ : le produit de deux facteurs tendant vers $+\infty$ tend vers $+\infty$, et $x+\frac12\to+\infty$ s'ajoute sans changer le sens.
        steps:
          - math: '\lim_{x\to+\infty}\left(\tfrac12\ln x - 1\right) = +\infty, \quad \lim_{x\to+\infty}\ln x = +\infty'
            note: "Les deux facteurs du produit tendent vers +infini."
          - math: '\lim_{x\to+\infty}\left(\tfrac12\ln x-1\right)\ln x = +\infty \implies \lim_{x\to+\infty} f(x) = +\infty'
            note: "Produit de deux termes vers +infini, plus x+1/2 vers +infini."
      - id: q4
        stem: |-
          **(0,5 pt)** Montrer que pour tout $x$ de $]0,+\infty[$, $\dfrac{(\ln x)^2}{x} = 4\left(\dfrac{\ln\sqrt{x}}{\sqrt{x}}\right)^2$ puis en déduire que $\displaystyle\lim_{x \to +\infty} \dfrac{(\ln x)^2}{x} = 0$.
        reasoning: |-
          On part du membre de droite : $\ln\sqrt x = \frac12\ln x$ (propriété racine carrée, R2) et $(\sqrt x)^2=x$. Le carré fait apparaître $\frac14\frac{(\ln x)^2}{x}$, et le facteur $4$ compense exactement ce quart. Une fois l'égalité posée, la limite se lit sur le carré de $\frac{\ln\sqrt x}{\sqrt x}$ : en posant $t=\sqrt x$ (qui tend vers $+\infty$ avec $x$), c'est $\left(\frac{\ln t}{t}\right)^2$, qui tend vers $0$ par croissances comparées (R4).
        steps:
          - math: '\ln\sqrt{x} = \tfrac12\ln x, \qquad (\sqrt{x})^2 = x'
            note: "Propriete racine carree (R2)."
          - math: '4\left(\dfrac{\ln\sqrt{x}}{\sqrt{x}}\right)^2 = 4\cdot\dfrac{\frac14(\ln x)^2}{x} = \dfrac{(\ln x)^2}{x}'
            note: "Le carre du 1/2 donne 1/4, compense par le facteur 4."
          - math: 't=\sqrt{x}\to+\infty, \quad \dfrac{\ln t}{t}\to 0 \implies \dfrac{(\ln x)^2}{x} = 4\left(\dfrac{\ln t}{t}\right)^2 \to 0'
            note: "Croissances comparees (R4) appliquees a t = racine de x."
      - id: q5
        stem: |-
          **(0,75 pt)** Montrer que $(C)$ admet au voisinage de $+\infty$ une branche parabolique de direction asymptotique la droite $(\Delta)$ d'équation $y = x$.
        reasoning: |-
          Une branche parabolique de direction $(\Delta):y=x$ demande deux vérifications : la pente $\frac{f(x)}{x}$ tend vers $1$ (la direction), mais l'écart $f(x)-x$ ne tend pas vers un réel fini — sinon $(\Delta)$ serait une vraie asymptote. Pour la pente, on divise chaque terme par $x$ et on utilise les croissances comparées : $\frac{\ln x}{x}\to0$ (R4) et $\frac{(\ln x)^2}{x}\to0$ (question précédente). Pour l'écart, $\frac12(\ln x)^2$ domine et pousse vers $+\infty$.
        steps:
          - math: '\dfrac{f(x)}{x} = 1+\dfrac{1}{2x}-\dfrac{\ln x}{x}+\dfrac{(\ln x)^2}{2x} \xrightarrow[x\to+\infty]{} 1'
            note: "Chaque terme sauf 1 tend vers 0 (croissances comparees) : la pente tend vers 1."
          - math: 'f(x)-x = \tfrac12-\ln x+\tfrac12(\ln x)^2 \xrightarrow[x\to+\infty]{} +\infty'
            note: "Le terme en (ln x) carre l'emporte : pas d'asymptote, branche parabolique de direction y=x."
      - id: q6
        part: "Première partie — Dérivée, variations et convexité"
        stem: |-
          **(0,5 pt)** Montrer que pour tout $x$ de $]0,1]$ : $(x-1) + \ln x \le 0$ et que pour tout $x$ de $[1,+\infty[$ : $(x-1) + \ln x \ge 0$.
        reasoning: |-
          On fixe le signe d'une expression par sa monotonie : soit $g(x)=x-1+\ln x$. $g'(x)=1+\frac1x>0$ sur $]0,+\infty[$ (somme de deux termes positifs), donc $g$ est strictement croissante. Comme $g(1)=0$, la stricte croissance fait tout le travail : avant $1$, $g$ est en dessous de $g(1)=0$ ; après $1$, au-dessus.
        steps:
          - math: 'g(x)=x-1+\ln x, \qquad g''(x) = 1+\dfrac1x > 0 \text{ sur } ]0,+\infty['
            note: "g strictement croissante."
          - math: 'g(1) = 1-1+\ln 1 = 0'
            note: "Zero de g en x=1."
          - math: 'x\le 1 \implies g(x)\le 0 \qquad ; \qquad x\ge 1 \implies g(x)\ge 0'
            note: "Lecture directe de la stricte croissance autour du zero."
      - id: q7
        stem: |-
          **(1 pt)** Montrer que pour tout $x$ de $]0,+\infty[$, $f'(x) = \dfrac{x - 1 + \ln x}{x}$.
        reasoning: |-
          On dérive terme à terme, en traitant $\frac12(\ln x)^2$ comme une composée : $(\ln u)'=\frac{u'}{u}$ étendu au carré (rung R3) donne $\left(\frac12u^2\right)'=u'u$ avec $u=\ln x$, $u'=\frac1x$, soit $\frac{\ln x}{x}$. Les deux autres termes donnent $1$ et $-\frac1x$. On réduit au même dénominateur $x$.
        steps:
          - math: 'f''(x) = 1-\dfrac1x+\dfrac{\ln x}{x}'
            note: "Derivee de x, -ln x, et 1/2 (ln x) carre (regle (ln u)'=u'/u, R3)."
          - math: 'f''(x) = \dfrac{x-1+\ln x}{x}'
            note: "Reduction au meme denominateur x."
      - id: q8
        stem: |-
          **(0,5 pt)** Dresser le tableau de variations de la fonction $f$.
        reasoning: |-
          Le dénominateur de $f'(x)$ est $x>0$ : le signe de $f'$ est donc celui du numérateur $x-1+\ln x$, connu par la question précédente (négatif sur $]0,1]$, positif sur $[1,+\infty[$, nul en $1$). On lit les variations : $f$ décroît sur $]0,1]$, croît sur $[1,+\infty[$, avec un minimum en $x=1$. On complète avec les limites déjà établies ($+\infty$ en $0^+$ et en $+\infty$) et $f(1)=1+\frac12-0+0=\frac32$.
        steps:
          - math: 'f''(x)=\dfrac{x-1+\ln x}{x}, \quad x>0 \implies \text{signe}(f'') = \text{signe}(x-1+\ln x)'
            note: "Denominateur positif : le signe est celui du numerateur (question precedente)."
          - math: 'f(1) = 1+\tfrac12-\ln 1+\tfrac12(\ln 1)^2 = \tfrac32'
            note: "Minimum de f, atteint en x=1 : f decroit de +infini a 3/2, puis croit de 3/2 a +infini."
      - id: q9
        stem: |-
          **(0,5 pt)** Montrer que $f''(x) = \dfrac{2 - \ln x}{x^2}$ pour tout $x$ de $]0,+\infty[$.
        reasoning: |-
          On dérive $f'(x)=1-\frac1x+\frac{\ln x}{x}$. Le terme $-\frac1x$ donne $\frac1{x^2}$. Le terme $\frac{\ln x}{x}$ est un quotient : $\left(\frac{\ln x}{x}\right)'=\frac{\frac1x\cdot x-\ln x}{x^2}=\frac{1-\ln x}{x^2}$ (dérivée d'un quotient, hors socle de cette leçon — vu au chapitre « Dérivabilité et étude de fonctions »). On additionne et on regroupe sur $x^2$.
        steps:
          - math: 'f''''(x) = \dfrac{1}{x^2}+\dfrac{1-\ln x}{x^2}'
            note: "Derivee de -1/x, plus derivee du quotient ln x sur x."
          - math: 'f''''(x) = \dfrac{2-\ln x}{x^2}'
            note: "On regroupe les numerateurs."
      - id: q10
        stem: |-
          **(0,5 pt)** En déduire que $(C)$ admet un point d'inflexion dont on déterminera les coordonnées.
        reasoning: |-
          Un point d'inflexion est un point où $f''$ change de signe. Le dénominateur $x^2>0$ ne change jamais de signe : tout dépend du numérateur $2-\ln x$, qui s'annule en $x=e^2$ en passant du positif au négatif. La convexité change bien là : c'est un point d'inflexion. On calcule l'ordonnée en remplaçant $\ln(e^2)=2$.
        steps:
          - math: 'f''''(x)=0 \iff \ln x = 2 \iff x=e^2'
            note: "f'' change de signe en x = e au carre : point d'inflexion."
          - math: 'f(e^2) = e^2+\tfrac12-2+\tfrac12\cdot 2^2 = e^2+\tfrac12'
            note: "ln(e^2)=2. Point d'inflexion I(e^2 , e^2 + 1/2)."
      - id: q11
        part: "Première partie — Position relative et aire"
        stem: |-
          **(0,5 pt)** Montrer que pour tout $x$ de $]0,+\infty[$, $f(x) - x = \dfrac{1}{2}(\ln x - 1)^2$ et déduire la position relative de $(C)$ et $(\Delta)$.
        reasoning: |-
          On calcule $f(x)-x=\frac12-\ln x+\frac12(\ln x)^2$ et on y reconnaît un carré : $(\ln x)^2-2\ln x+1=(\ln x-1)^2$. Un carré est toujours $\ge0$, donc $f(x)\ge x$ : $(C)$ reste au-dessus de $(\Delta)$ sur tout le domaine, avec contact seulement quand le carré est nul, c'est-à-dire $\ln x=1$, soit $x=e$.
        steps:
          - math: 'f(x)-x = \tfrac12-\ln x+\tfrac12(\ln x)^2 = \tfrac12\left[(\ln x)^2-2\ln x+1\right] = \tfrac12(\ln x-1)^2'
            note: "Identite remarquable : carre parfait."
          - math: '\tfrac12(\ln x-1)^2 \ge 0 \implies f(x)\ge x, \quad \text{egalite} \iff x=e'
            note: "(C) au-dessus de (Delta) partout, contact unique en (e,e)."
      - id: q12
        stem: |-
          **(1 pt)** Construire $(\Delta)$ et $(C)$ dans le même repère $(O,\vec{i},\vec{j})$.
        reasoning: |-
          On ne construit pas au hasard : on reporte les éléments déjà établis. La droite $(\Delta):y=x$ d'abord. Pour $(C)$ : l'asymptote verticale $x=0$ (question 1), le minimum $\left(1,\frac32\right)$ (question sur les variations), le point d'inflexion $I\left(e^2,e^2+\frac12\right)$ (question précédente), le fait que $(C)$ reste au-dessus de $(\Delta)$ en la touchant seulement en $(e,e)$, et la branche parabolique de direction $(\Delta)$ au voisinage de $+\infty$ (la courbe s'écarte de plus en plus au-dessus de la première bissectrice). $(C)$ descend de $+\infty$ le long de $x=0$ jusqu'au minimum en $1$, puis remonte en restant au-dessus de $(\Delta)$.
      - id: q13
        stem: |-
          **(0,5 pt)** Montrer que la fonction $H : x \mapsto x\ln x - x$ est une primitive de la fonction $h : x \mapsto \ln x$ sur $]0,+\infty[$.
        reasoning: |-
          Montrer que $H$ est une primitive de $h$, c'est vérifier $H'=h$. On dérive $x\ln x$ comme un produit : $(x\ln x)'=1\cdot\ln x+x\cdot\frac1x=\ln x+1$ (règle du produit, hors socle de cette leçon). En retirant la dérivée de $-x$, qui vaut $-1$, il reste $\ln x$.
        steps:
          - math: '(x\ln x)'' = \ln x + x\cdot\dfrac1x = \ln x+1'
            note: "Derivee d'un produit u=x, v=ln x."
          - math: 'H''(x) = (\ln x+1)-1 = \ln x = h(x)'
            note: "H'=h sur ]0,+infini[ : H est une primitive de h."
      - id: q14
        stem: |-
          **(0,75 pt)** À l'aide d'une intégration par parties, montrer que $\displaystyle\int_1^e (\ln x)^2\, dx = e - 2$.
        reasoning: |-
          L'intégration par parties (technique du chapitre « Calcul intégral ») lit $(\ln x)^2=\ln x\cdot\ln x$ : on dérive $u=\ln x$ (plus simple, $u'=\frac1x$) et on primitive $v'=\ln x$ en $v=H(x)=x\ln x-x$ (question précédente). Le crochet $\big[\ln x\,(x\ln x-x)\big]_1^e$ s'annule aux deux bornes ($\ln e=1$ et $e-e=0$ ; $\ln1=0$). Il reste $-\int_1^e\frac1x(x\ln x-x)\,dx=-\int_1^e(\ln x-1)\,dx$, qu'on calcule avec $H$.
        steps:
          - math: 'u=\ln x,\ u''=\dfrac1x\ ; \quad v''=\ln x,\ v=H(x)=x\ln x-x'
            note: "Choix IPP : deriver ln x, primitiver ln x via H."
          - math: '\int_1^e(\ln x)^2\,dx = \big[\ln x\,(x\ln x-x)\big]_1^e - \int_1^e\dfrac1x(x\ln x-x)\,dx'
            note: "Formule de l'IPP. Le crochet vaut 0 aux deux bornes."
          - math: '\int_1^e(\ln x-1)\,dx = \big[x\ln x-2x\big]_1^e = (e-2e)-(0-2) = 2-e'
            note: "Une primitive de ln x - 1 est x ln x - 2x."
          - math: '\int_1^e(\ln x)^2\,dx = 0-(2-e) = e-2'
            note: "Crochet nul moins (2-e)."
      - id: q15
        stem: |-
          **(0,5 pt)** Calculer en $\text{cm}^2$ l'aire du domaine plan limité par $(C)$ et $(\Delta)$ et les droites d'équations $x = 1$ et $x = e$.
        reasoning: |-
          Sur $[1,e]$, $(C)$ est au-dessus de $(\Delta)$ (question sur la position relative), donc l'aire vaut $\int_1^e(f(x)-x)\,dx=\int_1^e\frac12(\ln x-1)^2\,dx$. On développe le carré et on réutilise $\int_1^e(\ln x)^2\,dx=e-2$ (question précédente) et $\int_1^e\ln x\,dx=[H]_1^e=1$. Le repère étant orthonormé d'unité $1$ cm, une unité d'aire vaut $1\ \text{cm}^2$.
        steps:
          - math: '\mathcal{A} = \int_1^e(f(x)-x)\,dx = \int_1^e\left(\tfrac12(\ln x)^2-\ln x+\tfrac12\right)dx'
            note: "(C) au-dessus de (Delta) sur [1,e] : aire = integrale de f(x)-x."
          - math: '\int_1^e \ln x\,dx = \big[x\ln x-x\big]_1^e = (e-e)-(0-1) = 1'
            note: "Avec la primitive H."
          - math: '\mathcal{A} = \tfrac12(e-2)-1+\tfrac12(e-1) = e-\tfrac52'
            note: "En reutilisant les deux integrales de reference."
          - math: '\mathcal{A} = \left(e-\tfrac52\right)\text{cm}^2 \approx 0{,}22\ \text{cm}^2'
            note: "Unite orthonormee 1 cm : aire positive, comme attendu."
      - id: q16
        part: "Deuxième partie — La suite $u_0=1$, $u_{n+1}=f(u_n)$"
        stem: |-
          Soit $(u_n)$ la suite numérique définie par : $u_0 = 1$ et $u_{n+1} = f(u_n)$ pour tout $n$ de $\mathbb{N}$.

          **(0,5 pt)** Montrer par récurrence que $1 \le u_n \le e$ pour tout $n$ de $\mathbb{N}$.
        reasoning: |-
          Récurrence d'encadrement classique pour $u_{n+1}=f(u_n)$ (chapitre « Suites numériques »). Initialisation : $u_0=1$ vérifie $1\le1\le e$. Hérédité : on suppose $1\le u_n\le e$, et on exploite que $f$ est croissante sur $[1,e]$ (établi dans le tableau de variations, valable car $[1,e]\subset[1,+\infty[$) : $f(1)\le f(u_n)\le f(e)$. Avec $f(1)=\frac32$ et $f(e)=e+\frac12-1+\frac12=e$, on obtient $\frac32\le u_{n+1}\le e$, et comme $\frac32\ge1$, l'encadrement passe au rang $n+1$.
        steps:
          - math: '\text{Initialisation : } u_0=1, \quad 1\le1\le e \ \checkmark'
            note: "Rang 0."
          - math: 'f \text{ croissante sur } [1,e] \implies f(1)\le f(u_n)\le f(e)'
            note: "Monotonie de f appliquee a l'hypothese de recurrence."
          - math: 'f(1)=\tfrac32, \quad f(e)=e \implies 1\le\tfrac32\le u_{n+1}\le e'
            note: "f(e)=e car ln e=1. L'encadrement passe au rang n+1."
      - id: q17
        stem: |-
          **(0,5 pt)** Montrer que la suite $(u_n)$ est croissante.
        reasoning: |-
          On étudie le signe de $u_{n+1}-u_n=f(u_n)-u_n$. C'est exactement la différence calculée plus haut : $f(x)-x=\frac12(\ln x-1)^2\ge0$. Appliquée à $x=u_n$, elle donne $u_{n+1}-u_n\ge0$.
        steps:
          - math: 'u_{n+1}-u_n = f(u_n)-u_n = \tfrac12(\ln u_n-1)^2 \ge 0'
            note: "Reutilisation de f(x)-x = 1/2 (ln x - 1) carre, un carre donc positif."
      - id: q18
        stem: |-
          **(0,5 pt)** En déduire que la suite $(u_n)$ est convergente.
        reasoning: |-
          $(u_n)$ est croissante (question précédente) et majorée par $e$ (question sur l'encadrement) : le théorème de convergence monotone (chapitre « Suites numériques ») conclut directement.
        steps:
          - math: '(u_n) \text{ croissante et majoree par } e \implies (u_n) \text{ converge}'
            note: "Theoreme de la convergence monotone."
      - id: q19
        stem: |-
          **(0,75 pt)** Calculer la limite de la suite $(u_n)$.
        reasoning: |-
          Notons $\ell$ la limite. En passant l'encadrement $1\le u_n\le e$ à la limite, $\ell\in[1,e]$, en particulier $\ell>0$. Comme $f$ est continue (composée/somme de fonctions continues sur $]0,+\infty[$) et $u_{n+1}=f(u_n)$, en passant à la limite : $\ell=f(\ell)$. Or $f(\ell)=\ell\iff\frac12(\ln\ell-1)^2=0\iff\ln\ell=1\iff\ell=e$, et $e\in[1,e]$.
        steps:
          - math: '1\le u_n\le e \implies \ell\in[1,e]\ ; \quad u_{n+1}=f(u_n), f \text{ continue} \implies \ell=f(\ell)'
            note: "La limite est un point fixe de f, dans [1,e]."
          - math: 'f(\ell)=\ell \iff \tfrac12(\ln\ell-1)^2=0 \iff \ell=e'
            note: "Reutilisation de f(x)-x = 1/2 (ln x -1) carre. Comme e appartient a [1,e] : lim u_n = e."

  # ── bk-2023-n-x4 — 2023 N, Problème : f avec ln, 2 points d'inflexion, aire, suite ──
  - id: bk-2023-n-x4
    title: "Étude d'une fonction avec ln, deux points d'inflexion, aire par IPP et suite récurrente"
    source:
      year: 2023
      session: normale
      filiere: "SExp"
      exercise_label: "Problème"
    bareme_total: 11
    duration_min: 75
    sourcing:
      status: sourced
      note: >-
        2023 session normale — examen national SExp BIOF (SVT et Sciences
        Physiques), Mathématiques (3 h, coef 7), code NS 22F, AlloSchool
        element/137482 (Problème, 11 points, pages 3-4 du scan). Transcription
        fidèle de l'entrée « vérifié » de
        docs/sujets/maths/fonction-logarithme.md, section « 2023 — session
        normale — Problème » (re-fetch AlloSchool indépendant element/137482
        → course-438/upload-84924 + diff caractère-par-caractère conforme au
        scan, y compris le tableau de variations de $f'$ et la courbe
        $(C_g)$, agent-vérificateur-adversarial, 2026-07-12). Recoupe aussi
        `calcul-integral` (Q6, IPP) et `suites-numeriques` (Q7).
    intro: |-
      **Problème (examen national, session normale 2023, Sciences Expérimentales).**

      On considère la fonction numérique $f$ définie sur $]0,+\infty[$ par $f(x) = 2 - \dfrac{2}{x} + (1 - \ln x)^2$, et $(C_f)$ sa courbe représentative dans un repère orthonormé $(O,\vec{i},\vec{j})$ *(unité : 1 cm)*.

      Deux techniques reviennent tout au long du problème et méritent d'être repérées d'emblée : la substitution $t=\sqrt x$ pour ramener une limite en $x$ à une croissance comparée de référence (R4), et la lecture du signe de $f''$ directement sur les VARIATIONS déjà connues de $f'$ (pas besoin de recalculer $f''$ à chaque fois : $f''=(f')'$).
    questions:
      - id: q1
        part: "Partie 1 — Limites, asymptote et branche parabolique"
        stem: |-
          **(0,25 pt)** Vérifier que pour tout $x \in ]0,+\infty[$ : $f(x) = \dfrac{3x - 2 - 2x\ln x + x(\ln x)^2}{x}$.
        reasoning: |-
          Vérification directe : on développe $(1-\ln x)^2=1-2\ln x+(\ln x)^2$, on ajoute $2-\frac2x$, puis on réduit tout au même dénominateur $x$.
        steps:
          - math: 'f(x) = 2-\dfrac2x+(1-\ln x)^2 = 3-\dfrac2x-2\ln x+(\ln x)^2'
            note: "On developpe le carre : (1-ln x) carre = 1 - 2 ln x + (ln x) carre."
          - math: 'f(x) = \dfrac{3x-2-2x\ln x+x(\ln x)^2}{x}'
            note: "Chaque terme mis sur le denominateur commun x."
      - id: q2
        stem: |-
          **(0,5 pt)** Montrer que $\displaystyle\lim_{x \to 0^+} x(\ln x)^2 = 0$ et que $\displaystyle\lim_{x \to +\infty} \dfrac{(\ln x)^2}{x} = 0$ *(On peut poser : $t = \sqrt{x}$)*.
        reasoning: |-
          Les deux limites se ramènent, via $t=\sqrt x$, aux résultats de référence du cours (R4). En $0^+$ : $x=t^2$, $t\to0^+$, et $\ln x=2\ln t$, donc $x(\ln x)^2=4t^2(\ln t)^2=4(t\ln t)^2$ ; or $t\ln t\to0$ (corollaire des croissances comparées, R4), donc le carré aussi. En $+\infty$ : $t\to+\infty$, $\frac{(\ln x)^2}{x}=\frac{4(\ln t)^2}{t^2}=4\left(\frac{\ln t}{t}\right)^2\to0$ par croissances comparées (R4).
        steps:
          - math: 't=\sqrt{x}\to0^+, \quad x(\ln x)^2 = t^2(2\ln t)^2 = 4(t\ln t)^2 \to 0'
            note: "t ln t tend vers 0 en 0+ (corollaire des croissances comparees, R4)."
          - math: 't=\sqrt{x}\to+\infty, \quad \dfrac{(\ln x)^2}{x} = \dfrac{4(\ln t)^2}{t^2} = 4\left(\dfrac{\ln t}{t}\right)^2 \to 0'
            note: "Croissances comparees en +infini (R4)."
      - id: q3
        stem: |-
          **(0,5 pt)** Déduire que $\displaystyle\lim_{x \to 0^+} f(x) = -\infty$, puis donner une interprétation géométrique du résultat.
        reasoning: |-
          On repart de la forme de la question 1 : $f(x)=\frac{3x-2-2x\ln x+x(\ln x)^2}{x}$. Au numérateur, quand $x\to0^+$ : $3x\to0$, $x\ln x\to0$ (limite de référence, R4) et $x(\ln x)^2\to0$ (question précédente) — il ne reste que le $-2$. Le numérateur tend donc vers $-2$, et le dénominateur $x\to0^+$ : le quotient tend vers $-\infty$. Une limite infinie en une borne finie du domaine signifie que $x=0$ est asymptote verticale à $(C_f)$.
        steps:
          - math: '3x-2-2x\ln x+x(\ln x)^2 \xrightarrow[x\to0^+]{} 0-2-2\times0+0 = -2'
            note: "x ln x tend vers 0 (R4), x (ln x) carre tend vers 0 (question precedente)."
          - math: '\lim_{x\to0^+} f(x) = \lim_{x\to0^+}\dfrac{-2}{x} = -\infty'
            note: "Numerateur vers -2, denominateur vers 0+ : asymptote verticale x=0."
      - id: q4
        stem: |-
          **(0,75 pt)** Calculer $\displaystyle\lim_{x \to +\infty} f(x)$, puis montrer que la courbe $(C_f)$ admet une branche parabolique de direction l'axe des abscisses au voisinage de $+\infty$.
        reasoning: |-
          Pour la limite : $-\frac2x\to0$ et $(1-\ln x)^2\to+\infty$ (car $\ln x\to+\infty$), donc $f(x)\to+\infty$. Pour la branche parabolique de direction l'axe des abscisses, il faut montrer $\frac{f(x)}{x}\to0$ (la courbe monte, mais infiniment plus lentement qu'aucune droite non horizontale). On développe $(1-\ln x)^2/x$ et on reconnaît des croissances comparées : $\frac{(\ln x)^2}{x}\to0$ (question 2) et $\frac{\ln x}{x}\to0$ (R4).
        steps:
          - math: '\lim_{x\to+\infty}\left(2-\dfrac2x\right) = 2, \quad \lim_{x\to+\infty}(1-\ln x)^2 = +\infty \implies \lim_{x\to+\infty} f(x) = +\infty'
            note: "(1-ln x) carre part vers +infini."
          - math: '\dfrac{f(x)}{x} = \dfrac2x-\dfrac{2}{x^2}+\dfrac{(\ln x)^2-2\ln x+1}{x} \xrightarrow[x\to+\infty]{} 0'
            note: "Chaque terme tend vers 0 (croissances comparees, R4, et question 2) : branche parabolique de direction l'axe des abscisses."
      - id: q5
        part: "Partie 2 — Dérivée, variations et concavité"
        stem: |-
          **(0,5 pt)** Montrer que pour tout $x \in ]0,+\infty[$ : $f'(x) = \dfrac{2(1 - x + x\ln x)}{x^2}$.
        reasoning: |-
          On dérive $f(x)=2-\frac2x+(1-\ln x)^2$ terme à terme. $-\frac2x$ se dérive en $\frac{2}{x^2}$. Le carré est une composée : $\left((1-\ln x)^2\right)'=2(1-\ln x)\cdot\left(-\frac1x\right)$, en appliquant $(\ln u)'=\frac{u'}{u}$ à l'intérieur $1-\ln x$ (règle du R3, ici combinée à la règle de la composée par un carré). On réduit ensuite au même dénominateur $x^2$.
        steps:
          - math: 'f''(x) = \dfrac{2}{x^2} - 2(1-\ln x)\cdot\dfrac1x = \dfrac{2}{x^2}-\dfrac{2(1-\ln x)}{x}'
            note: "Derivee de -2/x, et du carre (1-ln x) au carre via (ln u)'=u'/u (R3)."
          - math: 'f''(x) = \dfrac{2-2x(1-\ln x)}{x^2} = \dfrac{2-2x+2x\ln x}{x^2} = \dfrac{2(1-x+x\ln x)}{x^2}'
            note: "Reduction au denominateur commun x carre, puis facteur 2."
      - id: q6
        stem: |-
          En exploitant le tableau de variation ci-dessous, de la fonction dérivée $f'$ de $f$ sur $]0,+\infty[$ *(on donne $\beta \approx 4{,}9$)* : $f'$ part de $+\infty$ en $0^+$, décroît jusqu'à $0$ en $x=1$, croît jusqu'à $f'(\beta)$, puis décroît vers $0$ en $+\infty$ — autrement dit $f'(x) \ge 0$ sur $]0,+\infty[$, nul seulement en $x=1$.

          **(0,5 pt)** Prouver que $f$ est strictement croissante sur $]0,+\infty[$ puis dresser le tableau de variations de $f$.
        reasoning: |-
          Le tableau donné dit exactement $f'(x)\ge0$ sur tout le domaine, avec égalité seulement au point isolé $x=1$ : une dérivée positive ou nulle, nulle en un point isolé seulement, suffit pour une stricte croissance. Le tableau de variations de $f$ tient donc en une seule flèche montante, de $\displaystyle\lim_{x\to0^+}f(x)=-\infty$ (question 3) à $\displaystyle\lim_{x\to+\infty}f(x)=+\infty$ (question 4), avec un point de repère utile $f(1)=2-2+(1-0)^2=1$.
        steps:
          - math: 'f''(x)\ge0 \text{ sur } ]0,+\infty[, \text{ nul seulement en } x=1 \implies f \text{ strictement croissante}'
            note: "Derivee positive ou nulle, nulle en un point isole : stricte croissance."
          - math: 'f(1) = 2-2+(1-\ln1)^2 = 1'
            note: "Point de repere. f croit de -infini (en 0+) a +infini, en passant par 1 en x=1."
      - id: q7
        stem: |-
          **(0,5 pt)** Donner le tableau de signe de la dérivée seconde $f''$ de la fonction $f$ sur $]0,+\infty[$.
        reasoning: |-
          Pas besoin de recalculer $f''$ explicitement : $f''=(f')'$, donc le signe de $f''$ est celui de la variation de $f'$, déjà donnée dans le tableau : $f'$ décroît sur $]0,1[$ (donc $f''<0$), croît sur $]1,\beta[$ (donc $f''>0$), décroît sur $]\beta,+\infty[$ (donc $f''<0$), avec $f''(1)=0$ et $f''(\beta)=0$ aux deux points où $f'$ change de sens de variation.
        steps:
          - math: 'f'' \text{ decroissante sur } ]0,1[ \implies f''''(x)<0 \text{ sur } ]0,1['
            note: "Le signe de f'' est celui de la variation de f'."
          - math: 'f'' \text{ croissante sur } ]1,\beta[ \implies f''''(x)>0 \text{ sur } ]1,\beta['
            note: "f' croit : sa derivee f'' est positive."
          - math: 'f'' \text{ decroissante sur } ]\beta,+\infty[ \implies f''''(x)<0 \text{ sur } ]\beta,+\infty[, \quad f''''(1)=f''''(\beta)=0'
            note: "Aux deux points ou f' change de sens (x=1 et x=beta), f'' s'annule."
      - id: q8
        stem: |-
          **(1 pt)** Déduire la concavité de la courbe $(C_f)$ en précisant les abscisses de ses deux points d'inflexion.
        reasoning: |-
          $f''<0$ signifie $(C_f)$ concave, $f''>0$ signifie $(C_f)$ convexe. D'après le tableau de signe précédent : $(C_f)$ est concave sur $]0,1[$, convexe sur $]1,\beta[$, de nouveau concave sur $]\beta,+\infty[$. La concavité change en $x=1$ et en $x=\beta$ : ce sont les abscisses des deux points d'inflexion de $(C_f)$.
        steps:
          - math: '(C_f) \text{ concave sur } ]0,1[, \text{ convexe sur } ]1,\beta[, \text{ concave sur } ]\beta,+\infty['
            note: "Lecture directe du tableau de signe de f'' (question precedente)."
          - math: '\text{Points d''inflexion en } x=1 \text{ et } x=\beta'
            note: "La concavite change exactement la ou f'' s'annule en changeant de signe."
      - id: q9
        part: "Partie 3 — Position de (Δ) par rapport à (C_f), à partir de (C_g)"
        stem: |-
          La courbe $(C_g)$ ci-contre est la représentation graphique de la fonction $g : x \mapsto f(x) - x$, qui s'annule en $\alpha$ et $1$ ($\alpha \approx 0{,}3$) : $g$ est négative sur $]0,\alpha[$, s'annule en $x=\alpha$, devient positive (maximum faible, ordonnée $<0{,}5$) sur $]\alpha,1[$, s'annule de nouveau en $x=1$, puis reste négative sur $]1,+\infty[$. Soit $(\Delta)$ la droite d'équation $y = x$.

          **(0,5 pt)** À partir de la courbe $(C_g)$, déterminer le signe de la fonction $g$ sur $]0,+\infty[$.
        reasoning: |-
          On lit directement le signe sur la description de $(C_g)$ : elle est sous l'axe des abscisses (négative) avant $\alpha$, au-dessus (positive) entre $\alpha$ et $1$, de nouveau en dessous après $1$.
        steps:
          - math: 'g(x)<0 \text{ sur } ]0,\alpha[, \quad g(x)>0 \text{ sur } ]\alpha,1[, \quad g(x)<0 \text{ sur } ]1,+\infty['
            note: "Lecture directe de la courbe (Cg) : g(alpha)=g(1)=0."
      - id: q10
        stem: |-
          **(0,5 pt)** Déduire que la droite $(\Delta)$ est en dessous de $(C_f)$ sur l'intervalle $[\alpha,1]$ et au-dessus de $(C_f)$ sur les intervalles $]0,\alpha]$ et $[1,+\infty[$.
        reasoning: |-
          $g(x)=f(x)-x$ ; $g>0$ signifie $f(x)>x$, c'est-à-dire $(C_f)$ au-dessus de $(\Delta)$, donc $(\Delta)$ en dessous. $g<0$ signifie l'inverse. D'après la question précédente : $g>0$ sur $]\alpha,1[$ donne $(\Delta)$ en dessous de $(C_f)$ sur $[\alpha,1]$ ; $g<0$ sur $]0,\alpha[$ et $]1,+\infty[$ donne $(\Delta)$ au-dessus de $(C_f)$ sur ces deux intervalles.
        steps:
          - math: 'g(x)>0 \iff f(x)>x \iff (C_f) \text{ au-dessus de } (\Delta)'
            note: "g = f(x) - x : son signe donne la position relative."
          - math: 'g>0 \text{ sur } ]\alpha,1[ \implies (\Delta) \text{ en dessous de } (C_f) \text{ sur } [\alpha,1]'
            note: "Traduction geometrique directe."
          - math: 'g<0 \text{ sur } ]0,\alpha[ \text{ et } ]1,+\infty[ \implies (\Delta) \text{ au-dessus de } (C_f) \text{ sur ces intervalles}'
            note: "Meme traduction, signe oppose."
      - id: q11
        part: "Partie 4 — Construction de la courbe"
        stem: |-
          **(1,5 pt)** Construire la courbe $(C_f)$ et la droite $(\Delta)$ dans le repère $(O,\vec{i},\vec{j})$. *(On prend : $\alpha \approx 0{,}3$, $\beta \approx 4{,}9$ et $f(\beta) \approx 1{,}9$)*.
        reasoning: |-
          On reporte tout ce qui a déjà été établi. La droite $(\Delta):y=x$ d'abord. Pour $(C_f)$ : l'asymptote verticale $x=0$ avec $f\to-\infty$ (question 3), la stricte croissance sur tout le domaine (question 6) avec $f(1)=1$, les deux points d'inflexion en $x=1$ et $x=\beta\approx4{,}9$ (avec $f(\beta)\approx1{,}9$ donné), la branche parabolique de direction l'axe des abscisses au voisinage de $+\infty$ (question 4 — la courbe s'aplatit), et la position par rapport à $(\Delta)$ (question précédente : $(C_f)$ en dessous de $(\Delta)$ avant $\alpha$ et après $1$, au-dessus entre $\alpha$ et $1$). La courbe monte de $-\infty$ le long de $x=0$ jusqu'à croiser $(\Delta)$ en $\alpha$, reste au-dessus entre $\alpha$ et $1$, la recroise en $1$, puis continue de monter en dessous de $(\Delta)$, de plus en plus lentement.
      - id: q12
        part: "Partie 5 — Primitive, intégration par parties et aire"
        stem: |-
          **(0,5 pt)** Vérifier que la fonction $x \mapsto 2x - x\ln x$ est une primitive de la fonction $x \mapsto 1 - \ln x$ sur $[\alpha,1]$.
        reasoning: |-
          On dérive $F(x)=2x-x\ln x$ : $F'(x)=2-\left(\ln x+x\cdot\frac1x\right)=2-\ln x-1=1-\ln x$ (dérivée d'un produit pour $x\ln x$, hors socle de cette leçon). C'est exactement la fonction annoncée.
        steps:
          - math: 'F(x)=2x-x\ln x, \quad F''(x) = 2-\left(\ln x + x\cdot\dfrac1x\right) = 2-\ln x-1'
            note: "Derivee d'un produit pour x ln x."
          - math: 'F''(x) = 1-\ln x'
            note: "F est bien une primitive de 1 - ln x sur [alpha,1]."
      - id: q13
        stem: |-
          **(1 pt)** En utilisant une intégration par parties, montrer que $\displaystyle\int_\alpha^1 (1 - \ln x)^2\, dx = 5(1 - \alpha) + \alpha(4 - \ln\alpha)\ln\alpha$.
        reasoning: |-
          On lit $(1-\ln x)^2=(1-\ln x)\cdot(1-\ln x)$ : on dérive $u=1-\ln x$ ($u'=-\frac1x$) et on primitive $v'=1-\ln x$ en $v=F(x)=2x-x\ln x$ (question précédente) — technique d'IPP du chapitre « Calcul intégral ». Le crochet $\big[(1-\ln x)(2x-x\ln x)\big]_\alpha^1$ vaut $2$ en $x=1$ (car $\ln1=0$). Il reste $+\int_\alpha^1(2x-x\ln x)\cdot\frac1x\,dx=\int_\alpha^1(2-\ln x)\,dx$, dont une primitive est $3x-x\ln x$.
        steps:
          - math: 'u=1-\ln x,\ u''=-\dfrac1x\ ; \quad v''=1-\ln x,\ v=F(x)=2x-x\ln x'
            note: "Choix IPP : deriver 1-ln x, primitiver 1-ln x via F (question precedente)."
          - math: '\int_\alpha^1(1-\ln x)^2 dx = \big[(1-\ln x)(2x-x\ln x)\big]_\alpha^1 + \int_\alpha^1(2-\ln x)\,dx'
            note: "Formule de l'IPP : le u prime negatif transforme le moins en plus."
          - math: '\big[(1-\ln x)(2x-x\ln x)\big]_\alpha^1 = 2 - \alpha(1-\ln\alpha)(2-\ln\alpha) = 2-2\alpha+3\alpha\ln\alpha-\alpha(\ln\alpha)^2'
            note: "Valeur en 1 : (1-0)(2-0)=2. Valeur en alpha developpee."
          - math: '\int_\alpha^1(2-\ln x)\,dx = \big[3x-x\ln x\big]_\alpha^1 = 3-(3\alpha-\alpha\ln\alpha) = 3-3\alpha+\alpha\ln\alpha'
            note: "Primitive de 2 - ln x : 3x - x ln x."
          - math: '\int_\alpha^1(1-\ln x)^2 dx = 5-5\alpha+4\alpha\ln\alpha-\alpha(\ln\alpha)^2 = 5(1-\alpha)+\alpha(4-\ln\alpha)\ln\alpha'
            note: "Somme des deux morceaux, factoree."
      - id: q14
        stem: |-
          **(0,75 pt)** Déduire en fonction de $\alpha$ l'aire de la partie du plan délimitée par la courbe $(C_f)$, l'axe des abscisses et les droites d'équations $x = \alpha$ et $x = 1$.
        reasoning: |-
          Sur $[\alpha,1]$, $(\Delta):y=x$ est en dessous de $(C_f)$ (question 10), et $y=x>0$ sur ce domaine puisque $x>0$ ; donc $f(x)\ge x>0$ sur $[\alpha,1]$ : $(C_f)$ reste au-dessus de l'axe des abscisses, pas besoin de valeur absolue. L'aire est $\int_\alpha^1 f(x)\,dx$, qu'on décompose en trois morceaux élémentaires, dont le carré $(1-\ln x)^2$ calculé à la question précédente.
        steps:
          - math: '\int_\alpha^1 f(x)\,dx = \int_\alpha^1 2\,dx - \int_\alpha^1\dfrac2x\,dx + \int_\alpha^1(1-\ln x)^2\,dx'
            note: "On decompose f(x) = 2 - 2/x + (1-ln x) carre (position au-dessus de l'axe garantie par la question 10)."
          - math: '\int_\alpha^1 2\,dx = 2(1-\alpha), \qquad \int_\alpha^1\dfrac2x\,dx = 2\big[\ln x\big]_\alpha^1 = -2\ln\alpha'
            note: "Deux integrales elementaires."
          - math: '\mathcal{A} = 2(1-\alpha)+2\ln\alpha+5(1-\alpha)+\alpha(4-\ln\alpha)\ln\alpha'
            note: "On reutilise le resultat de la question precedente pour le troisieme morceau."
          - math: '\mathcal{A} = 7(1-\alpha) + 2(1+2\alpha)\ln\alpha - \alpha(\ln\alpha)^2 \quad \text{cm}^2'
            note: "Regroupement final en fonction de alpha (aire en cm carre, repere orthonorme unite 1 cm)."
      - id: q15
        part: "Partie 6 — La suite (u_n)"
        stem: |-
          Soit la suite numérique $(u_n)$ définie par $u_0 \in ]\alpha,1[$ et la relation $u_{n+1} = f(u_n)$, pour tout $n \in \mathbb{N}$.

          **(0,5 pt)** Montrer par récurrence que $\alpha < u_n < 1$, pour tout $n$ de $\mathbb{N}$.
        reasoning: |-
          Le point clé : $\alpha$ et $1$ sont tous deux des zéros de $g=f(x)-x$ (donnés dans l'énoncé), donc des points fixes de $f$ : $f(\alpha)=\alpha$ et $f(1)=1$ (déjà calculé, question 6). Comme $f$ est strictement croissante sur $]0,+\infty[$ (question 6), pour $x\in]\alpha,1[$ : $f(\alpha)<f(x)<f(1)$, c'est-à-dire $\alpha<f(x)<1$. Initialisation : $u_0\in]\alpha,1[$ donné. Hérédité : si $\alpha<u_n<1$, la stricte croissance donne $\alpha=f(\alpha)<f(u_n)<f(1)=1$, soit $\alpha<u_{n+1}<1$.
        steps:
          - math: 'f(\alpha)=\alpha, \quad f(1)=1 \quad (\alpha \text{ et } 1 \text{ sont des zeros de } g=f(x)-x)'
            note: "Points fixes de f, donnes par l'enonce de la question 9."
          - math: '\alpha<u_n<1, \ f \text{ strict. croissante} \implies f(\alpha)<f(u_n)<f(1) \implies \alpha<u_{n+1}<1'
            note: "On applique la stricte croissance de f (question 6) a l'hypothese de recurrence."
      - id: q16
        stem: |-
          **(0,5 pt)** Montrer que la suite $(u_n)$ est croissante. *(on peut utiliser la question 4) b))*
        reasoning: |-
          $u_{n+1}-u_n=f(u_n)-u_n=g(u_n)$. La question 10 donne $(\Delta)$ en dessous de $(C_f)$ sur $[\alpha,1]$, c'est-à-dire $g>0$ sur cet intervalle. Comme $u_n\in]\alpha,1[$ (question précédente), $g(u_n)>0$, donc $u_{n+1}>u_n$.
        steps:
          - math: 'u_{n+1}-u_n = f(u_n)-u_n = g(u_n)'
            note: "Difference consecutive = g evaluee en u_n."
          - math: 'u_n\in\,]\alpha,1[ \text{ et } g>0 \text{ sur } ]\alpha,1[ \ (\text{question 9-10}) \implies g(u_n)>0 \implies u_{n+1}>u_n'
            note: "(u_n) est croissante."
      - id: q17
        stem: |-
          **(0,75 pt)** En déduire que la suite $(u_n)$ est convergente et calculer sa limite.
        reasoning: |-
          $(u_n)$ est croissante (question précédente) et majorée par $1$ (question sur l'encadrement) : elle converge, vers une limite $\ell$ avec $\ell\le1$ (passage à la limite dans $u_n<1$) et $\ell\ge u_0>\alpha$ (suite croissante à partir de $u_0$). Comme $f$ est continue et $u_{n+1}=f(u_n)$, $\ell$ vérifie $\ell=f(\ell)$, c'est-à-dire $g(\ell)=0$. Les seuls zéros de $g$ sur $]0,+\infty[$ sont $\alpha$ et $1$ (donnés) ; comme $\ell>\alpha$, il reste $\ell=1$.
        steps:
          - math: '(u_n) \text{ croissante et majoree par } 1 \implies (u_n) \text{ converge vers } \ell, \quad \ell>\alpha, \ \ell\le1'
            note: "Theoreme de la convergence monotone ; u_0 > alpha et suite croissante donnent la limite > alpha."
          - math: '\ell=f(\ell) \iff g(\ell)=0 \iff \ell=\alpha \text{ ou } \ell=1'
            note: "Les seuls zeros de g sont alpha et 1 (donnes dans l'enonce)."
          - math: '\ell>\alpha \implies \ell = 1'
            note: "On elimine alpha : la limite de la suite est 1."

  # ── bk-2021-n-x4 — 2021 N, Problème : f prolongee en 0, minimum/inegalite, reciproque, h par morceaux ──
  - id: bk-2021-n-x4
    title: "Étude d'une fonction avec ln prolongée en 0, minimum et inégalité, fonction réciproque et fonction par morceaux"
    source:
      year: 2021
      session: normale
      filiere: "SExp"
      exercise_label: "Problème"
    bareme_total: 9
    duration_min: 54
    sourcing:
      status: sourced
      note: >-
        2021 session normale — examen national SExp BIOF (SVT et Sciences
        Physiques), Mathématiques (3 h, coef 7), code NS 22F, AlloSchool
        element/127180 (Problème, 9 points, pages 3-4 du scan). Transcription
        fidèle de l'entrée « vérifié » de
        docs/sujets/maths/fonction-logarithme.md, section « 2021 — session
        normale — Problème » (re-fetch AlloSchool indépendant element/127180
        → course-438/upload-84139 + diff caractère-par-caractère conforme au
        scan, agent-vérificateur-adversarial, 2026-08-06 ; mathématiques
        re-dérivées indépendamment par le même agent : f'(x)=2 ln x, minimum
        f(1)=-2, f(x)=0 en x=e, f(x)=x en x=e^{3/2}, IPP
        ∫₁^e x ln x dx=(1+e²)/4, J=[-2,+∞[ pour g). Notation « Etudier » sans
        accent conservée telle quelle aux questions 8-a/8-b (scan, E capital
        sans accent) ; notation « 4.5 » à point décimal conservée telle
        quelle à la question 4-b (scan). Recoupe `calcul-integral` (question
        5, IPP) et `derivabilite-etude-fonctions` (question 7, fonction
        réciproque et son domaine — corollaire du théorème des valeurs
        intermédiaires, hors socle R1–R6 de cette leçon ; question 3-a, taux
        d'accroissement). Pas de volet suites dans ce problème (l'Exercice 2
        autonome du même sujet porte les suites, hors périmètre de cette
        notion — voir suites-numeriques.md). Ne recoupe PAS le sommet de
        leçon r-bac/r-variation (exercises.yaml), qui transcrit le sujet
        2019, différent de celui-ci.
    intro: |-
      **Problème (examen national, session normale 2021, Sciences Expérimentales).**

      Soit $f$ la fonction définie sur $[0,+\infty[$ par : $f(0) = 0$ et $f(x) = 2x\ln x - 2x$ si $x > 0$,

      et $(C)$ sa courbe représentative dans un repère orthonormé $(O, \vec{i}, \vec{j})$ *(unité : 1cm)*.

      Point de vigilance qui traverse tout le problème : $f$ est prolongée par continuité en $0$ — le vérifier est justement la première question. Garde en tête le corollaire de croissances comparées vu en cours (R4) : $x\ln x \to 0$ quand $x\to 0^+$, c'est ce qui rend ce prolongement possible.
    questions:
      - id: q1
        part: "Continuité de f en 0"
        stem: |-
          **(0,5 pt)** Montrer que $f$ est continue à droite au point $0$.
        reasoning: |-
          Continue à droite en $0$ signifie $\displaystyle\lim_{x\to0^+} f(x) = f(0)$. On calcule cette limite avec le corollaire de croissances comparées de la leçon (R4) : $x\ln x \to 0$ quand $x\to0^+$. Comme $f(x)=2x\ln x-2x$ pour $x>0$, chaque terme tend vers $0$, et la limite vaut $f(0)=0$.
        steps:
          - math: '\lim_{x \to 0^+} x\ln x = 0'
            note: "Corollaire de croissances comparees, R4 (limite de reference en 0+)."
          - math: '\lim_{x\to0^+} f(x) = \lim_{x\to0^+}\left(2x\ln x - 2x\right) = 2\times 0 - 0 = 0 = f(0)'
            note: "Les deux termes tendent vers 0 : continuite a droite en 0 verifiee."
      - id: q2
        part: "Limites en +infini et direction asymptotique"
        stem: |-
          **(0,5 pt)** Calculer $\displaystyle\lim_{x \to +\infty} f(x)$.
        reasoning: |-
          On met $2x$ en facteur commun : $f(x)=2x(\ln x - 1)$. Quand $x\to+\infty$, $\ln x\to+\infty$ (R4), donc $\ln x - 1\to+\infty$ : les deux facteurs $2x$ et $\ln x-1$ tendent vers $+\infty$, leur produit aussi. Pas d'indétermination.
        steps:
          - math: 'f(x) = 2x\ln x - 2x = 2x(\ln x - 1)'
            note: "Mise en facteur de 2x."
          - math: '\lim_{x\to+\infty}(\ln x - 1) = +\infty \ \text{(R4)}, \quad \lim_{x\to+\infty} 2x = +\infty \implies \lim_{x\to+\infty} f(x) = +\infty'
            note: "Produit de deux facteurs qui tendent vers plus l'infini."
      - id: q3
        stem: |-
          **(0,5 pt)** Calculer $\displaystyle\lim_{x \to +\infty} \dfrac{f(x)}{x}$ puis interpréter géométriquement le résultat.
        reasoning: |-
          En divisant la forme factorisée de la question précédente par $x$, il reste $\dfrac{f(x)}{x}=2\ln x - 2$, qui tend vers $+\infty$ car $\ln x\to+\infty$ (R4). Quand le rapport $\dfrac{f(x)}{x}$ part lui-même à l'infini, la courbe grossit plus vite que n'importe quelle droite passant par l'origine : c'est une branche parabolique de direction $(O,\vec{j})$ (l'axe des ordonnées).
        steps:
          - math: '\dfrac{f(x)}{x} = \dfrac{2x(\ln x - 1)}{x} = 2\ln x - 2'
            note: "Division par x, x non nul."
          - math: '\lim_{x\to+\infty} (2\ln x - 2) = +\infty \ \text{(R4)}'
            note: "Ln x tend vers plus l'infini."
          - math: '\lim_{x\to+\infty} \dfrac{f(x)}{x} = +\infty \implies (C) \text{ admet une branche parabolique de direction } (O,\vec{j}) \text{ au voisinage de } +\infty'
            note: "Rapport infini : la courbe grossit plus vite que toute droite issue de l'origine, direction verticale."
      - id: q4
        part: "Taux d'accroissement en 0, derivee et tableau de variations"
        stem: |-
          **(0,75 pt)** Calculer $\displaystyle\lim_{x \to 0^+} \dfrac{f(x)}{x}$ et interpréter géométriquement le résultat.
        reasoning: |-
          Comme $f(0)=0$, ce rapport est exactement le taux d'accroissement de $f$ entre $0$ et $x$ : $\dfrac{f(x)}{x}=\dfrac{f(x)-f(0)}{x-0}$. Sur $]0,+\infty[$, $\dfrac{f(x)}{x}=2\ln x-2$ (même calcul que la question précédente, valable ici aussi), qui tend vers $-\infty$ quand $x\to0^+$ car $\ln x\to-\infty$ (R4). Un taux d'accroissement qui part à l'infini signale une tangente verticale : $(C)$ admet une demi-tangente verticale au point $(0,0)$.
        steps:
          - math: '\dfrac{f(x)}{x} = \dfrac{f(x)-f(0)}{x-0} = 2\ln x - 2 \quad (x>0)'
            note: "Taux d'accroissement en 0, meme formule que f(x)/x."
          - math: '\lim_{x\to0^+} (2\ln x - 2) = -\infty \ \text{(R4)}'
            note: "Ln x tend vers moins l'infini en 0+."
          - math: '\lim_{x\to0^+} \dfrac{f(x)}{x} = -\infty \implies (C) \text{ admet une demi-tangente verticale au point } (0,0)'
            note: "Taux d'accroissement infini : tangente verticale a l'origine."
      - id: q5
        stem: |-
          **(0,5 pt)** Calculer $f'(x)$ pour tout $x$ de $]0, +\infty[$.
        reasoning: |-
          $f(x)=2x\ln x-2x$. On dérive $2x\ln x$ comme un produit (dérivée d'un produit, chapitre « Dérivabilité et étude de fonctions », hors socle de cette leçon) : $(2x\ln x)'=2\ln x+2x\cdot\dfrac1x=2\ln x+2$. On retire ensuite la dérivée de $2x$, qui vaut $2$.
        steps:
          - math: '(2x\ln x)'' = 2\ln x + 2x\cdot\dfrac1x = 2\ln x + 2'
            note: "Derivee d'un produit u=2x, v=ln x, hors socle de cette lecon."
          - math: 'f''(x) = (2\ln x + 2) - 2 = 2\ln x'
            note: "On retire la derivee de -2x, qui vaut -2."
      - id: q6
        stem: |-
          **(0,5 pt)** Dresser le tableau de variations de la fonction $f$ sur $[0, +\infty[$.
        reasoning: |-
          Le signe de $f'(x)=2\ln x$ est celui de $\ln x$ (le facteur $2$ est positif) : négatif sur $]0,1[$, nul en $1$, positif sur $]1,+\infty[$ (R1). $f$ décroît donc sur $[0,1]$ puis croît sur $[1,+\infty[$, avec un minimum en $x=1$ valant $f(1)=2\ln1-2=-2$. On complète avec $f(0)=0$ (donné) et $\displaystyle\lim_{x\to+\infty}f(x)=+\infty$ (question 2).
        steps:
          - math: 'f''(x) = 2\ln x, \quad x>0 \implies \text{signe}(f'') = \text{signe}(\ln x)'
            note: "Le facteur 2 est positif : le signe de f' suit celui de ln x."
          - math: 'f''(x) < 0 \text{ sur } ]0,1[, \quad f''(1)=0, \quad f''(x) > 0 \text{ sur } ]1,+\infty['
            note: "Signe de ln x autour de 1 (R1)."
          - math: 'f(1) = 2\ln 1 - 2 = -2'
            note: "Minimum de f, atteint en x=1. f decroit de f(0)=0 a -2, puis croit de -2 a plus l'infini."
      - id: q7
        part: "Equations f(x)=0, f(x)=x et construction de (C)"
        stem: |-
          **(0,5 pt)** Résoudre dans l'intervalle $]0, +\infty[$ les équations $f(x) = 0$ et $f(x) = x$.
        reasoning: |-
          Pour $f(x)=0$, on repart de la forme factorisée $f(x)=2x(\ln x-1)$ (question 2) : sur $]0,+\infty[$, $x\ne0$, donc l'équation équivaut à $\ln x=1$, soit $x=e$ (définition de $e$, R5). Pour $f(x)=x$, on regroupe : $2x\ln x-3x=0$, soit $x(2\ln x-3)=0$ ; comme $x\ne0$, il reste $\ln x=\dfrac32$, soit $x=e^{3/2}$ — cohérent avec l'indication $e^{3/2}\simeq4.5$ donnée à la question suivante pour la construction.
        steps:
          - math: 'f(x)=0 \iff 2x(\ln x - 1) = 0 \iff \ln x = 1 \iff x = e \quad (x\ne0 \text{ sur } ]0,+\infty[)'
            note: "Produit nul, x non nul sur le domaine considere. x=e par definition de e (R5)."
          - math: 'f(x)=x \iff 2x\ln x - 3x = 0 \iff x(2\ln x - 3) = 0 \iff \ln x = \dfrac32 \iff x = e^{3/2}'
            note: "Meme methode, x non nul."
      - id: q8
        stem: |-
          **(1 pt)** Construire la courbe $(C)$ dans le repère $(O, \vec{i}, \vec{j})$ *(on prend $e^{\frac{3}{2}} \simeq 4.5$)*.
        reasoning: |-
          On reporte tout ce qui a déjà été établi. La demi-tangente verticale à l'origine $(0,0)$ (question 4) ; le minimum au point $(1,-2)$ (question 6) ; le zéro de $f$ au point $(e,0)$ et le point $(e^{3/2},e^{3/2})\approx(4{,}5\,;\,4{,}5)$ où $(C)$ croise la droite $y=x$ (question 7) ; et la branche parabolique de direction $(O,\vec{j})$ au voisinage de $+\infty$ (question 3), c'est-à-dire une courbe qui remonte de plus en plus abruptement. $(C)$ part du point $(0,0)$ avec une tangente verticale, descend jusqu'au minimum $(1,-2)$, puis remonte en traversant l'axe des abscisses en $e$ et la droite $y=x$ en $e^{3/2}$.
      - id: q9
        part: "Integration par parties et aire"
        stem: |-
          **(0,5 pt)** En utilisant une intégration par parties, montrer que $\displaystyle\int_1^e x\ln x\, dx = \dfrac{1 + e^2}{4}$.
        reasoning: |-
          Technique du chapitre « Calcul intégral », hors socle de cette leçon. On lit $x\ln x$ comme un produit à intégrer par parties : on dérive $u=\ln x$ (plus simple, $u'=\dfrac1x$) et on primitive $v'=x$ en $v=\dfrac{x^2}{2}$. Le crochet se calcule aux bornes $1$ et $e$ avec $\ln e=1$ et $\ln 1=0$, puis il reste une intégrale élémentaire de $x/2$.
        steps:
          - math: 'u=\ln x,\ u''=\dfrac1x\ ; \quad v''=x,\ v=\dfrac{x^2}{2}'
            note: "Choix IPP : deriver ln x, primitiver x."
          - math: '\int_1^e x\ln x\,dx = \left[\dfrac{x^2}{2}\ln x\right]_1^e - \int_1^e \dfrac{x}{2}\,dx'
            note: "Formule de l'IPP."
          - math: '\left[\dfrac{x^2}{2}\ln x\right]_1^e = \dfrac{e^2}{2}\times 1 - \dfrac{1}{2}\times 0 = \dfrac{e^2}{2}'
            note: "Ln e=1, ln 1=0."
          - math: '\int_1^e \dfrac{x}{2}\,dx = \left[\dfrac{x^2}{4}\right]_1^e = \dfrac{e^2}{4} - \dfrac{1}{4}'
            note: "Primitive elementaire de x/2."
          - math: '\int_1^e x\ln x\,dx = \dfrac{e^2}{2} - \left(\dfrac{e^2}{4} - \dfrac14\right) = \dfrac{e^2}{4} + \dfrac14 = \dfrac{1+e^2}{4}'
            note: "Regroupement final."
      - id: q10
        stem: |-
          **(0,5 pt)** En déduire : $\displaystyle\int_1^e f(x)\, dx$.
        reasoning: |-
          $f(x)=2x\ln x-2x$, donc par linéarité $\displaystyle\int_1^e f(x)\,dx = 2\int_1^e x\ln x\,dx - 2\int_1^e x\,dx$. On réutilise le résultat de la question précédente pour le premier terme, et une primitive élémentaire de $x$ pour le second.
        steps:
          - math: '\int_1^e f(x)\,dx = 2\int_1^e x\ln x\,dx - 2\int_1^e x\,dx'
            note: "Linearite de l'integrale."
          - math: '\int_1^e x\,dx = \left[\dfrac{x^2}{2}\right]_1^e = \dfrac{e^2-1}{2}'
            note: "Primitive elementaire."
          - math: '\int_1^e f(x)\,dx = 2\cdot\dfrac{1+e^2}{4} - 2\cdot\dfrac{e^2-1}{2} = \dfrac{1+e^2}{2} - (e^2-1)'
            note: "Reutilisation de la question precedente."
          - math: '\int_1^e f(x)\,dx = \dfrac{3-e^2}{2}'
            note: "Regroupement final."
      - id: q11
        part: "Minimum de f et une inegalite"
        stem: |-
          **(0,25 pt)** Déterminer le minimum de $f$ sur $]0, +\infty[$.
        reasoning: |-
          Lecture directe du tableau de variations (question 6) : $f$ décroît puis croît, avec un minimum en $x=1$.
        steps:
          - math: 'f \text{ decroit sur } ]0,1] \text{ puis croit sur } [1,+\infty[ \implies \min_{]0,+\infty[} f = f(1)'
            note: "Lecture directe du tableau de variations (question 6)."
          - math: 'f(1) = 2\ln 1 - 2 = -2'
            note: "Le minimum de f sur ]0,+infini[ vaut -2, atteint en x=1."
      - id: q12
        stem: |-
          **(0,5 pt)** En déduire que pour tout $x$ de $]0, +\infty[$, $\ln x \ge \dfrac{x - 1}{x}$.
        reasoning: |-
          Comme $f(1)=-2$ est le minimum global de $f$ sur $]0,+\infty[$ (question précédente), $f(x)\ge f(1)$ pour tout $x>0$, soit $2x\ln x-2x\ge-2$. On divise par $2x$, strictement positif, ce qui préserve le sens de l'inégalité : $\ln x - 1 \ge -\dfrac1x$, c'est-à-dire $\ln x \ge 1-\dfrac1x=\dfrac{x-1}{x}$.
        steps:
          - math: '\forall x>0,\ f(x)\ge f(1) \iff 2x\ln x - 2x \ge -2'
            note: "f(1)=-2 est le minimum global de f (question precedente)."
          - math: '2x\ln x - 2x \ge -2 \iff \ln x - 1 \ge -\dfrac1x \quad (\text{division par } 2x>0)'
            note: "On divise par 2x, strictement positif : le sens de l'inegalite est preserve."
          - math: '\ln x \ge 1 - \dfrac1x = \dfrac{x-1}{x}'
            note: "Inegalite demandee."
      - id: q13
        part: "Fonction reciproque de la restriction g de f a [1,+infini["
        stem: |-
          Soit $g$ la restriction de la fonction $f$ à l'intervalle $[1, +\infty[$.

          **(0,5 pt)** Montrer que la fonction $g$ admet une fonction réciproque $g^{-1}$ définie sur un intervalle $J$ qu'on déterminera.
        reasoning: |-
          $g$ hérite de $f$ sa continuité et sa stricte croissance sur $[1,+\infty[$ (question 6). Une fonction continue et strictement monotone sur un intervalle réalise une bijection de cet intervalle vers l'intervalle image (corollaire du théorème des valeurs intermédiaires, chapitre « Dérivabilité et étude de fonctions », hors socle de cette leçon). L'intervalle image se lit aux bornes : $g(1)=f(1)=-2$ (question 11) et $\displaystyle\lim_{x\to+\infty}g(x)=\lim_{x\to+\infty}f(x)=+\infty$ (question 2). Donc $g$ admet une réciproque $g^{-1}$ définie sur $J=[-2,+\infty[$.
        steps:
          - math: 'g \text{ continue et strictement croissante sur } [1,+\infty[ \quad (\text{restriction de } f, \text{ question 6})'
            note: "Continuite et stricte croissance heritees de f."
          - math: 'g(1) = f(1) = -2 \quad (\text{question 11}), \qquad \lim_{x\to+\infty} g(x) = \lim_{x\to+\infty} f(x) = +\infty \quad (\text{question 2})'
            note: "Images des bornes de l'intervalle de depart."
          - math: 'g \text{ realise une bijection de } [1,+\infty[ \text{ sur } J = [-2,+\infty['
            note: "Corollaire du theoreme des valeurs intermediaires (continuite + stricte monotonie), hors socle de cette lecon."
      - id: q14
        stem: |-
          **(0,75 pt)** Construire dans le même repère $(O, \vec{i}, \vec{j})$ la courbe représentative de la fonction $g^{-1}$.
        reasoning: |-
          La courbe d'une fonction réciproque est le symétrique de la courbe de la fonction de départ par rapport à la droite $(\Delta):y=x$. La courbe de $g$ est la portion de $(C)$ pour $x\ge1$ : elle part du minimum $(1,-2)$, passe par $(e,0)$ et par $(e^{3/2},e^{3/2})$, puis remonte indéfiniment (questions 6 et 7). En reflétant cette portion par rapport à $(\Delta)$, on obtient la courbe de $g^{-1}$, définie sur $J=[-2,+\infty[$, qui part du point $(-2,1)$, passe par $(0,e)$ et par $(e^{3/2},e^{3/2})$ (point fixe commun aux deux courbes, sur $(\Delta)$), puis continue de monter.
      - id: q15
        part: "Une fonction definie par morceaux : h"
        stem: |-
          On considère la fonction $h$ définie sur $\mathbb{R}$ par $\begin{cases} h(x) = x^3 + 3x & ;\ x \le 0 \\ h(x) = 2x\ln x - 2x & ;\ x > 0 \end{cases}$

          **(0,5 pt)** Etudier la continuité de $h$ au point $0$.
        reasoning: |-
          On calcule les deux limites latérales et on les compare à $h(0)$. À gauche, $h$ est le polynôme $x^3+3x$, continu partout, donc $\displaystyle\lim_{x\to0^-}h(x)=0=h(0)$. À droite, $h(x)=f(x)$ pour $x>0$ : c'est exactement le calcul de la question 1, qui donnait $\displaystyle\lim_{x\to0^+}f(x)=0$. Les deux limites latérales coïncident avec $h(0)=0$ : $h$ est continue en $0$.
        steps:
          - math: '\lim_{x\to0^-} h(x) = \lim_{x\to0^-} (x^3+3x) = 0 = h(0)'
            note: "Polynome, continu partout."
          - math: '\lim_{x\to0^+} h(x) = \lim_{x\to0^+} (2x\ln x - 2x) = 0 \quad (\text{meme calcul que la question 1})'
            note: "Reutilisation du corollaire x ln x tend vers 0."
          - math: '\lim_{x\to0^-} h(x) = \lim_{x\to0^+} h(x) = h(0) = 0 \implies h \text{ continue en } 0'
            note: "Limites a gauche et a droite egales a h(0)."
      - id: q16
        stem: |-
          **(0,5 pt)** Etudier la dérivabilité de la fonction $h$ à gauche au point $0$ puis interpréter géométriquement le résultat.
        reasoning: |-
          On calcule la limite à gauche du taux d'accroissement de $h$ en $0$ : $\dfrac{h(x)-h(0)}{x-0}=\dfrac{x^3+3x}{x}=x^2+3$ pour $x\ne0$ (on simplifie par $x$). Cette expression a une limite finie, $3$, quand $x\to0^-$ : $h$ est dérivable à gauche en $0$. Géométriquement, $(C_h)$ admet au point $(0,0)$ une demi-tangente à gauche de coefficient directeur $3$.
        steps:
          - math: '\dfrac{h(x)-h(0)}{x-0} = \dfrac{x^3+3x}{x} = x^2+3 \quad (x\ne0)'
            note: "Taux d'accroissement, simplification par x."
          - math: '\lim_{x\to0^-} (x^2+3) = 3'
            note: "Limite finie : h derivable a gauche en 0."
          - math: '\text{Interpretation : } (C_h) \text{ admet en } (0,0) \text{ une demi-tangente a gauche de coefficient directeur } 3'
            note: "Interpretation geometrique de la derivabilite a gauche."
      - id: q17
        stem: |-
          **(0,25 pt)** La fonction $h$ est-elle dérivable au point $0$ ? justifier.
        reasoning: |-
          $h$ est dérivable en $0$ seulement si les deux demi-dérivées existent ET coïncident. À droite, le taux d'accroissement de $h$ en $0$ est exactement celui de $f$ (question 4), qui tend vers $-\infty$ : ce n'est même pas une demi-dérivée finie. À gauche, la demi-dérivée vaut $3$ (question précédente), un nombre fini. Une limite finie ne peut pas égaler une limite infinie : les deux demi-dérivées ne coïncident pas, donc $h$ n'est pas dérivable en $0$.
        steps:
          - math: '\lim_{x\to0^+} \dfrac{h(x)-h(0)}{x-0} = \lim_{x\to0^+} \dfrac{f(x)}{x} = -\infty \quad (\text{question 4})'
            note: "Reutilisation du taux d'accroissement de f en 0, deja calcule."
          - math: '\lim_{x\to0^-} \dfrac{h(x)-h(0)}{x-0} = 3 \ne \lim_{x\to0^+} \dfrac{h(x)-h(0)}{x-0} = -\infty'
            note: "Les deux demi-derivees ne coincident pas : l'une finie, l'autre infinie."
          - math: 'h \text{ n''est pas derivable en } 0'
            note: "Conclusion : pas de derivabilite en 0 (mais deux demi-tangentes distinctes)."

  # ── bk-2024-n-x4 — 2024 N, Problème : comparaison e^x/x, f(x)=x+1-ln(e^x-x), points fixes, reciproque ──
  - id: bk-2024-n-x4
    title: "Comparaison de l'exponentielle et de x, étude d'une fonction avec ln, points fixes et fonction réciproque"
    source:
      year: 2024
      session: normale
      filiere: "SExp"
      exercise_label: "Problème"
    bareme_total: 8
    duration_min: 48
    sourcing:
      status: sourced
      note: >-
        2024 session normale — examen national SExp (mesures Sciences de la
        Vie et de la Terre, et Sciences Physiques, option française),
        Mathématiques (3 h, coef 7), code NS 22F, AlloSchool element/144505
        (Problème, 8 points, page 4 seule du scan — le problème tient sur
        cette unique page). Transcription fidèle de l'entrée « vérifié
        (corrigé sur re-lecture) » de docs/sujets/maths/fonction-logarithme.md,
        section « 2024 — session normale — Problème » (re-fetch AlloSchool
        indépendant element/144505 → course-438/upload-87124 + diff
        caractère-par-caractère conforme au scan, agent-vérificateur-
        adversarial, 2026-08-06). Deux corrections actées lors de cette
        relecture, reprises telles quelles ici : (1) « Etudier » sans accent
        (E capital) à la question 3-b de la Partie II, conforme au scan ;
        (2) la position de l'étiquette « (C_f) » sur la figure, mesurée sur
        le scan à environ (3 ; 1), posée sous la courbe — la courbe
        elle-même passant par (3 ; 1,16). La description de la figure
        (C_f), reprise dans la question 4-a de la Partie II (justification
        graphique des deux solutions α et β de f(x)=x), a été rejugée
        FIDÈLE au scan après vérification au zoom point par point (cadre
        x∈[-4,4] / y∈[-3,4], deux tracés — droite de pente 1 par l'origine
        non étiquetée, courbe (C_f) étiquetée sous elle-même — allure,
        intersections et asymptote confirmées) et corroborée par le calcul
        indépendant (α≈-2,65, β≈1,42, f(1)=2-ln(e-1)≈1,46). Recoupe
        `calcul-integral` (Partie I, question 3 — aire), `derivabilite-
        etude-fonctions` (Partie II, question 5 — fonction réciproque et
        dérivée de la réciproque en un point) et surtout `fonction-
        exponentielle` (Partie I tout entière — comparaison graphique de
        $e^x$ et $x$ ; Partie II, questions 1-b/1-c/2-a/2-c — l'identité
        $\ln(e^x)=x$ et les limites de référence de l'exponentielle, dont
        $xe^{-x}\to0$, sont des outils du chapitre « Fonction
        exponentielle », hors socle R1–R6 de cette leçon). Pas de volet
        suites dans ce problème (l'Exercice 1 autonome du même sujet porte
        les suites, hors périmètre de cette notion). Ne recoupe PAS le
        sommet de leçon r-bac/r-variation (exercises.yaml), qui transcrit
        le sujet 2019, différent de celui-ci.
    intro: |-
      **Problème (examen national, session normale 2024, Sciences Expérimentales).**

      **Partie I.** On considère les deux fonctions $u$ et $v$ définies sur $\mathbb{R}$ par : $u(x) = e^x$ et $v(x) = x$.

      **Partie II.** On considère la fonction numérique $f$ définie par $f(x) = x + 1 - \ln\left(e^x - x\right)$.

      Point de vigilance qui traverse tout le problème : la Partie I n'est pas un hors-d'œuvre isolé — elle sert à garantir que $e^x-x>0$ pour tout $x$ réel, condition sans laquelle $\ln(e^x-x)$ de la Partie II n'aurait aucun sens (rappel R1 : $\ln$ n'existe que sur $]0,+\infty[$).
    questions:
      - id: q1
        part: "Partie I — Comparer u(x)=e^x et v(x)=x, puis une aire"
        stem: |-
          **(0,5 pt)** Tracer dans un même repère orthonormé les courbes $(\mathcal{C}_u)$ et $(\mathcal{C}_v)$ des fonctions $u$ et $v$.
        reasoning: |-
          $(\mathcal{C}_v)$ est la droite $y=x$, la première bissectrice. $(\mathcal{C}_u)$ est la courbe de l'exponentielle (chapitre « Fonction exponentielle », hors socle de cette leçon) : elle passe par $(0,1)$, est strictement croissante, tend vers $0$ en $-\infty$ (asymptote horizontale) et vers $+\infty$ en $+\infty$, en restant strictement au-dessus de $(\mathcal{C}_v)$ — c'est justement ce que la question suivante demande de justifier.
      - id: q2
        stem: |-
          **(0,25 pt)** Justifier graphiquement que $e^x - x > 0$ pour tout $x$ de $\mathbb{R}$.
        reasoning: |-
          Sur le tracé de la question précédente, $(\mathcal{C}_u)$ reste strictement au-dessus de $(\mathcal{C}_v)$ pour tout $x$ réel — les deux courbes ne se croisent jamais. Lire graphiquement $u(x)>v(x)$ pour tout $x$, c'est lire $e^x>x$, c'est-à-dire $e^x-x>0$.
      - id: q3
        stem: |-
          **(0,5 pt)** Calculer l'aire de la partie du plan délimitée par la courbe $(\mathcal{C}_u)$, la courbe $(\mathcal{C}_v)$ et les droites d'équations $x = 0$ et $x = 1$.
        reasoning: |-
          $(\mathcal{C}_u)$ est au-dessus de $(\mathcal{C}_v)$ partout (question précédente), donc en particulier sur $[0,1]$ : l'aire vaut $\displaystyle\int_0^1(e^x-x)\,dx$, une primitive élémentaire (technique du chapitre « Calcul intégral », hors socle de cette leçon).
        steps:
          - math: '\mathcal{A} = \int_0^1 (u(x)-v(x))\,dx = \int_0^1 (e^x-x)\,dx'
            note: "u au-dessus de v sur [0,1] (question precedente)."
          - math: '\int_0^1 (e^x-x)\,dx = \left[e^x - \dfrac{x^2}{2}\right]_0^1 = \left(e-\dfrac12\right) - (1-0)'
            note: "Primitive elementaire de e^x - x."
          - math: '\mathcal{A} = e - \dfrac32 \quad \text{unites d''aire}'
            note: "Aire positive, coherente avec la position relative etablie."
      - id: q4
        part: "Partie II — Domaine, une autre ecriture de f et limite en +infini"
        stem: |-
          **(0,25 pt)** Vérifier que $f$ est définie sur $\mathbb{R}$.
        reasoning: |-
          $f(x)=x+1-\ln(e^x-x)$ n'a de sens que là où $e^x-x>0$ (rappel de domaine du $\ln$, R1). Or c'est précisément ce que la Partie I vient d'établir pour tout $x$ réel : $f$ est donc définie sur $\mathbb{R}$ tout entier.
        steps:
          - math: '\forall x\in\mathbb{R},\ e^x - x > 0 \quad (\text{Partie I, question 2})'
            note: "Reutilisation directe du resultat de la Partie I."
          - math: 'e^x-x>0 \implies \ln(e^x-x) \text{ existe pour tout } x \implies f \text{ est definie sur } \mathbb{R}'
            note: "Le domaine du ln (R1) est verifie partout : f est definie sur R tout entier."
      - id: q5
        stem: |-
          **(0,5 pt)** Montrer que pour tout $x \in \mathbb{R}$, $f(x) = 1 - \ln\left(1 - xe^{-x}\right)$.
        reasoning: |-
          On factorise l'intérieur du $\ln$ par $e^x$ (jamais nul) : $e^x-x=e^x\left(1-xe^{-x}\right)$. La propriété du produit (R2) donne $\ln(e^x-x)=\ln(e^x)+\ln(1-xe^{-x})$, et $\ln(e^x)=x$ pour tout réel $x$ (propriété de la réciprocité entre $\ln$ et l'exponentielle, chapitre « Fonction exponentielle », hors socle de cette leçon). En remplaçant dans $f(x)=x+1-\ln(e^x-x)$, les deux $x$ s'annulent.
        steps:
          - math: 'e^x - x = e^x\left(1-xe^{-x}\right)'
            note: "On factorise par e^x, jamais nul."
          - math: '\ln(e^x-x) = \ln(e^x) + \ln\left(1-xe^{-x}\right) = x + \ln\left(1-xe^{-x}\right)'
            note: "Propriete du produit (R2) ; ln(e^x)=x, propriete de la reciproque de ln, chapitre Fonction exponentielle, hors socle."
          - math: 'f(x) = x+1-\Big(x+\ln(1-xe^{-x})\Big) = 1 - \ln\left(1-xe^{-x}\right)'
            note: "Les x s'annulent."
      - id: q6
        stem: |-
          **(0,5 pt)** En déduire que $\displaystyle\lim_{x \to +\infty} f(x) = 1$, puis interpréter géométriquement ce résultat.
        reasoning: |-
          On part de l'écriture de la question précédente. Quand $x\to+\infty$, $xe^{-x}\to0$ (limite de référence de l'exponentielle, analogue de la limite $x\ln x\to0$ du R4 mais pour $e^{-x}$, chapitre « Fonction exponentielle », hors socle de cette leçon), donc $1-xe^{-x}\to1$, et par continuité de $\ln$ en $1$ (R1/R5), $\ln(1-xe^{-x})\to\ln(1)=0$. Donc $f(x)\to1-0=1$. Géométriquement, la droite $y=1$ est asymptote horizontale à $(\mathcal{C}_f)$ au voisinage de $+\infty$.
        steps:
          - math: '\lim_{x\to+\infty} xe^{-x} = 0 \quad (\text{limite de reference de l''exponentielle, hors socle})'
            note: "Analogue, pour l'exponentielle, du corollaire x ln x tend vers 0 du R4."
          - math: '\lim_{x\to+\infty}\big(1-xe^{-x}\big) = 1 \implies \lim_{x\to+\infty}\ln\big(1-xe^{-x}\big) = \ln(1) = 0'
            note: "Continuite de ln en 1, deja etablie R1/R5."
          - math: '\lim_{x\to+\infty} f(x) = 1-0 = 1'
            note: "Asymptote horizontale y=1 en plus l'infini."
      - id: q7
        part: "Partie II — Limite en -infini et branche parabolique"
        stem: |-
          **(0,25 pt)** Calculer $\displaystyle\lim_{x \to -\infty} f(x)$.
        reasoning: |-
          Quand $x\to-\infty$, $e^x\to0$ (limite de référence de l'exponentielle, hors socle) et $-x\to+\infty$ : leur somme $e^x-x\to+\infty$. Par la limite de référence du $\ln$ en $+\infty$ (R4), $\ln(e^x-x)\to+\infty$. Donc $f(x)=x+1-\ln(e^x-x)$ est somme de deux termes qui tendent tous les deux vers $-\infty$ ($x+1\to-\infty$ et $-\ln(e^x-x)\to-\infty$) : pas d'indétermination, $f(x)\to-\infty$.
        steps:
          - math: '\lim_{x\to-\infty} e^x = 0, \quad \lim_{x\to-\infty} (-x) = +\infty \implies \lim_{x\to-\infty} (e^x-x) = +\infty'
            note: "Limite de reference de l'exponentielle en moins l'infini (hors socle), plus -x qui domine."
          - math: '\lim_{x\to-\infty} \ln(e^x-x) = +\infty \quad (\text{R4, argument qui tend vers } +\infty)'
            note: "Limite de reference du ln en plus l'infini, appliquee a l'argument e^x - x."
          - math: '\lim_{x\to-\infty} (x+1) = -\infty, \quad \lim_{x\to-\infty} \ln(e^x-x) = +\infty \implies \lim_{x\to-\infty} f(x) = -\infty'
            note: "x+1 tend vers moins l'infini, ln(e^x-x) vers plus l'infini : deux termes qui tirent vers moins l'infini, sans indetermination."
      - id: q8
        stem: |-
          **(0,5 pt)** Vérifier que pour tout $x < 0$, $f(x) = x + 1 - \ln(-x) - \ln\left(1 - \dfrac{1}{xe^{-x}}\right)$.
        reasoning: |-
          Pour $x<0$, on factorise cette fois par $-x$ (strictement positif) plutôt que par $e^x$ : $e^x-x=-x\left(1-\dfrac{e^x}{x}\right)$, et $\dfrac{e^x}{x}=\dfrac1{xe^{-x}}$. La propriété du produit (R2) s'applique car les deux facteurs $-x$ et $1-\dfrac1{xe^{-x}}$ sont strictement positifs pour $x<0$. En substituant dans $f(x)=x+1-\ln(e^x-x)$, on obtient l'écriture demandée.
        steps:
          - math: 'x<0 \implies e^x - x = -x\left(1-\dfrac{e^x}{x}\right) = -x\left(1-\dfrac{1}{xe^{-x}}\right)'
            note: "On factorise par -x, strictement positif pour x negatif ; e^x/x = 1/(x e^-x)."
          - math: '\ln(e^x-x) = \ln(-x) + \ln\left(1-\dfrac{1}{xe^{-x}}\right) \quad (\text{propriete du produit, R2})'
            note: "Les deux facteurs -x et 1-1/(x e^-x) sont strictement positifs pour x<0."
          - math: 'f(x) = x+1-\ln(e^x-x) = x+1-\ln(-x)-\ln\left(1-\dfrac{1}{xe^{-x}}\right)'
            note: "Substitution directe dans f(x)."
      - id: q9
        stem: |-
          **(0,75 pt)** Calculer $\displaystyle\lim_{x \to -\infty} \dfrac{f(x)}{x}$ puis déduire que la courbe $(\mathcal{C}_f)$ admet une branche parabolique de direction la droite d'équation $y = x$ au voisinage de $-\infty$.
        reasoning: |-
          On divise la forme de la question précédente par $x$ (négatif, non nul) et on pose $X=-x$ (qui tend vers $+\infty$) pour se ramener aux limites de référence. Le terme $\dfrac{\ln(-x)}{x}$ devient $-\dfrac{\ln X}{X}\to0$ par croissances comparées (R4, même limite de référence que dans la leçon, ici appliquée à $X$). Le dernier terme tend aussi vers $0$ : $xe^{-x}=-Xe^X\to-\infty$ (l'exponentielle écrase $X$, hors socle), donc $\dfrac1{xe^{-x}}\to0$ et $\ln\left(1-\dfrac1{xe^{-x}}\right)\to\ln(1)=0$. Il reste $\dfrac{f(x)}{x}\to1$ : c'est la direction de $(\Delta):y=x$. Mais l'écart $f(x)-x=1-\ln(e^x-x)$ tend vers $-\infty$ (question 7) — pas de limite finie, donc pas d'asymptote : c'est une branche parabolique de direction $(\Delta)$.
        steps:
          - math: '\dfrac{f(x)}{x} = 1+\dfrac1x-\dfrac{\ln(-x)}{x}-\dfrac1x\ln\left(1-\dfrac{1}{xe^{-x}}\right) \quad (x<0)'
            note: "On divise par x la forme de la question precedente."
          - math: 'X=-x\to+\infty,\quad \dfrac{\ln(-x)}{x} = \dfrac{\ln X}{-X} = -\dfrac{\ln X}{X} \to 0 \quad (\text{croissances comparees, R4})'
            note: "Meme limite de reference que R4, avec le changement de variable X=-x."
          - math: 'xe^{-x} = -Xe^{X} \to -\infty \implies \dfrac{1}{xe^{-x}} \to 0 \implies \ln\left(1-\dfrac{1}{xe^{-x}}\right) \to \ln(1) = 0'
            note: "L'exponentielle ecrase X (hors socle) ; continuite de ln en 1 (R1/R5)."
          - math: '\lim_{x\to-\infty} \dfrac{f(x)}{x} = 1+0-0-0 = 1'
            note: "La pente tend vers 1 : direction de (Delta):y=x."
          - math: 'f(x)-x = 1-\ln(e^x-x) \xrightarrow[x\to-\infty]{} -\infty \quad (\text{question 7})'
            note: "L'ecart ne tend pas vers une limite finie : pas d'asymptote, branche parabolique de direction y=x."
      - id: q10
        part: "Partie II — Derivee, tableau de variations et un zero de f"
        stem: |-
          **(0,5 pt)** Montrer que pour tout $x \in \mathbb{R}$ : $f'(x) = \dfrac{1 - x}{e^x - x}$.
        reasoning: |-
          On dérive $f(x)=x+1-\ln(e^x-x)$ : le terme $\ln(e^x-x)$ est une composée, on applique $(\ln u)'=\dfrac{u'}{u}$ (R3) avec $u(x)=e^x-x$, $u'(x)=e^x-1$. Il reste à réduire $1-\dfrac{e^x-1}{e^x-x}$ au même dénominateur.
        steps:
          - math: 'u(x)=e^x-x,\ u''(x)=e^x-1 \implies \big(\ln(e^x-x)\big)'' = \dfrac{e^x-1}{e^x-x} \quad (\text{regle } (\ln u)''=u''/u, \text{ R3})'
            note: "On derive l'interieur du ln."
          - math: 'f''(x) = 1-\dfrac{e^x-1}{e^x-x} = \dfrac{(e^x-x)-(e^x-1)}{e^x-x}'
            note: "Derivee de x+1 moins la derivee du ln, meme denominateur."
          - math: 'f''(x) = \dfrac{1-x}{e^x-x}'
            note: "Simplification du numerateur : les e^x s'annulent."
      - id: q11
        stem: |-
          **(0,5 pt)** Etudier le signe de la fonction dérivée de $f$, puis déduire le tableau de variations de $f$ sur $\mathbb{R}$.
        reasoning: |-
          Le dénominateur $e^x-x$ est strictement positif pour tout $x$ (Partie I), donc ne compte pas dans le signe : le signe de $f'(x)$ est celui de $1-x$, positif avant $1$, nul en $1$, négatif après. $f$ croît donc sur $]-\infty,1]$ puis décroît sur $[1,+\infty[$, avec un maximum en $x=1$ valant $f(1)=1+1-\ln(e-1)=2-\ln(e-1)$. On complète avec les deux limites déjà établies : $-\infty$ en $-\infty$ (question 7) et $1$ en $+\infty$ (question 6, asymptote).
        steps:
          - math: 'e^x-x>0 \text{ pour tout } x \quad (\text{Partie I}) \implies \text{signe}(f'') = \text{signe}(1-x)'
            note: "Le denominateur ne change jamais de signe."
          - math: 'f''(x) > 0 \text{ sur } ]-\infty,1[, \quad f''(1)=0, \quad f''(x) < 0 \text{ sur } ]1,+\infty['
            note: "Signe de 1-x autour de 1."
          - math: 'f(1) = 1+1-\ln(e-1) = 2-\ln(e-1)'
            note: "Maximum de f, atteint en x=1. f croit de moins l'infini a 2-ln(e-1), puis decroit vers 1 (asymptote)."
      - id: q12
        stem: |-
          **(0,75 pt)** Montrer que l'équation $f(x) = 0$ admet une solution unique dans l'intervalle $]-1, 0[$.
        reasoning: |-
          $f$ est continue et strictement croissante sur $]-\infty,1]$ (question précédente), donc en particulier sur $[-1,0]$ : elle y est injective. On calcule $f(-1)$ et $f(0)$ : les deux valeurs sont de signes opposés, ce qui donne l'existence d'un zéro, et le corollaire du théorème des valeurs intermédiaires (continuité + stricte monotonie, hors socle de cette leçon) garantit que ce zéro est unique.
        steps:
          - math: 'f(-1) = -1+1-\ln\left(e^{-1}+1\right) = -\ln\left(\dfrac1e+1\right) < 0 \quad \left(\dfrac1e+1>1\right)'
            note: "Valeur en -1, negative car l'interieur du ln depasse 1."
          - math: 'f(0) = 0+1-\ln(e^0-0) = 1-\ln(1) = 1 > 0'
            note: "Valeur en 0, positive."
          - math: 'f \text{ continue et strictement croissante sur } [-1,0] \ (\subset\, ]-\infty,1], \text{ question 11}), \quad f(-1)<0<f(0)'
            note: "Corollaire du theoreme des valeurs intermediaires, hors socle de cette lecon."
          - math: '\implies \exists!\, x_0 \in\, ]-1,0[,\ f(x_0)=0'
            note: "Unicite garantie par la stricte monotonie (injectivite) de f sur cet intervalle."
      - id: q13
        part: "Partie II — Deux points fixes alpha et beta"
        stem: |-
          La courbe $(\mathcal{C}_f)$ ci-contre est la représentation graphique de $f$ dans un repère orthonormé : cadre gradué de $-4$ à $4$ en abscisse et de $-3$ à $4$ en ordonnée, avec deux tracés — une droite non étiquetée passant par l'origine, de pente $1$ (la droite $(\Delta):y=x$), et la courbe $(\mathcal{C}_f)$ elle-même, étiquetée juste en dessous d'elle. La courbe monte depuis le coin inférieur gauche du cadre (où elle est sous $(\Delta)$ et plus pentue qu'elle), coupe $(\Delta)$ une première fois dans le troisième quadrant, coupe l'axe des abscisses une unique fois entre $-1$ et $0$ (question 12), passe par le point $(0,1)$, atteint un maximum aux alentours de $1{,}5$ en $x=1$, recoupe $(\Delta)$ une seconde fois entre $1$ et $2$, puis redescend lentement vers l'asymptote $y=1$ en restant sous $(\Delta)$.

          **(0,5 pt)** Justifier graphiquement que l'équation $f(x) = x$ admet deux solutions $\alpha$ et $\beta$.
        reasoning: |-
          Sur le graphique, $(\mathcal{C}_f)$ croise la droite $(\Delta):y=x$ en exactement deux points : l'un dans le troisième quadrant, l'autre entre les abscisses $1$ et $2$. Ce n'est pas seulement une lecture graphique isolée : comme $f$ est strictement croissante sur $]-\infty,1]$ puis strictement décroissante sur $[1,+\infty[$ (question 11), l'équation $f(x)=x$ a au plus une solution sur chacune de ces deux branches monotones (stricte monotonie = injectivité). La lecture graphique montrant une intersection sur chaque branche, il y a donc exactement deux solutions, notées $\alpha$ (sur la branche croissante) et $\beta$ (sur la branche décroissante).
        steps:
          - math: '(\mathcal{C}_f) \text{ croise } (\Delta) \text{ en un point du troisieme quadrant, puis en un second point entre } x=1 \text{ et } x=2'
            note: "Lecture directe de la figure."
          - math: 'f \text{ strictement croissante sur } ]-\infty,1] \text{ puis strictement decroissante sur } [1,+\infty[ \implies \text{au plus une solution de } f(x)=x \text{ par branche}'
            note: "Stricte monotonie = injectivite sur chaque branche (question 11)."
          - math: '\implies \text{exactement deux solutions } \alpha \text{ (branche croissante) et } \beta \text{ (branche decroissante)}'
            note: "Une intersection observee sur chaque branche : deux solutions au total."
      - id: q14
        stem: |-
          **(0,5 pt)** Montrer que : $e^{\alpha} - e^{\beta} = \alpha - \beta$.
        reasoning: |-
          On réécrit d'abord l'équation $f(x)=x$ sous une forme plus maniable, avec la méthode du R6 : isoler le $\ln$ puis utiliser l'injectivité de $\ln$. $f(x)=x \iff \ln(e^x-x)=1$. Or $1=\ln(e)$ (définition de $e$, R5), donc par injectivité de $\ln$ (R1, stricte croissance) : $e^x-x=e$. En appliquant cette équivalence aux deux solutions $\alpha$ et $\beta$ de la question précédente, on obtient deux égalités qu'il suffit de soustraire.
        steps:
          - math: 'f(x)=x \iff x+1-\ln(e^x-x)=x \iff \ln(e^x-x)=1'
            note: "On isole le terme en ln."
          - math: '\ln(e^x-x) = 1 = \ln(e) \iff e^x-x=e \quad (\text{injectivite de ln, R1/R6}; \ \ln(e)=1 \text{ par definition, R5})'
            note: "Equation resolue par injectivite, comme au R6."
          - math: 'f(\alpha)=\alpha,\ f(\beta)=\beta \implies e^{\alpha}-\alpha = e \quad \text{et} \quad e^{\beta}-\beta = e'
            note: "Alpha et beta sont les deux solutions donnees par la question precedente."
          - math: '\big(e^{\alpha}-\alpha\big)-\big(e^{\beta}-\beta\big) = 0 \implies e^{\alpha}-e^{\beta} = \alpha-\beta'
            note: "On soustrait les deux egalites et on regroupe."
      - id: q15
        part: "Partie II — Fonction reciproque g et sa derivee en 1"
        stem: |-
          Soit $g$ la restriction de la fonction $f$ sur l'intervalle $I = ]-\infty, 1]$.

          **(0,5 pt)** Montrer que $g$ admet une fonction réciproque $g^{-1}$ définie sur un intervalle $J$ que l'on déterminera. *(Il n'est pas demandé de déterminer $g^{-1}(x)$)*.
        reasoning: |-
          $g$ hérite de $f$ sa continuité et sa stricte croissance sur $I=]-\infty,1]$ (question 11). Une fonction continue et strictement monotone sur un intervalle réalise une bijection vers l'intervalle image (corollaire du théorème des valeurs intermédiaires, hors socle de cette leçon). L'intervalle image se lit aux bornes de $I$ : $\displaystyle\lim_{x\to-\infty}g(x)=-\infty$ (question 7) et $g(1)=f(1)=2-\ln(e-1)$ (question 11). Donc $g$ admet une réciproque $g^{-1}$ définie sur $J=\left]-\infty,\,2-\ln(e-1)\right]$.
        steps:
          - math: 'g \text{ continue et strictement croissante sur } I=\,]-\infty,1] \quad (\text{restriction de } f, \text{ question 11})'
            note: "Continuite et stricte monotonie heritees de f."
          - math: '\lim_{x\to-\infty} g(x) = -\infty \ (\text{question 7}), \qquad g(1) = f(1) = 2-\ln(e-1) \ (\text{question 11})'
            note: "Images des bornes de l'intervalle I."
          - math: 'g \text{ realise une bijection de } I \text{ sur } J = \left]-\infty,\, 2-\ln(e-1)\right]'
            note: "Corollaire du theoreme des valeurs intermediaires (continuite + stricte monotonie), hors socle de cette lecon."
      - id: q16
        stem: |-
          **(0,75 pt)** Vérifier que $g^{-1}$ est dérivable en $1$ et calculer $\left(g^{-1}\right)'(1)$.
        reasoning: |-
          Le théorème de la dérivée d'une fonction réciproque (chapitre « Dérivabilité et étude de fonctions », hors socle de cette leçon) dit : si $g$ est dérivable en $x_0$ avec $g'(x_0)\ne0$, alors $g^{-1}$ est dérivable en $y_0=g(x_0)$, et $(g^{-1})'(y_0)=\dfrac1{g'(x_0)}$. Ici on cherche $x_0\in I$ tel que $g(x_0)=1$ : $x_0=0$ convient, car $f(0)=0+1-\ln(e^0-0)=1-\ln1=1$. On calcule ensuite $g'(0)=f'(0)$ avec la formule de la question 10 : $f'(0)=\dfrac{1-0}{e^0-0}=1$, qui est non nul. Le théorème s'applique donc, et $(g^{-1})'(1)=\dfrac1{g'(0)}=1$.
        steps:
          - math: 'g(0) = f(0) = 0+1-\ln(e^0-0) = 1-\ln(1) = 1'
            note: "On cherche x0 tel que g(x0)=1 : x0=0 convient (0 appartient a I)."
          - math: 'g''(0) = f''(0) = \dfrac{1-0}{e^0-0} = 1 \ne 0'
            note: "Derivee de f en 0 (formule de la question 10), non nulle."
          - math: 'g \text{ derivable en } 0,\ g''(0)\ne0 \implies g^{-1} \text{ derivable en } g(0)=1, \quad \left(g^{-1}\right)''(1) = \dfrac{1}{g''(0)} = 1'
            note: "Theoreme de la derivee d'une reciproque (chapitre Derivabilite et etude de fonctions, hors socle de cette lecon)."
  # ── bk-2023-r-x1 — 2023 R, Exercice 1 : le probleme d'analyse (10 pts),
  #    f_n(x) = sqrt(x).(ln x)^n — trois parties, quatre domaines ──
  #
  # NOTE ÉDITORIALE — CARTE DES DOMAINES, à lire avant de s'y attaquer. C'est un
  # problème de 10 points, la moitié de l'épreuve, et il traverse quatre
  # chapitres. Le classement du vérificateur les recense partie par partie ;
  # cette entrée reste ENTIÈRE sous fonction-logarithme (slug dominant : le
  # ressort de tout l'exercice est la croissance comparée ln u / u -> 0,
  # mobilisée via les réécritures imposées en I-1a et I-1c) et ne crée AUCUNE
  # entrée ailleurs — l'assemblage d'épreuves somme les bareme_total, et
  # découper produirait des moitiés orphelines. Le cross-list est honoré dans
  # le raisonnement, qui nomme le domaine à chaque fois qu'il en change :
  #   — Partie I, question 1 : continuité à droite, limites -> « Limites et
  #     continuité » ;
  #   — Partie I, question 2 : dérivée, tableau de variations, point
  #     d'inflexion -> « Dérivabilité et étude de fonctions » ;
  #   — Partie II : deux suites, (u_n) puis (x_n) -> « Suites numériques »,
  #     avec le TVI et la stricte monotonie de « Dérivabilité » pour II-2a ;
  #   — Partie III : double intégration par parties et volume de révolution
  #     -> « Calcul intégral ».
  # Ce qu'un élève doit réviser avant : ln et croissances comparées d'abord, le
  # reste au fil des parties.
  #
  # NOTE ÉDITORIALE — MOJIBAKE DU SCAN, et pourquoi il ne coûte rien ici. Le
  # scan de ce sujet remplace les lettres ajourées (N, R, C) par un assortiment
  # de glyphes cassés, la même lettre étant rendue par deux glyphes différents à
  # deux lignes d'écart. La passe adversariale a établi deux faits mesurés au
  # pixel : les étoiles en exposant survivent au mojibake (donc leur ABSENCE est
  # une lecture, pas une incertitude), et un tofu ne rend jamais un symbole
  # non-ajouré. Dans cet exercice, les deux occurrences concernées sont les
  # quantificateurs de la Partie II, lus N* par l'indexation de la suite
  # (u_n) n>=1 — lecture sans alternative. Aucune valeur numérique n'est touchée.
  - id: bk-2023-r-x1
    title: "Problème d'analyse : la famille f_n(x) = √x (ln x)ⁿ — continuité prolongée en 0, variations selon la parité de n, deux suites définies implicitement, et un volume de révolution"
    source:
      year: 2023
      session: rattrapage
      filiere: "SM"
      exercise_label: "Exercice 1 — Analyse (10 points)"
    bareme_total: 10
    duration_min: 60
    sourcing:
      status: sourced
      note: >-
        2023 session de rattrapage — examen national Mathématiques, filière
        Sciences Mathématiques (A) et (B), option française, 4 h, coefficient
        9, code sujet RS 24F, AlloSchool element/142494, scan
        course-436/upload-85320, pages 2 et 3 sur 5 (consignes en page 1).
        Barème de l'exercice = 10 points, recompté partie par partie sur le
        scan et revérifié ici question par question : Partie I =
        0,5 + 0,25 + 0,75 + 0,5 + 0,75 + 0,25 + 1 + 0,25 = 4,25 ; Partie II =
        0,25 + 0,25 + 0,25 + 0,5 + 0,75 + 0,5 + 0,25 + 0,25 + 0,25 = 3,25 ;
        Partie III = 0,25 + 1 + 0,5 + 0,25 + 0,5 = 2,5 ; total 4,25 + 3,25 +
        2,5 = 10, conforme à l'annonce de la page 1 (« L'exercice 1 se rapporte
        à l'analyse (10 pts) »). Transcription vérifiée par une passe
        adversariale indépendante (2026-08-23,
        docs/sujets/_incoming/maths-sm-2023-r.md, statut « vérifié ») :
        re-fetch de element/142494, re-dérivation des chemins d'images depuis
        le HTML (5 pages 0001 à 0005-big.jpg, 1240x1754 chacune), et re-lecture
        de chaque page SUR L'IMAGE — chaque nombre, indice, exposant, borne et
        barème relu sur le scan et jamais sur le texte transcrit. L'exposant
        1/(2n) a été relu au pixel à ses quatre occurrences (questions I-1a et
        I-1c). Aucune figure n'est imprimée dans ce sujet : les cinq pages sont
        du texte mathématique seul. NOTE DE LECTURE (mojibake) : le scan
        substitue aux lettres ajourées un assortiment de glyphes cassés ; en
        Partie II le quantificateur s'écrit une fois avec un tofu et une fois
        avec un glyphe d'angle, pour le même ensemble, lu N* par l'indexation
        de la suite (u_n) n>=1. La passe adversariale a établi au pixel que les
        étoiles en exposant survivent au mojibake et qu'un tofu ne rend jamais
        un symbole non-ajouré — la lecture est donc sans alternative, et
        aucune valeur n'en dépend. CONTRE-CALCUL REFAIT ICI, identité par
        identité : (I-1a) (2n)^n (x^(1/2n) ln(x^(1/2n)))^n = (2n)^n . x^(1/2) .
        (ln x)^n / (2n)^n = sqrt(x)(ln x)^n — identité exacte ; (I-2a)
        f_n' = (1/(2 sqrt x))(ln x)^n + (n/sqrt x)(ln x)^(n-1) =
        (1/(2 sqrt x))(ln x)^(n-1)(2n + ln x) — conforme ; (I-2b) la
        restriction n >= 2 est nécessaire ET suffisante : pour n = 1 le facteur
        (ln x)^0 vaut 1 et ne s'annule jamais, seule racine x = e^(-2) — la
        racine est donc bien e^(-2n) et non e^(-2) ; (II-1b) u_(n+1)/u_n =
        ln beta appartient à ]0,1[ donc la suite décroît ; (II-2b)
        f_(n+1)(x_n) = f_n(x_n) . ln x_n = ln x_n < 1 donne la croissance ;
        (II-3) démonstration par l'absurde cohérente — n ln(ln x_n) =
        ln((ln x_n)^n) tend vers ln(1/sqrt(ell)), qui est FINI, tandis que
        ell < e forcerait -infini, d'où ell = e ; (III-1b) avec
        G(t) = (t^2/2) ln^2 t - (t^2/2) ln t + t^2/4, primitive de t(ln t)^2
        obtenue par la double IPP demandée, F(x) = G(1) - G(x) =
        -(x^2/2) ln^2 x + (x^2/2) ln x + (1/4)(1 - x^2) — EXACTEMENT
        l'expression imprimée, signes et coefficients compris ; d'où
        F(0+) = 1/4, F(0) = 1/4 et V = pi F(0) = pi/4 cm3. Le vérificateur
        conclut que l'exercice se résout entièrement avec les données
        transcrites et qu'aucun point ne reste indécidable. AJOUT DE CETTE
        ENTRÉE AU CORPUS : elle complète l'épreuve SM 2023 rattrapage, dont les
        exercices 2, 3 et 4 étaient déjà convertis (nombres-complexes-2,
        structures-algebriques, arithmetique) — l'épreuve passe de 10,00 à
        20,00 points et devient assemblable en entier dans Examens blancs.
    intro: |-
      **Problème d'analyse (examen national, Sciences Mathématiques, session de rattrapage 2023 — Exercice 1, 10 points).**

      *L'épreuve dure 4 heures et comporte quatre exercices indépendants, traitables dans l'ordre choisi par le candidat. Cet exercice à lui seul pèse la moitié du barème. L'usage de la calculatrice n'est pas autorisé.*

      **Partie I.** Pour tout entier naturel **non nul** $n$, on considère la fonction $f_n$ définie sur $I = [0, +\infty[$ par :

      $$f_n(0) = 0 \qquad\text{et}\qquad \left(\forall x \in\, ]0, +\infty[\right)\ ;\quad f_n(x) = \sqrt{x}\,(\ln x)^n$$

      et soit $(C_n)$ sa courbe représentative dans un repère orthonormé $\left(O; \vec{i}, \vec{j}\right)$.

      **Partie II.** On y étudie deux suites construites à partir de cette famille : $u_n = f_n(\beta)$ pour un réel $\beta$ fixé dans $]1, e[$, puis la suite $(x_n)$ définie **implicitement** par $f_n(x_n) = 1$.

      **Partie III.** On y pose $F(x) = \displaystyle\int_x^1 \left(f_1(t)\right)^2 dt$ pour $x \in I$, et l'on termine par un volume de révolution.

      *Aucune figure n'accompagne ce sujet : tout se joue sur le texte. La carte des domaines, pour savoir quoi réviser avant : la Partie I mobilise les limites et la continuité, puis la dérivation et l'étude de fonctions ; la Partie II les suites numériques et le théorème des valeurs intermédiaires ; la Partie III le calcul intégral. Le fil qui traverse tout, et qu'il faut avoir en main dès la première question, c'est la croissance comparée $\dfrac{\ln u}{u} \to 0$ — les deux réécritures imposées par l'énoncé, en I-1a et I-1c, ne servent qu'à la faire apparaître.*
    questions:
      - id: q1a
        part: "Partie I — 1. La fonction au voisinage de 0 et de +∞"
        stem: |-
          **1- a)** (0,5 pt) Vérifier que : $\left(\forall x \in\, ]0, +\infty[\right)$ ; $\sqrt{x}\,(\ln x)^n = (2n)^n \left( x^{\frac{1}{2n}} \ln\!\left( x^{\frac{1}{2n}} \right) \right)^n$, en déduire que $f_n$ est continue à droite en $0$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** La question a deux moitiés, et la première existe uniquement pour rendre la seconde possible. Telle qu'elle est écrite, $f_n(x) = \sqrt{x}\,(\ln x)^n$ est en $0^+$ une forme indéterminée $0 \times (\pm\infty)$ que rien de connu ne tranche. La réécriture imposée par l'énoncé la transforme en une puissance d'une expression du type $t\ln t$, dont la limite en $0^+$ est, elle, une limite de référence. **L'énoncé te donne la clé : ne cherche pas une autre porte.**

          **La vérification de l'identité, de droite à gauche.** On part du membre de droite, parce que c'est le plus compliqué et que le simplifier est plus sûr que de compliquer le membre de gauche. Posons le calcul en deux temps.

          D'abord, la propriété du logarithme d'une puissance (rung 2) :

          $$\ln\!\left(x^{\frac{1}{2n}}\right) = \frac{1}{2n}\,\ln x$$

          Donc le contenu de la parenthèse vaut $x^{\frac{1}{2n}} \cdot \dfrac{1}{2n}\ln x$. On l'élève à la puissance $n$ :

          $$\left( x^{\frac{1}{2n}} \cdot \frac{1}{2n}\ln x \right)^{\!n}
          = \left(x^{\frac{1}{2n}}\right)^{\!n} \cdot \frac{1}{(2n)^n}\,(\ln x)^n
          = x^{\frac{1}{2}} \cdot \frac{(\ln x)^n}{(2n)^n}$$

          car $\left(x^{\frac{1}{2n}}\right)^{n} = x^{\frac{n}{2n}} = x^{\frac{1}{2}} = \sqrt{x}$.

          Il ne reste qu'à multiplier par le facteur $(2n)^n$ placé devant, qui se simplifie exactement avec le dénominateur :

          $$(2n)^n \cdot \sqrt{x}\,\frac{(\ln x)^n}{(2n)^n} = \sqrt{x}\,(\ln x)^n$$

          L'identité est vérifiée pour tout $x > 0$. Remarque le rôle du $(2n)^n$ : il n'est pas décoratif, il est exactement là pour compenser le $\frac{1}{2n}$ qui sort du logarithme quand on l'élève à la puissance $n$.

          **La continuité à droite en $0$, maintenant.** Par définition, $f_n$ est continue à droite en $0$ si $\displaystyle\lim_{x \to 0^+} f_n(x) = f_n(0) = 0$.

          On pose le **changement de variable** que la réécriture appelle : $t = x^{\frac{1}{2n}}$. Quand $x \to 0^+$, on a $t \to 0^+$ également, car $\frac{1}{2n} > 0$. L'expression devient :

          $$f_n(x) = (2n)^n \left(t\,\ln t\right)^n$$

          Or $\displaystyle\lim_{t \to 0^+} t\ln t = 0$ : c'est une **limite de référence** du chapitre — le logarithme diverge vers $-\infty$, mais si lentement que le facteur $t$ l'emporte. La fonction $z \mapsto z^n$ étant continue en $0$, la puissance $n$-ième suit :

          $$\lim_{x \to 0^+} f_n(x) = (2n)^n \times 0^n = 0 = f_n(0)$$

          **Conclusion à écrire :** $f_n$ est continue à droite en $0$, et le prolongement $f_n(0) = 0$ posé par l'énoncé n'est pas arbitraire — c'est le seul qui rende la fonction continue sur $I$ tout entier.

          **Le piège nommé de cette question.** Vouloir conclure directement « $\sqrt{x} \to 0$ et $(\ln x)^n \to \pm\infty$, donc… ». Le produit $0 \times \infty$ ne se tranche pas par inspection : c'est précisément le cas où il faut faire apparaître une croissance comparée. Le second piège, plus discret : oublier que pour $n$ **pair** $(\ln x)^n \to +\infty$ et pour $n$ **impair** $(\ln x)^n \to -\infty$ — l'indétermination est de nature différente selon la parité, alors que la réécriture, elle, les traite d'un seul coup. C'est un troisième avantage de l'indication de l'énoncé.
      - id: q1b
        stem: |-
          **1- b)** (0,25 pt) Calculer $\displaystyle\lim_{x \to +\infty} f_n(x)$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Un quart de point : il n'y a **aucune** indétermination ici, et la question est là pour te faire remarquer le contraste avec la précédente. Il suffit de regarder les deux facteurs séparément.

          Quand $x \to +\infty$ :

          $$\sqrt{x} \to +\infty \qquad\text{et}\qquad \ln x \to +\infty$$

          Comme $\ln x \to +\infty$, en particulier $\ln x > 0$ à partir d'un certain rang (dès $x > 1$), donc $(\ln x)^n \to +\infty$ **quelle que soit la parité de $n$** — élever un nombre qui tend vers $+\infty$ à une puissance entière positive donne $+\infty$.

          Le produit de deux facteurs qui tendent tous deux vers $+\infty$ tend vers $+\infty$ :

          $$\boxed{\lim_{x \to +\infty} f_n(x) = +\infty}$$

          **Ce qu'il faut avoir compris.** En $0^+$ la parité de $n$ comptait, parce que $\ln x \to -\infty$ et qu'une puissance change le signe d'un négatif selon la parité. En $+\infty$ elle ne compte plus, parce que $\ln x$ est positif. Cette dissymétrie va gouverner toute l'étude des variations en question 2-c : garde-la en tête.
      - id: q1c
        stem: |-
          **1- c)** (0,75 pt) Vérifier que : $\left(\forall x \in\, ]0, +\infty[\right)$ ; $\dfrac{f_n(x)}{x} = (2n)^n \left( \dfrac{\ln\!\left( x^{\frac{1}{2n}} \right)}{x^{\frac{1}{2n}}} \right)^{\!n}$, en déduire $\displaystyle\lim_{x \to +\infty} \dfrac{f_n(x)}{x}$, puis interpréter graphiquement le résultat obtenu.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Même mécanique qu'en 1-a, mais retournée : là-bas la réécriture faisait apparaître $t\ln t$ pour le voisinage de $0$ ; ici elle fait apparaître $\dfrac{\ln t}{t}$, la **croissance comparée** du voisinage de $+\infty$. Trois quarts de point, pour trois gestes distincts — vérifier, calculer, interpréter — et le troisième est celui qu'on oublie.

          **La vérification de l'identité.** Le membre de gauche vaut d'abord, en simplifiant :

          $$\frac{f_n(x)}{x} = \frac{\sqrt{x}\,(\ln x)^n}{x} = \frac{(\ln x)^n}{\sqrt{x}}$$

          car $\dfrac{\sqrt x}{x} = \dfrac{1}{\sqrt x}$. Le membre de droite, maintenant, en réutilisant $\ln\!\left(x^{\frac{1}{2n}}\right) = \dfrac{1}{2n}\ln x$ :

          $$(2n)^n \left( \frac{\frac{1}{2n}\ln x}{x^{\frac{1}{2n}}} \right)^{\!n}
          = (2n)^n \cdot \frac{1}{(2n)^n} \cdot \frac{(\ln x)^n}{\left(x^{\frac{1}{2n}}\right)^{n}}
          = \frac{(\ln x)^n}{\sqrt{x}}$$

          Les deux membres coïncident : l'identité est vérifiée sur $]0, +\infty[$.

          **La limite.** On pose de nouveau $t = x^{\frac{1}{2n}}$, qui tend vers $+\infty$ quand $x \to +\infty$. L'expression devient :

          $$\frac{f_n(x)}{x} = (2n)^n \left(\frac{\ln t}{t}\right)^{\!n}$$

          Or $\displaystyle\lim_{t \to +\infty} \frac{\ln t}{t} = 0$ : c'est **la** limite de référence du chapitre, celle qui dit que le logarithme croît plus lentement que toute puissance de $t$. La fonction $z \mapsto z^n$ étant continue en $0$ :

          $$\boxed{\lim_{x \to +\infty} \frac{f_n(x)}{x} = (2n)^n \times 0^n = 0}$$

          **L'interprétation graphique, et c'est le troisième geste.** On sait deux choses maintenant : $f_n(x) \to +\infty$ (question 1-b) et $\dfrac{f_n(x)}{x} \to 0$. La conjonction porte un nom précis dans le cours :

          > $(C_n)$ admet, au voisinage de $+\infty$, une **branche parabolique de direction l'axe des abscisses**.

          **Ce que cela veut dire concrètement.** La courbe monte indéfiniment, mais de moins en moins vite **relativement à $x$** : elle finit par se coucher, se rapprochant de la direction horizontale sans jamais devenir une asymptote. C'est le comportement typique de tout ce qui contient un logarithme — $\sqrt{x}(\ln x)^n$ grandit, mais reste négligeable devant $x$.

          **Le piège nommé de cette question.** Répondre « la courbe admet une asymptote horizontale ». Non : une asymptote horizontale demanderait $f_n(x) \to$ constante finie, alors qu'ici $f_n(x) \to +\infty$. La limite nulle porte sur le **quotient** $f_n(x)/x$, c'est-à-dire sur la pente moyenne, pas sur la fonction. Confondre les deux est l'erreur la plus fréquente sur ce type de question, et elle coûte le tiers du barème.
      - id: q1d
        stem: |-
          **1- d)** (0,5 pt) Calculer, suivant la parité de $n$, $\displaystyle\lim_{x \to 0^+} \dfrac{f_n(x)}{x}$ puis interpréter graphiquement le résultat obtenu.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Le quotient $\dfrac{f_n(x)}{x}$ est ici bien plus qu'un calcul : comme $f_n(0) = 0$, on a

          $$\frac{f_n(x)}{x} = \frac{f_n(x) - f_n(0)}{x - 0}$$

          c'est-à-dire exactement le **taux d'accroissement de $f_n$ entre $0$ et $x$**. Sa limite en $0^+$ décide donc de la dérivabilité à droite en $0$. C'est ce que la question appelle « interpréter graphiquement », et c'est là que va la moitié du barème.

          **Le calcul, sans réécriture cette fois.** On repart de la forme simplifiée obtenue en 1-c :

          $$\frac{f_n(x)}{x} = \frac{(\ln x)^n}{\sqrt{x}}$$

          Quand $x \to 0^+$, il n'y a **aucune indétermination** — les deux facteurs poussent dans le même sens :
          — $\dfrac{1}{\sqrt{x}} \to +\infty$ ;
          — $\ln x \to -\infty$, donc $(\ln x)^n \to +\infty$ si $n$ est **pair**, et $(\ln x)^n \to -\infty$ si $n$ est **impair**.

          D'où, par produit :

          $$\boxed{\ \lim_{x \to 0^+} \frac{f_n(x)}{x} = +\infty \ \text{ si } n \text{ est pair}\ }
          \qquad
          \boxed{\ \lim_{x \to 0^+} \frac{f_n(x)}{x} = -\infty \ \text{ si } n \text{ est impair}\ }$$

          **L'interprétation graphique.** La limite du taux d'accroissement est **infinie**, donc :

          > $f_n$ n'est **pas dérivable à droite en $0$**, et $(C_n)$ admet au point $O$ une **demi-tangente verticale**.

          Le signe précise son orientation : la demi-tangente est dirigée **vers le haut** si $n$ est pair, **vers le bas** si $n$ est impair.

          **Ce qui vient d'être établi et qu'il faut garder.** La courbe part de l'origine en montant à la verticale ($n$ pair) ou en plongeant à la verticale ($n$ impair). Combiné à la question 1-b, on a déjà les deux bouts du portrait : départ vertical en $O$, envol couché en $+\infty$. Il ne manque que ce qui se passe entre les deux — c'est l'objet de la question 2.

          **Le piège nommé de cette question.** Conclure « $f_n$ n'est pas continue en $0$ ». C'est faux, et la question 1-a a démontré le contraire. **Continuité et dérivabilité sont deux choses distinctes** : la fonction arrive bien en $0$ avec la valeur $0$ (elle est continue), mais elle y arrive avec une pente infinie (elle n'est pas dérivable). La tangente verticale est le dessin exact de cette différence.
      - id: q2a
        part: "Partie I — 2. Dérivée, variations et point d'inflexion"
        stem: |-
          **2- a)** (0,75 pt) Montrer que $f_n$ est dérivable sur $]0; +\infty[$ et que : $\left(\forall x \in\, ]0, +\infty[\right)$ ; $f_n'(x) = \dfrac{1}{2\sqrt{x}}\,(\ln x)^{n-1}\,(2n + \ln x)$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Une question « montrer que » : le résultat est donné, donc les points sont dans le chemin — justifier la dérivabilité, dériver, **et factoriser jusqu'à la forme imprimée**. Cette dernière étape n'est pas cosmétique : c'est la forme factorisée qui rendra l'étude du signe possible en 2-c.

          **La dérivabilité, en une phrase.** Sur $]0, +\infty[$, la fonction $x \mapsto \sqrt{x}$ est dérivable, et $x \mapsto (\ln x)^n$ l'est aussi comme composée de $\ln$ (dérivable sur $]0,+\infty[$) et de $z \mapsto z^n$ (dérivable sur $\mathbb{R}$). $f_n$ est le **produit** de deux fonctions dérivables sur cet intervalle : elle y est donc dérivable.

          **La dérivation, terme par terme.** On applique la formule du produit $(uv)' = u'v + uv'$ avec $u(x) = \sqrt{x}$ et $v(x) = (\ln x)^n$ :

          $$u'(x) = \frac{1}{2\sqrt{x}}
          \qquad\text{et}\qquad
          v'(x) = n\,(\ln x)^{n-1} \cdot \frac{1}{x}$$

          la seconde étant la dérivée d'une puissance composée, $\left(w^n\right)' = n\,w^{n-1}w'$ avec $w = \ln x$.

          $$f_n'(x) = \frac{1}{2\sqrt{x}}\,(\ln x)^n + \sqrt{x} \cdot \frac{n\,(\ln x)^{n-1}}{x}$$

          **La simplification du second terme.** C'est le point technique : $\dfrac{\sqrt{x}}{x} = \dfrac{1}{\sqrt{x}}$, donc

          $$f_n'(x) = \frac{1}{2\sqrt{x}}\,(\ln x)^n + \frac{n\,(\ln x)^{n-1}}{\sqrt{x}}$$

          **La factorisation, qui est le vrai travail.** On met en facteur ce que les deux termes ont en commun : $\dfrac{1}{2\sqrt{x}}$ et $(\ln x)^{n-1}$. Le premier terme donne alors $(\ln x)^{n} = (\ln x)^{n-1} \cdot \ln x$, donc il laisse $\ln x$ ; le second terme, qui vaut $\dfrac{2n\,(\ln x)^{n-1}}{2\sqrt{x}}$, laisse $2n$ :

          $$\boxed{\ f_n'(x) = \frac{1}{2\sqrt{x}}\,(\ln x)^{n-1}\,\left(2n + \ln x\right)\ }$$

          C'est exactement l'expression imprimée par l'énoncé.

          **Pourquoi cette forme et pas une autre.** Trois facteurs, dont on sait lire le signe séparément : $\dfrac{1}{2\sqrt{x}}$ toujours strictement positif, $(\ln x)^{n-1}$ dont le signe dépend de la parité de $n-1$, et $(2n + \ln x)$ affine en $\ln x$ qui s'annule en $x = e^{-2n}$. Toute la question 2-c tient dans cette lecture — c'est pourquoi l'énoncé impose cette factorisation-là.

          **Le piège nommé de cette question.** S'arrêter à la somme non factorisée. Elle est juste, mais elle ne permet pas d'étudier le signe, et une question « montrer que » exige la forme demandée : sans elle, les points ne sont pas acquis, et la suite du problème devient impraticable.
      - id: q2b
        stem: |-
          **2- b)** (0,25 pt) Vérifier que : $\forall n \geq 2$, $f_n'(x) = 0$ si et seulement si $(x = 1$ ou $x = e^{-2n})$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Un produit de trois facteurs est nul si et seulement si l'un d'eux l'est. Le premier, $\dfrac{1}{2\sqrt{x}}$, ne s'annule jamais sur $]0,+\infty[$. Il reste deux équations.

          **Premier facteur annulable.**

          $$(\ln x)^{n-1} = 0 \iff \ln x = 0 \iff x = 1$$

          **Attention à la condition $n \geq 2$, qui n'est pas décorative.** Cette équivalence n'est valable que si l'exposant $n-1$ est **strictement positif**, c'est-à-dire si $n \geq 2$. Pour $n = 1$, le facteur vaut $(\ln x)^0 = 1$, qui ne s'annule **jamais** — et $x = 1$ n'est alors pas racine de $f_1'$. C'est précisément pourquoi l'énoncé écrit « $\forall n \geq 2$ » : le cas $n = 1$ est une exception réelle, pas une précaution de rédaction.

          **Second facteur annulable.**

          $$2n + \ln x = 0 \iff \ln x = -2n \iff x = e^{-2n}$$

          en appliquant l'exponentielle, qui est une bijection strictement croissante de $\mathbb{R}$ sur $]0,+\infty[$ — la valeur trouvée est donc bien dans le domaine.

          **Conclusion.** Pour $n \geq 2$ :

          $$f_n'(x) = 0 \iff x = 1 \ \text{ ou } \ x = e^{-2n}$$

          **Le repère à garder pour la suite.** $e^{-2n} < 1$ pour tout $n \geq 1$, puisque $-2n < 0$. Les deux racines sont donc rangées dans cet ordre : $0 < e^{-2n} < 1$, et elles découpent $]0,+\infty[$ en trois intervalles. C'est la trame du tableau de variations de la question suivante.

          **Le piège nommé de cette question.** Écrire $x = e^{-2}$ au lieu de $e^{-2n}$, en oubliant que l'exposant du logarithme est $n$ et que le terme constant du troisième facteur est $2n$. Le contrôle : plus $n$ grandit, plus $e^{-2n}$ se rapproche de $0$ — la racine « fuit » vers l'origine, ce qui est cohérent avec une fonction de plus en plus écrasée près de $0$.
      - id: q2c
        stem: |-
          **2- c)** (1 pt) Étudier, suivant la parité de $n$, le sens de variation de $f_n$ et donner son tableau de variations.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Un point entier, le plus gros de la Partie I : c'est une étude de signe complète, à mener **deux fois** — une par parité. Toute l'affaire tient dans le signe du facteur $(\ln x)^{n-1}$, qui dépend de la parité de l'exposant $n-1$, donc de celle de $n$.

          **Le point de départ.** De la question 2-a :

          $$f_n'(x) = \underbrace{\frac{1}{2\sqrt{x}}}_{>\,0} \cdot (\ln x)^{n-1} \cdot (2n + \ln x)$$

          Le premier facteur est strictement positif partout : **le signe de $f_n'$ est celui du produit des deux autres**. Et les racines de la question 2-b découpent le domaine en trois : $]0, e^{-2n}[$, $]e^{-2n}, 1[$, $]1, +\infty[$.

          **Le signe des deux facteurs, intervalle par intervalle.**
          — Sur $]0, e^{-2n}[$ : $\ln x < -2n < 0$, donc $(2n + \ln x) < 0$, et $\ln x < 0$.
          — Sur $]e^{-2n}, 1[$ : $-2n < \ln x < 0$, donc $(2n + \ln x) > 0$, et $\ln x < 0$.
          — Sur $]1, +\infty[$ : $\ln x > 0$, donc les deux facteurs sont positifs.

          Reste à élever $\ln x$ à la puissance $n-1$, et c'est là que la parité entre en scène.

          **Cas $n$ impair** (donc $n - 1$ **pair**) **:** $(\ln x)^{n-1} \geq 0$ partout, nul seulement en $x = 1$. Le signe de $f_n'$ est donc celui de $(2n + \ln x)$ seul :

          | $x$ | $0$ | | $e^{-2n}$ | | $1$ | | $+\infty$ |
          |---|---|---|---|---|---|---|---|
          | $f_n'(x)$ | | $-$ | $0$ | $+$ | $0$ | $+$ | |
          | $f_n$ | $0$ | $\searrow$ | $-\left(\frac{2n}{e}\right)^{n}$ | $\nearrow$ | $0$ | $\nearrow$ | $+\infty$ |

          $f_n$ décroît sur $]0, e^{-2n}]$ puis croît sur $[e^{-2n}, +\infty[$. Elle admet un **minimum global** en $e^{-2n}$, et $x = 1$ est un point où la dérivée s'annule **sans changer de signe** : ce n'est pas un extremum — c'est ce que la question 2-d va exploiter.

          **Cas $n$ pair** (donc $n - 1$ **impair**) **:** $(\ln x)^{n-1}$ a le signe de $\ln x$ : négatif sur $]0,1[$, positif sur $]1,+\infty[$. En multipliant par le signe de $(2n+\ln x)$ relevé ci-dessus :

          | $x$ | $0$ | | $e^{-2n}$ | | $1$ | | $+\infty$ |
          |---|---|---|---|---|---|---|---|
          | $f_n'(x)$ | | $+$ | $0$ | $-$ | $0$ | $+$ | |
          | $f_n$ | $0$ | $\nearrow$ | $\left(\frac{2n}{e}\right)^{n}$ | $\searrow$ | $0$ | $\nearrow$ | $+\infty$ |

          $f_n$ croît, décroît, puis croît : **maximum local** en $e^{-2n}$ et **minimum local** en $x = 1$.

          **Les valeurs remarquables, calculées et non laissées en suspens.** En $x = 1$ : $f_n(1) = \sqrt{1}\,(\ln 1)^n = 0$. En $x = e^{-2n}$ :

          $$f_n\!\left(e^{-2n}\right) = \sqrt{e^{-2n}} \cdot (-2n)^n = e^{-n}\,(-1)^n (2n)^n = (-1)^n \left(\frac{2n}{e}\right)^{\!n}$$

          soit $\left(\frac{2n}{e}\right)^{n} > 0$ si $n$ est pair, et $-\left(\frac{2n}{e}\right)^{n} < 0$ si $n$ est impair — ce qui confirme, sans calcul supplémentaire, qu'il s'agit bien d'un maximum dans un cas et d'un minimum dans l'autre.

          **Le cas $n = 1$, à traiter à part.** La question 2-b l'a écarté : pour $n = 1$, le facteur $(\ln x)^0$ vaut $1$ et ne s'annule jamais. Le signe de $f_1'$ est celui de $(2 + \ln x)$ seul : $f_1$ décroît sur $]0, e^{-2}]$ puis croît sur $[e^{-2}, +\infty[$, avec un minimum $f_1(e^{-2}) = -\dfrac{2}{e}$. Le tableau a la même allure que le cas impair, mais **sans le point stationnaire en $x = 1$**.

          **Le piège nommé de cette question.** Traiter le signe de $(\ln x)^{n-1}$ comme celui de $\ln x$ sans regarder la parité de l'exposant. C'est l'erreur qui échange les deux tableaux et retourne toute la description de la courbe. Le contrôle qui la débusque : les deux cas doivent donner un extremum de signe **opposé** en $e^{-2n}$, puisque $f_n(e^{-2n})$ porte le facteur $(-1)^n$.
      - id: q2d
        stem: |-
          **2- d)** (0,25 pt) Montrer que si $n$ est impair et $n \geq 3$ alors le point d'abscisse $1$ est un point d'inflexion de $(C_n)$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Un point d'inflexion est un point où la courbe **change de concavité**, c'est-à-dire où $f_n''$ s'annule **en changeant de signe**. La tentation est de calculer $f_n''$ : c'est faisable mais long. Il existe un argument beaucoup plus court, et c'est celui qu'un quart de point récompense.

          **L'argument, en une observation.** Reprenons la forme factorisée :

          $$f_n'(x) = \frac{1}{2\sqrt{x}}\,(\ln x)^{n-1}\,(2n + \ln x)$$

          Plaçons-nous **au voisinage de $x = 1$**. Là, $\ln x$ est proche de $0$, donc $(2n + \ln x)$ est proche de $2n > 0$ : ce facteur est **strictement positif** sur tout un voisinage de $1$. Le facteur $\dfrac{1}{2\sqrt{x}}$ l'est aussi. Enfin, $n$ étant impair avec $n \geq 3$, l'exposant $n-1$ est **pair et au moins égal à $2$**, donc $(\ln x)^{n-1} \geq 0$, avec égalité **uniquement** en $x = 1$.

          Conclusion : sur un voisinage de $1$, $f_n'(x) > 0$ pour $x \neq 1$, et $f_n'(1) = 0$.

          **Ce que cela dit de $f_n'$, et donc de $f_n''$.** La fonction $f_n'$ est positive autour de $1$ et s'annule exactement en $1$ : elle atteint donc en $x = 1$ un **minimum local**, de valeur $0$. Or une fonction dérivable qui admet un minimum local en un point y a une dérivée qui **change de signe, du négatif au positif**. Autrement dit :

          $$f_n''(x) < 0 \text{ juste avant } 1 \qquad\text{et}\qquad f_n''(x) > 0 \text{ juste après } 1$$

          $f_n''$ s'annule en $1$ **en changeant de signe** : le point d'abscisse $1$ est bien un **point d'inflexion** de $(C_n)$.

          **La lecture géométrique, qui rend le résultat visible.** La courbe est concave avant $1$, convexe après, et la tangente en ce point est **horizontale** puisque $f_n'(1) = 0$. C'est donc un point d'inflexion à tangente horizontale : la courbe s'aplatit complètement, marque un palier, puis repart en montant — sans jamais redescendre. C'est exactement ce que le tableau du cas impair montrait avec un « $0$ » entre deux flèches montantes.

          **Pourquoi les deux hypothèses sont nécessaires, et il faut le dire.** Si $n$ était **pair**, $n-1$ serait impair, $(\ln x)^{n-1}$ changerait de signe en $1$, et $x = 1$ serait un **minimum** de $f_n$ — pas un point d'inflexion. Si $n = 1$, le facteur $(\ln x)^{0} = 1$ ne s'annule pas et $x = 1$ n'est même pas un point critique. Les deux conditions « $n$ impair » et « $n \geq 3$ » sont donc toutes deux indispensables : une démonstration qui n'utilise ni l'une ni l'autre est nécessairement fausse quelque part.
      - id: q3a
        part: "Partie II — 1. La suite u_n = f_n(β), à β fixé"
        stem: |-
          **1- a)** (0,25 pt) Soit $\beta \in\, ]1, e[$ un réel fixé. On considère la suite numérique $(u_n)_{n\geq 1}$ définie par : $\left(\forall n \in \mathbb{N}^*\right)$ ; $u_n = f_n(\beta)$. Montrer que : $\left(\forall n \in \mathbb{N}^*\right)$ ; $0 < u_n < \sqrt{e}$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Changement complet de point de vue, et il faut le prendre consciemment : jusqu'ici $n$ était fixé et $x$ variait ; maintenant c'est $\beta$ qui est **fixé** et $n$ qui varie. La variable de la suite est l'**exposant**, pas le point.

          **La traduction de l'hypothèse, qui est toute la question.** $\beta \in\, ]1, e[$, et $\ln$ est strictement croissante, donc :

          $$\ln 1 < \ln \beta < \ln e \qquad\Longrightarrow\qquad 0 < \ln \beta < 1$$

          **Retiens ce fait : $\ln\beta$ est strictement compris entre $0$ et $1$.** C'est lui qui porte les trois questions de cette section — la positivité, la décroissance et la limite en découlent toutes.

          **La minoration.** $u_n = \sqrt{\beta}\,(\ln \beta)^n$ est un produit de deux facteurs strictement positifs : $\sqrt{\beta} > 0$ car $\beta > 1 > 0$, et $(\ln \beta)^n > 0$ car $\ln\beta > 0$. Donc $u_n > 0$.

          **La majoration.** Comme $0 < \ln \beta < 1$, toute puissance entière $n \geq 1$ reste inférieure à $1$ :

          $$(\ln \beta)^n < 1$$

          Donc $u_n = \sqrt{\beta}\,(\ln\beta)^n < \sqrt{\beta}$. Et comme $\beta < e$ et que la racine carrée est croissante, $\sqrt{\beta} < \sqrt{e}$. En enchaînant :

          $$0 < u_n < \sqrt{\beta} < \sqrt{e}$$

          **Ce qui est acquis pour la suite.** La suite est bornée. Combinée à la décroissance de la question suivante, cette borne garantira la convergence sans avoir à calculer quoi que ce soit — même si, ici, la limite se calculera directement.
      - id: q3b
        stem: |-
          **1- b)** (0,25 pt) Montrer que la suite $(u_n)_{n\geq 1}$ est décroissante.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Deux méthodes existent — comparer $u_{n+1} - u_n$ à $0$, ou comparer $\dfrac{u_{n+1}}{u_n}$ à $1$. Ici la seconde est bien plus rapide, parce que la suite est **géométrique** : c'est le réflexe à avoir dès qu'on voit un terme général de la forme $a \cdot q^{\,n}$.

          **Le quotient, et il se simplifie entièrement.** Les termes sont strictement positifs (question précédente), donc le quotient est licite :

          $$\frac{u_{n+1}}{u_n}
          = \frac{\sqrt{\beta}\,(\ln \beta)^{n+1}}{\sqrt{\beta}\,(\ln \beta)^{n}}
          = \ln \beta$$

          Le facteur $\sqrt\beta$ disparaît, et il ne reste que la raison. La suite $(u_n)$ est donc **géométrique de raison $q = \ln\beta$**, de premier terme $u_1 = \sqrt\beta\,\ln\beta$.

          **La conclusion.** On sait de la question précédente que $0 < \ln\beta < 1$, donc :

          $$\frac{u_{n+1}}{u_n} = \ln\beta < 1$$

          Les termes étant strictement positifs, cela équivaut à $u_{n+1} < u_n$ pour tout $n \geq 1$ : la suite $(u_n)_{n\geq 1}$ est **strictement décroissante**.

          **La variante par la différence, si tu préfères.** $u_{n+1} - u_n = \sqrt{\beta}\,(\ln\beta)^n\,(\ln\beta - 1)$, produit d'un facteur strictement positif par $(\ln\beta - 1) < 0$ : négatif. Même conclusion, un peu plus long.

          **Le piège nommé de cette question.** Diviser par $u_n$ sans avoir dit qu'il est non nul, ou conclure de $\frac{u_{n+1}}{u_n} < 1$ à $u_{n+1} < u_n$ sans dire que les termes sont positifs. Si $u_n$ était négatif, l'inégalité se retournerait. La question 1-a n'est pas un préliminaire décoratif : elle est l'hypothèse dont celle-ci a besoin.
      - id: q3c
        stem: |-
          **1- c)** (0,25 pt) Déterminer $\displaystyle\lim_{n \to +\infty} u_n$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** La suite est géométrique de raison $q = \ln\beta$ avec $0 < q < 1$ : le résultat du cours s'applique directement, sans détour.

          $$\lim_{n \to +\infty} q^{\,n} = 0 \qquad \text{lorsque } |q| < 1$$

          Le facteur $\sqrt{\beta}$ est une constante, indépendante de $n$ ; il ne change rien à la limite :

          $$\boxed{\lim_{n \to +\infty} u_n = \sqrt{\beta} \times 0 = 0}$$

          **La lecture, qui vaut la peine d'être faite.** $u_n = f_n(\beta)$ : ce résultat dit que **en un point fixé de $]1, e[$, la famille de courbes $(C_n)$ s'écrase vers $0$ quand $n$ grandit**. Ce n'est pas anodin — la question 2 va justement chercher, pour chaque $n$, le point $x_n$ où $f_n$ vaut exactement $1$. Comme la fonction s'aplatit sur $]1,e[$, ce point doit être poussé de plus en plus à droite pour maintenir la valeur $1$. C'est exactement la croissance de $(x_n)$ que la question 2-b va démontrer, et sa convergence vers $e$ qu'on établira à la fin. Les deux sections de cette partie racontent la même chose, vue de deux côtés.
      - id: q4a
        part: "Partie II — 2. La suite (xₙ) définie implicitement par fₙ(xₙ) = 1"
        stem: |-
          **2- a)** (0,5 pt) Montrer que pour tout entier $n$ non nul, il existe un unique réel $x_n \in\, ]1; e[$ tel que : $f_n(x_n) = 1$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** « Il existe un unique » : deux affirmations, deux outils, et il faut fournir les deux. L'existence vient du **théorème des valeurs intermédiaires**, l'unicité de la **stricte monotonie**. Un candidat qui n'en invoque qu'un perd la moitié du barème — c'est le schéma de démonstration le plus rentable de tout le programme d'analyse, et il se rédige toujours pareil.

          **Les trois hypothèses à vérifier, une par une.**

          *Continuité.* $f_n$ est dérivable sur $]0,+\infty[$ (question 2-a de la Partie I), donc continue sur $]1, e[$, qui est inclus dedans.

          *Stricte monotonie.* De l'étude de signe de la question 2-c : sur $]1, +\infty[$, les trois facteurs de $f_n'$ sont strictement positifs — $\dfrac{1}{2\sqrt x} > 0$, $(\ln x)^{n-1} > 0$ car $\ln x > 0$, et $(2n + \ln x) > 0$. Donc $f_n' > 0$ et $f_n$ est **strictement croissante** sur $]1, e[$, **et ce quelle que soit la parité de $n$** — c'est le seul intervalle du domaine où les deux cas coïncident, et c'est pourquoi l'énoncé y place la recherche.

          *Les valeurs aux bornes.* $f_n(1) = \sqrt{1}\,(\ln 1)^n = 0$, et $f_n(e) = \sqrt{e}\,(\ln e)^n = \sqrt{e} \times 1^n = \sqrt{e}$.

          **La conclusion, en une phrase.** $f_n$ est continue et strictement croissante sur $]1, e[$ : elle y réalise donc une **bijection** de $]1, e[$ sur son image $\left]f_n(1),\, f_n(e)\right[ \;=\; \left]0,\, \sqrt{e}\right[$.

          Or $\sqrt{e} \approx 1{,}65 > 1$, donc $1 \in\, ]0, \sqrt{e}[$. **L'équation $f_n(x) = 1$ admet donc une solution et une seule dans $]1, e[$** — c'est $x_n$.

          **Le détail qui décide, et qu'il faut écrire.** Toute la démonstration repose sur $\sqrt{e} > 1$. C'est vrai parce que $e > 1$ et que la racine carrée conserve cette inégalité. Sans cette remarque, rien ne garantit que la valeur $1$ soit atteinte : si l'on avait $f_n(e) < 1$, la suite $(x_n)$ n'existerait tout simplement pas. Un correcteur cherche cette ligne.

          **Le piège nommé de cette question.** Invoquer le TVI et s'arrêter là. Le TVI donne l'**existence**, jamais l'unicité — une fonction continue peut valoir $1$ en dix points. C'est la stricte croissance qui interdit un second passage par la valeur $1$. Les deux arguments sont complémentaires, et aucun ne remplace l'autre.
      - id: q4b
        stem: |-
          **2- b)** (0,75 pt) Montrer que la suite $(x_n)_{n \in \mathbb{N}^*}$ ainsi définie est croissante, en déduire qu'elle est convergente.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** $x_n$ n'est donné par aucune formule : il est défini **implicitement**, comme la solution d'une équation. On ne peut donc pas comparer $x_{n+1}$ et $x_n$ par le calcul. La technique — et c'est celle qu'il faut retenir pour toutes les suites définies implicitement — consiste à **évaluer la fonction du rang suivant au point du rang courant**, puis à utiliser la monotonie pour remonter aux abscisses.

          **Le calcul central, et il est court.** Évaluons $f_{n+1}$ en $x_n$ :

          $$f_{n+1}(x_n) = \sqrt{x_n}\,(\ln x_n)^{n+1}
          = \underbrace{\sqrt{x_n}\,(\ln x_n)^{n}}_{=\,f_n(x_n)\,=\,1} \times \ln x_n
          = \ln x_n$$

          On a simplement séparé un facteur $\ln x_n$ de la puissance $n+1$, et reconnu $f_n(x_n)$, qui vaut $1$ par définition de $x_n$.

          **L'encadrement qui conclut.** $x_n \in\, ]1, e[$, donc $0 < \ln x_n < 1$. Par conséquent :

          $$f_{n+1}(x_n) = \ln x_n < 1 = f_{n+1}(x_{n+1})$$

          **Le passage aux abscisses.** $f_{n+1}$ est strictement croissante sur $]1, e[$ (même argument qu'en 2-a, valable pour tout rang). Une fonction strictement croissante conserve l'ordre **et le renverse quand on la lit à l'envers** : de $f_{n+1}(x_n) < f_{n+1}(x_{n+1})$ on déduit

          $$x_n < x_{n+1}$$

          La suite $(x_n)_{n \geq 1}$ est donc **strictement croissante**.

          **La convergence.** Par construction, $x_n \in\, ]1, e[$ pour tout $n$, donc la suite est **majorée par $e$**. Une suite croissante et majorée converge (théorème de la limite monotone) : $(x_n)$ est convergente, et sa limite $\ell$ vérifie $\ell \leq e$.

          **Ce qu'on n'a PAS démontré, et qu'il ne faut pas croire acquis.** Le théorème donne la convergence, pas la valeur de la limite. Rien ici ne dit que $\ell = e$ — la suite pourrait très bien converger vers $2$. C'est tout l'objet de la question 3, qui procédera par l'absurde.

          **Le piège nommé de cette question.** Écrire $f_{n+1}(x_{n+1}) = 1 = f_n(x_n)$ et en conclure directement quelque chose sur $x_{n+1}$ et $x_n$. C'est une comparaison entre **deux fonctions différentes** en deux points différents : elle ne dit rien. Il faut impérativement évaluer **une seule et même fonction** — ici $f_{n+1}$ — en deux points, pour que la monotonie ait prise.
      - id: q5a
        part: "Partie II — 3. La limite ℓ, obtenue par l'absurde"
        stem: |-
          **3- a)** (0,5 pt) On pose $\ell = \displaystyle\lim_{n \to +\infty} x_n$. Montrer que : $1 < \ell \leq e$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Deux inégalités, et elles ne se démontrent pas de la même façon — c'est la subtilité de la question, et elle vaut la moitié de son barème.

          **La majoration, par passage à la limite.** Pour tout $n$, $x_n < e$. Le passage à la limite dans une inégalité **large** donne $\ell \leq e$. Attention : le passage à la limite **affaiblit** toujours une inégalité stricte — de $x_n < e$ on ne peut conclure que $\ell \leq e$, jamais $\ell < e$. C'est précisément pour cela que l'énoncé écrit $\ell \leq e$ avec un signe large, et c'est ce qui laisse la porte ouverte à la valeur $e$ qu'on trouvera en 3-d.

          **La minoration, et il faut résister au même réflexe.** De $x_n > 1$, le passage à la limite ne donnerait que $\ell \geq 1$ — trop faible, l'énoncé demande une inégalité **stricte**. L'argument qui la fournit est ailleurs : la suite est **croissante** (question 2-b), donc **minorée par son premier terme** :

          $$\ell = \lim_{n\to+\infty} x_n \geq x_1$$

          Or $x_1 \in\, ]1, e[$, donc $x_1 > 1$ **strictement**. En enchaînant :

          $$\ell \geq x_1 > 1$$

          d'où $\ell > 1$. On obtient bien :

          $$\boxed{\,1 < \ell \leq e\,}$$

          **Ce qui est réellement acquis, et pourquoi cela compte.** $\ell > 1$ **strictement** garantit que $\ln \ell > 0$, donc que $\ln\ell$ n'est pas nul — condition sans laquelle $\ln(\ln x_n)$ n'aurait aucun sens à la limite, et sans laquelle la question 3-c s'effondrerait. Cette question n'est pas un préliminaire de confort : elle installe la condition de validité de toute la fin du problème.

          **Le piège nommé de cette question.** Passer à la limite dans $x_n > 1$ et écrire $\ell > 1$. C'est une faute classique et le correcteur la cherche : le passage à la limite ne conserve **jamais** la stricte inégalité. Le contre-exemple qui la tue : $\frac{1}{n} > 0$ pour tout $n$, et pourtant la limite vaut $0$.
      - id: q5b
        stem: |-
          **3- b)** (0,25 pt) Montrer que : $\displaystyle\lim_{n \to +\infty} (\ln x_n)^n = \dfrac{1}{\sqrt{\ell}}$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** L'expression $(\ln x_n)^n$ a un exposant qui varie : on ne sait rien en dire directement. Mais la définition de $x_n$ la relie à quelque chose de simple — il suffit de retourner l'équation qui définit la suite.

          **Le retournement.** Par définition, $f_n(x_n) = 1$, c'est-à-dire :

          $$\sqrt{x_n}\,(\ln x_n)^n = 1
          \qquad\Longrightarrow\qquad
          (\ln x_n)^n = \frac{1}{\sqrt{x_n}}$$

          la division étant licite car $x_n > 1 > 0$, donc $\sqrt{x_n} \neq 0$.

          **Le passage à la limite.** Le membre de droite ne contient plus d'exposant variable : c'est une simple composée. On sait que $x_n \to \ell$ avec $\ell > 1 > 0$ (question 3-a), et la fonction $t \mapsto \dfrac{1}{\sqrt{t}}$ est **continue sur $]0, +\infty[$**, donc en $\ell$. Par composition des limites :

          $$\lim_{n \to +\infty} (\ln x_n)^n = \lim_{n \to +\infty} \frac{1}{\sqrt{x_n}} = \boxed{\frac{1}{\sqrt{\ell}}}$$

          **L'information capitale, à retenir pour la question 3-d.** Cette limite est un nombre **fini et strictement positif** — car $\ell$ est fini et strictement positif. Elle n'est ni nulle, ni infinie. Ce fait, apparemment anodin, est exactement l'arme qui produira la contradiction dans deux questions : c'est lui qui interdira à $n\ln(\ln x_n)$ de tendre vers $-\infty$.

          **La remarque qui montre qu'on a compris.** L'hypothèse $\ell > 1$ de la question précédente sert deux fois ici : elle assure $\ell \neq 0$ pour que $\frac{1}{\sqrt\ell}$ existe, et elle assure $\frac{1}{\sqrt\ell} > 0$ pour qu'on puisse en prendre le logarithme. Rien n'est gratuit dans l'enchaînement de cette partie.
      - id: q5c
        stem: |-
          **3- c)** (0,25 pt) Montrer que si $\ell < e$ alors $\displaystyle\lim_{n \to +\infty} n \ln(\ln x_n) = -\infty$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** C'est une implication **conditionnelle** : on ne prétend pas que $\ell < e$, on explore ce qui se passerait dans ce cas. Cette question prépare une démonstration par l'absurde — elle construit la branche qu'on va faire exploser à la question suivante.

          **On suppose donc $\ell < e$, et on en tire les conséquences.** Comme $x_n \to \ell$ et que $\ln$ est continue sur $]0,+\infty[$ :

          $$\ln x_n \longrightarrow \ln \ell$$

          Avec $1 < \ell < e$, la stricte croissance de $\ln$ donne :

          $$\ln 1 < \ln \ell < \ln e \qquad\Longrightarrow\qquad 0 < \ln \ell < 1$$

          **Le second logarithme.** $\ln\ell$ est strictement compris entre $0$ et $1$, donc on peut lui appliquer $\ln$ à nouveau, et le résultat est **strictement négatif** :

          $$\ln(\ln x_n) \longrightarrow \ln(\ln \ell) < 0$$

          car $\ln t < 0$ pour tout $t \in\, ]0,1[$.

          **La conclusion.** On multiplie une suite qui tend vers une constante **strictement négative** par $n$, qui tend vers $+\infty$ :

          $$n \, \ln(\ln x_n) \longrightarrow -\infty$$

          C'est le résultat annoncé. Aucune indétermination : la limite est un produit d'un facteur qui explose vers $+\infty$ par un facteur qui se stabilise sur une valeur strictement négative.

          **Là où la démonstration serait fausse.** Si l'on avait seulement $\ln\ell \leq 1$, avec égalité possible, alors $\ln(\ln\ell)$ pourrait valoir $0$ et le produit $n \times 0$ deviendrait une indétermination $\infty \times 0$. La stricte inégalité $\ell < e$ est donc **indispensable**, et c'est exactement l'hypothèse que l'énoncé pose. C'est le genre de détail qu'un correcteur vérifie.
      - id: q5d
        stem: |-
          **3- d)** (0,25 pt) En déduire la valeur de $\ell$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Les deux questions précédentes ont préparé deux affirmations sur la même quantité. Il ne reste qu'à les confronter — c'est un raisonnement **par l'absurde**, et il faut le rédiger comme tel : supposer, dériver la contradiction, conclure.

          **Ce que dit la question 3-b, traduit.** $(\ln x_n)^n \to \dfrac{1}{\sqrt{\ell}}$, une limite **finie et strictement positive**. On peut donc lui appliquer le logarithme, qui est continu en tout point strictement positif :

          $$\ln\!\left((\ln x_n)^n\right) \longrightarrow \ln\!\left(\frac{1}{\sqrt{\ell}}\right)$$

          Or, par la propriété du logarithme d'une puissance :

          $$\ln\!\left((\ln x_n)^n\right) = n\,\ln(\ln x_n)$$

          Donc $n\,\ln(\ln x_n)$ converge vers **un nombre fini**, à savoir $-\dfrac{1}{2}\ln\ell$.

          **La confrontation.** Supposons, par l'absurde, que $\ell < e$. La question 3-c affirme alors que $n\,\ln(\ln x_n) \to -\infty$. Mais on vient d'établir que cette même suite converge vers un nombre **fini**. Une suite ne peut pas à la fois tendre vers $-\infty$ et converger : **contradiction**.

          L'hypothèse $\ell < e$ est donc fausse. Comme la question 3-a a établi $\ell \leq e$, il ne reste qu'une possibilité :

          $$\boxed{\ \ell = e\ }$$

          **Le contrôle, qui vaut d'être fait.** Vérifions que la valeur trouvée est cohérente avec le reste. Avec $\ell = e$, la question 3-b donne $(\ln x_n)^n \to \dfrac{1}{\sqrt{e}}$. Et de fait, $\ln x_n \to \ln e = 1$ : on a une suite qui tend vers $1$, élevée à une puissance qui tend vers l'infini — une indétermination $1^\infty$, dont la limite $\frac{1}{\sqrt e} \approx 0{,}607$ est parfaitement plausible. Rien ne cloche.

          **La lecture d'ensemble, qui referme la Partie II.** La question 1-c avait montré que $f_n(\beta) \to 0$ en tout $\beta$ fixé de $]1,e[$ : les courbes s'écrasent vers $0$ sur cet intervalle. Pour que $f_n$ continue à valoir $1$ quelque part, le point où elle le fait doit donc fuir vers la seule borne où elle ne s'écrase pas — l'extrémité $e$, où $f_n(e) = \sqrt e$ reste constant. Le résultat $\ell = e$ n'est pas une surprise : c'était annoncé dès la première section de la partie, et la démonstration par l'absurde ne fait que le rendre rigoureux.
      - id: q6a
        part: "Partie III — 1. La fonction F et sa forme explicite"
        stem: |-
          **1- a)** (0,25 pt) On pose pour tout $x \in I$, $F(x) = \displaystyle\int_x^1 \left(f_1(t)\right)^2 dt$. Montrer que la fonction $F$ est continue sur $I$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** La difficulté potentielle est en $t = 0$, borne du domaine où $f_1$ n'est définie que par prolongement. Il faut donc s'assurer que la fonction qu'on intègre est bien continue **jusqu'en $0$ inclus** — et c'est la Partie I qui l'a déjà fait.

          **Ce qu'on intègre, explicité.** Pour $t > 0$ :

          $$\left(f_1(t)\right)^2 = \left(\sqrt{t}\,\ln t\right)^2 = t\,(\ln t)^2$$

          et en $t = 0$, $\left(f_1(0)\right)^2 = 0^2 = 0$. On intègre donc la fonction $g : t \mapsto t(\ln t)^2$, prolongée par $g(0) = 0$.

          **La continuité de $g$ sur $I$.** La question 1-a de la Partie I a établi que $f_1$ est continue à droite en $0$ ; elle est par ailleurs continue sur $]0,+\infty[$ comme produit de fonctions continues. Donc $f_1$ est **continue sur $I = [0, +\infty[$** tout entier. Le carré d'une fonction continue étant continu, $g = (f_1)^2$ est continue sur $I$.

          **La conclusion, en une phrase de cours.** Une fonction continue sur un intervalle y admet des primitives. Soit $G$ une primitive de $g$ sur $I$ ; alors

          $$F(x) = \int_x^1 g(t)\,dt = G(1) - G(x)$$

          $G$ étant dérivable sur $I$, elle y est continue, donc $F$ l'est aussi comme différence d'une constante et d'une fonction continue. **$F$ est continue sur $I$.**

          **Pourquoi cette question minuscule est en réalité l'une des plus utiles du problème.** C'est elle, et elle seule, qui autorisera à la question 2-b d'écrire $F(0) = \lim_{x\to 0^+} F(x)$. Sans la continuité en $0$, la limite calculée en 2-a ne dirait **rien** de la valeur $F(0)$ — et le volume de la question 2-c serait hors d'atteinte. Vingt-cinq centièmes de point qui portent la fin du problème.
      - id: q6b
        stem: |-
          **1- b)** (1 pt) En utilisant une double intégration par parties, montrer que : $\left(\forall x \in\, ]0, +\infty[\right)$ ; $F(x) = -\dfrac{x^2}{2}\ln^2(x) + \dfrac{x^2}{2}\ln(x) + \dfrac{1}{4}\left(1 - x^2\right)$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Un point entier, la question la plus chère de la Partie III. L'intégrande $t(\ln t)^2$ contient un logarithme au carré : chaque intégration par parties **fait descendre l'exposant du logarithme d'une unité**. Deux IPP suffisent donc à le faire disparaître, et l'énoncé annonce exactement ce plan.

          **Ce qu'on intègre.** $\left(f_1(t)\right)^2 = t\,(\ln t)^2$, donc

          $$F(x) = \int_x^1 t\,(\ln t)^2\,dt$$

          **Première intégration par parties.** Le choix se fait sur un principe : on **dérive** le logarithme (pour en baisser la puissance) et on **intègre** la puissance de $t$.

          $$u(t) = (\ln t)^2,\quad u'(t) = \frac{2\ln t}{t}
          \qquad\qquad
          v'(t) = t,\quad v(t) = \frac{t^2}{2}$$

          Les deux fonctions sont de classe $C^1$ sur $]0, +\infty[$, l'IPP est licite :

          $$F(x) = \left[\frac{t^2}{2}(\ln t)^2\right]_x^1 - \int_x^1 \frac{t^2}{2}\cdot\frac{2\ln t}{t}\,dt$$

          Le crochet : en $t = 1$, $(\ln 1)^2 = 0$ donc le terme est nul ; en $t = x$ il vaut $\dfrac{x^2}{2}(\ln x)^2$, à soustraire. L'intégrale restante se simplifie, $\dfrac{t^2}{2}\cdot\dfrac{2\ln t}{t} = t\ln t$ :

          $$F(x) = -\frac{x^2}{2}(\ln x)^2 - \int_x^1 t\ln t\,dt$$

          **Deuxième intégration par parties**, sur $\displaystyle\int_x^1 t\ln t\,dt$, avec le même principe :

          $$u(t) = \ln t,\quad u'(t) = \frac{1}{t}
          \qquad\qquad
          v'(t) = t,\quad v(t) = \frac{t^2}{2}$$

          $$\int_x^1 t\ln t\,dt = \left[\frac{t^2}{2}\ln t\right]_x^1 - \int_x^1 \frac{t}{2}\,dt
          = \left(0 - \frac{x^2}{2}\ln x\right) - \left[\frac{t^2}{4}\right]_x^1$$

          $$\int_x^1 t\ln t\,dt = -\frac{x^2}{2}\ln x - \left(\frac{1}{4} - \frac{x^2}{4}\right)$$

          **Le report, en surveillant le signe moins devant l'intégrale.** C'est ici que les copies se perdent : l'intégrale est **soustraite**, donc tous les signes de ce qu'on vient de calculer se retournent.

          $$F(x) = -\frac{x^2}{2}(\ln x)^2 - \left[-\frac{x^2}{2}\ln x - \frac{1}{4} + \frac{x^2}{4}\right]$$

          $$F(x) = -\frac{x^2}{2}(\ln x)^2 + \frac{x^2}{2}\ln x + \frac{1}{4} - \frac{x^2}{4}$$

          En factorisant les deux derniers termes par $\dfrac{1}{4}$ :

          $$\boxed{\ F(x) = -\frac{x^2}{2}\ln^2(x) + \frac{x^2}{2}\ln(x) + \frac{1}{4}\left(1 - x^2\right)\ }$$

          C'est exactement l'expression imprimée par l'énoncé.

          **Le contrôle gratuit, à faire systématiquement.** Évalue en $x = 1$ : les deux premiers termes contiennent $\ln 1 = 0$ et s'annulent, et le troisième vaut $\frac14(1 - 1) = 0$. On trouve $F(1) = 0$ — ce qui est nécessairement vrai puisque $F(1) = \int_1^1 = 0$. Un résultat qui échoue à ce test est faux ; celui-ci passe.

          **Le piège nommé de cette question.** Perdre le signe moins au report de la seconde IPP : c'est l'erreur qui produit $-\frac{x^2}{2}\ln x$ au lieu de $+\frac{x^2}{2}\ln x$, et le test en $x=1$ ne la détecte pas (le terme s'annule là). Le second piège : mal choisir $u$ et $v'$, en intégrant le logarithme au lieu de le dériver — l'exposant monterait au lieu de descendre, et la seconde IPP serait pire que la première.
      - id: q7a
        part: "Partie III — 2. La valeur en 0, et le volume"
        stem: |-
          **2- a)** (0,5 pt) Calculer $\displaystyle\lim_{\substack{x \to 0 \\ x > 0}} F(x)$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** On dispose maintenant d'une expression **explicite** de $F$ : la limite se calcule terme par terme, et chacun est une croissance comparée déjà connue.

          $$F(x) = -\frac{x^2}{2}\ln^2 x + \frac{x^2}{2}\ln x + \frac{1}{4}\left(1 - x^2\right)$$

          **Premier terme : $x^2\ln^2 x$.** C'est le carré de $x\ln x$, dont la limite en $0^+$ est $0$ (limite de référence, déjà utilisée en Partie I) :

          $$\lim_{x\to 0^+} x^2\ln^2 x = \left(\lim_{x\to 0^+} x\ln x\right)^{\!2} = 0^2 = 0$$

          **Deuxième terme : $x^2\ln x$.** Même mécanisme, en écrivant $x^2\ln x = x \cdot (x\ln x) \to 0 \times 0 = 0$. Autre lecture, plus directe : $\ln x$ diverge, mais $x^2$ écrase bien plus vite qu'il ne diverge.

          **Troisième terme.** $\dfrac{1}{4}(1 - x^2) \to \dfrac{1}{4}(1 - 0) = \dfrac{1}{4}$, sans difficulté.

          **La somme.**

          $$\boxed{\lim_{x \to 0^+} F(x) = 0 + 0 + \frac{1}{4} = \frac{1}{4}}$$

          **La remarque qui montre qu'on a compris.** Les deux termes qui portaient un logarithme s'évanouissent tous les deux : c'est le facteur $x^2$ qui gagne à chaque fois. Le comportement de $F$ en $0$ est donc entièrement dicté par le terme **polynomial**, celui qui ne contient aucun logarithme. Cette victoire systématique de la puissance sur le logarithme est le fil rouge de tout le problème, depuis la première question.
      - id: q7b
        stem: |-
          **2- b)** (0,25 pt) En déduire la valeur de $F(0)$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Le mot « en déduire » signale que la question précédente suffit — mais elle ne suffit qu'à une condition, et c'est cette condition qu'il faut citer.

          **L'argument, en deux lignes.** La question 1-a a établi que $F$ est **continue sur $I$**, donc en particulier continue à droite en $0$. Par définition de la continuité en un point :

          $$F(0) = \lim_{x \to 0^+} F(x) = \boxed{\frac{1}{4}}$$

          **Pourquoi il faut citer la continuité et pas seulement écrire le résultat.** Sans elle, la limite calculée en 2-a ne dirait rien de $F(0)$ : une fonction peut parfaitement avoir une limite en un point et y prendre une autre valeur. C'est exactement la fonction de la question 1-a dans l'architecture du problème — elle n'était pas là pour faire nombre.

          **Le contrôle de vraisemblance.** $F(0) = \int_0^1 t(\ln t)^2 dt$ est l'intégrale d'une fonction **positive** sur $[0,1]$ : elle doit être positive. $\frac14 > 0$ ✔. Et l'intégrande $t(\ln t)^2$ reste modeste sur $[0,1]$ — elle vaut $0$ aux deux bouts, avec un maximum intérieur — donc une valeur de l'ordre du quart est plausible. Rien ne cloche.
      - id: q7c
        stem: |-
          **2- c)** (0,5 pt) Calculer, en $\text{cm}^3$, le volume du solide engendré par la rotation d'un tour complet autour de l'axe des abscisses de la portion de la courbe $(C_1)$ relative à l'intervalle $[0,1]$. (On prendra $\left\|\vec{i}\right\| = 1\ \text{cm}$.)
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** La dernière question du problème, et elle est courte — tout le travail a déjà été fait. Il suffit de reconnaître que l'intégrale du volume est **exactement** $F(0)$.

          **La formule du cours.** Le volume engendré par la rotation autour de l'axe des abscisses de la portion de courbe de $f$ sur $[a, b]$ vaut :

          $$V = \pi \int_a^b \left(f(t)\right)^2 dt$$

          en unités de volume. L'idée derrière la formule : chaque tranche perpendiculaire à l'axe est un disque de rayon $|f(t)|$, donc d'aire $\pi f^2(t)$, et l'on somme ces aires le long de l'axe.

          **L'identification, qui est tout le travail restant.** Ici $f = f_1$, $a = 0$ et $b = 1$ :

          $$V = \pi \int_0^1 \left(f_1(t)\right)^2 dt$$

          Or, par définition, $F(0) = \displaystyle\int_0^1 \left(f_1(t)\right)^2 dt$ — les bornes coïncident exactement, dans le bon ordre. Donc :

          $$V = \pi\,F(0) = \pi \times \frac{1}{4}$$

          **L'unité, qu'il ne faut pas négliger.** L'énoncé précise $\left\|\vec{i}\right\| = 1\ \text{cm}$ dans un repère **orthonormé** : l'unité de longueur vaut $1\ \text{cm}$ sur les trois directions, donc l'unité de volume vaut $1\ \text{cm}^3$. Le nombre trouvé est donc directement un nombre de centimètres cubes.

          $$\boxed{\ V = \frac{\pi}{4}\ \text{cm}^3 \approx 0{,}785\ \text{cm}^3\ }$$

          **Le piège nommé de cette question.** Écrire $V = \pi\int_0^1 f_1(t)\,dt$ en oubliant le carré. La formule du volume porte sur le **carré** de la fonction — c'est le rayon du disque qui est $f_1(t)$, et l'aire d'un disque va en $r^2$. Second piège, plus insidieux : croire qu'il faut recalculer l'intégrale. Elle a déjà été calculée deux questions plus tôt ; toute la Partie III était construite pour aboutir ici, et refaire le calcul revient à ne pas avoir vu l'architecture du problème.
        steps:
          - math: 'V = \pi \int_0^1 \left(f_1(t)\right)^2 dt'
            note: "Formule du volume de revolution autour de (Ox) : le CARRE de la fonction, jamais la fonction."
          - math: 'F(0) = \int_0^1 \left(f_1(t)\right)^2 dt = \frac{1}{4}'
            note: "Meme integrale, memes bornes : c'est exactement F(0), etabli a la question 2-b."
          - math: 'V = \pi \times \frac{1}{4}'
            note: "Repere orthonorme avec norme de i egale a 1 cm : l'unite de volume vaut 1 cm3."
          - math: '\boxed{V = \frac{\pi}{4}\ \text{cm}^3 \approx 0{,}785\ \text{cm}^3}'
            note: "Positif et de l'ordre du cm3 : vraisemblable pour une courbe qui reste sous 1 sur [0,1]."
  # ── bk-2024-r-x1 — 2024 R, Exercice 1 : la famille f_n(x) = x - x^n ln x,
  #    la suite (alpha_n) de ses zeros dans ]1;2[, et sa limite ──
  #
  # NOTE ÉDITORIALE — CARTE DES DOMAINES. Comme bk-2023-r-x1 ci-dessus, ce
  # problème traverse plusieurs chapitres et reste ENTIER sous
  # fonction-logarithme (slug dominant : toute l'étude est une étude avec ln, et
  # les croissances comparées x^k ln x -> 0 portent quatre questions à elles
  # seules). Les cross-lists du classement — derivabilite-etude-fonctions,
  # limites-continuite, suites-numeriques — ne deviennent PAS des entrées : ce
  # serait un double comptage du barème que lib/examens.ts somme. Ils sont
  # honorés dans le raisonnement, qui nomme le domaine à chaque changement :
  #   — question 1-a, 1-b : continuité prolongée, limites, branche infinie ;
  #   — question 1-c, 1-d, 1-e : dérivabilité à droite, dérivée, variations ;
  #   — questions 3 et 4 : suite définie implicitement, monotonie, convergence.
  #
  # NOTE ÉDITORIALE — FLAG DE JUMELAGE (bk-2024-r-x1 face à bk-2023-r-x1). Les
  # deux sont des problèmes d'analyse de session de rattrapage bâtis sur une
  # FAMILLE de fonctions indexée par n, avec une suite définie implicitement par
  # une équation et une limite obtenue par l'absurde. Ce ne sont pas des
  # doublons, et la différence est instructive :
  #   — 2023 R : f_n(x) = sqrt(x)(ln x)^n, la suite (x_n) est CROISSANTE et sa
  #     limite est la BORNE DROITE e de l'intervalle ; la contradiction vient
  #     d'une limite finie confrontée à -infini ;
  #   — 2024 R : f_n(x) = x - x^n ln x, la suite (alpha_n) est DÉCROISSANTE et
  #     sa limite est la BORNE GAUCHE 1 ; la contradiction vient de +infini
  #     (le n-1 du membre de gauche) confronté à une limite finie.
  # Les deux miroirs se répondent. Proposition : garder les deux, et les
  # travailler dans cet ordre. À l'arbitrage de l'owner.
  - id: bk-2024-r-x1
    title: "Problème d'analyse : la famille f_n(x) = x − xⁿ ln x — dérivabilité à droite en 0, courbes emboîtées, et la suite décroissante de leurs zéros dans ]1 ; 2["
    source:
      year: 2024
      session: rattrapage
      filiere: "SM"
      exercise_label: "Exercice 1 — Analyse (6,5 points)"
    bareme_total: 6.5
    duration_min: 39
    sourcing:
      status: sourced
      note: >-
        2024 session de rattrapage — examen national Mathématiques, filière
        Sciences Mathématiques (A) et (B), option française, 4 h, coefficient
        9, code sujet RS 25, AlloSchool element/145741, scan
        course-436/upload-87449, page 2 sur 5 (l'énoncé complet tient sur cette
        seule page ; consignes en page 1). Barème de l'exercice = 6,5 points,
        recompté question par question sur le scan et revérifié ici :
        question 1 = 0,25 + 0,75 + 0,5 + 0,5 + 0,5 = 2,5 ; question 2 =
        0,5 + 0,25 = 0,75 ; question 3 = 0,5 + 0,25 + 0,25 + 0,5 + 0,25 =
        1,75 ; question 4 = 0,25 + 0,5 + 0,25 + 0,5 = 1,5 ; total 2,5 + 0,75 +
        1,75 + 1,5 = 6,5, conforme au total imprimé. Transcription vérifiée par
        une passe adversariale indépendante (2026-08-23,
        docs/sujets/_incoming/maths-sm-2024-r.md, statut « vérifié ») :
        re-fetch de element/145741, re-dérivation des chemins d'images depuis
        le HTML (5 pages, pagination « n/5 » sur chaque page, conforme), et
        re-lecture du texte caractère par caractère sur l'image — chaque
        nombre, indice, exposant, borne et quantificateur. L'exposant n de
        alpha_(n+1) puissance n, à la question 3-b, a été relu au zoom x5
        parce qu'il porte une question entière. Aucune figure n'accompagne cet
        exercice. Le vérificateur conclut : AUCUN DÉFAUT TROUVÉ — le sujet
        officiel est lui-même cohérent, aucune incohérence d'énoncé à
        signaler, aucun point laissé en lecture à confirmer.
        CONTRE-CALCUL REFAIT ICI, question par question : (1-d) la dérivée de
        x^n ln x vaut n x^(n-1) ln x + x^(n-1), d'où f_n'(x) = 1 - x^(n-1) -
        n x^(n-1) ln x, conforme à l'expression imprimée ; (1-e) le signe se
        traite en écrivant f_n'(x) = 1 - x^(n-1)(1 + n ln x) — pour x > 1 les
        deux facteurs dépassent 1 donc le produit aussi et f_n' < 0 ; pour
        0 < x < 1 le produit est inférieur à 1 dans les deux cas de figure
        (facteur négatif, ou deux facteurs entre 0 et 1) donc f_n' > 0 ;
        (3-a) avec ln 2 = 0,7 comme l'énoncé l'impose, f_n(2) = 2 - 0,7 x 2^n,
        et 2^n >= 4 dès n = 2 donne f_n(2) <= -0,8 < 0 tandis que f_n(1) = 1 >
        0 : le changement de signe est net et l'existence est acquise ; (3-b)
        f_(n+1)(alpha_(n+1)) = 0 donne alpha^(n+1) ln alpha = alpha, et la
        division par alpha (non nul car supérieur à 1) donne exactement
        l'identité demandée ; (4-b) de f_n(alpha_n) = 0 on tire
        alpha_n^(n-1) ln alpha_n = 1, et le passage au logarithme donne
        (n-1) ln alpha_n = -ln(ln alpha_n), d'où le résultat après division par
        ln alpha_n, non nul car alpha_n > 1 ; (4-d) le membre de gauche n - 1
        tend vers +infini tandis que l'hypothèse ell > 1 forcerait le membre de
        droite à converger vers ln(ln ell)/ln ell, fini — contradiction, donc
        ell = 1. Ce résultat coïncide avec la vérification numérique du
        vérificateur (alpha_2 environ 1,763, alpha_10 environ 1,205, alpha_200
        environ 1,020) et avec le contrôle refait ici sur alpha_2, racine de
        x ln x = 1 : 1,763 x ln(1,763) = 1,763 x 0,5670 = 0,9996. AJOUT DE
        CETTE ENTRÉE AU CORPUS : l'épreuve SM 2024 rattrapage, dont les
        exercices 2, 3, 4 et 5 étaient déjà convertis (calcul-integral,
        nombres-complexes-2, structures-algebriques, arithmetique), passe de
        13,50 à 20,00 points et devient assemblable en entier dans Examens
        blancs.
    intro: |-
      **Problème d'analyse (examen national, Sciences Mathématiques, session de rattrapage 2024 — Exercice 1, 6,5 points).**

      *L'épreuve dure 4 heures et comporte cinq exercices indépendants, traitables dans l'ordre choisi par le candidat. L'usage de la calculatrice n'est pas autorisé.*

      Soit $n$ un entier naturel supérieur ou égal à $2$. On considère la fonction numérique $f_n$ définie sur $[0, +\infty[$ par :

      $$f_n(0) = 0 \qquad\text{et}\qquad \forall x \in\, ]0, +\infty[\ ;\quad f_n(x) = x - x^n \ln x$$

      et on note $(C_n)$ sa courbe représentative dans un repère orthonormé.

      *Aucune figure n'accompagne cet exercice. La carte des domaines, pour savoir quoi réviser avant : la question 1 mobilise la continuité prolongée, les limites et les branches infinies, puis la dérivabilité à droite et l'étude de variations ; les questions 3 et 4 mobilisent les suites définies implicitement, leur monotonie et leur convergence. Le fil qui traverse tout est la croissance comparée $x^k \ln x \to 0$ en $0^+$ — elle porte à elle seule quatre questions.*

      *Prends note de l'hypothèse $n \geq 2$ : elle n'est pas décorative. La question 1-c tombe en défaut pour $n = 1$, et c'est exactement pour cela que l'énoncé l'exclut.*
    questions:
      - id: q1a
        part: "1 — La fonction : bords, dérivabilité et variations"
        stem: |-
          **1- a)** (0,25 pt) Montrer que $f_n$ est continue à droite en $0$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Il faut montrer que $\displaystyle\lim_{x \to 0^+} f_n(x) = f_n(0) = 0$. La fonction est une différence de deux termes ; on traite chacun séparément.

          $$f_n(x) = x - x^n \ln x$$

          **Le premier terme** tend vers $0$ sans difficulté.

          **Le second terme** est une forme indéterminée $0 \times (-\infty)$ : $x^n \to 0$ et $\ln x \to -\infty$. C'est la **croissance comparée** du chapitre qui la tranche :

          $$\lim_{x \to 0^+} x^k \ln x = 0 \qquad \text{pour tout entier } k \geq 1$$

          Ici $n \geq 2 \geq 1$, donc $x^n \ln x \to 0$. Si tu préfères t'appuyer sur la seule limite de référence $x\ln x \to 0$, écris $x^n \ln x = x^{n-1} \cdot (x \ln x) \to 0 \times 0 = 0$, ce qui est licite car $n - 1 \geq 1$.

          **La conclusion.**

          $$\lim_{x \to 0^+} f_n(x) = 0 - 0 = 0 = f_n(0)$$

          $f_n$ est donc **continue à droite en $0$**, et le prolongement $f_n(0) = 0$ posé par l'énoncé est le seul qui rende la fonction continue sur $[0, +\infty[$.

          **Le piège nommé de cette question.** Annoncer que $x^n \ln x \to -\infty$ « parce que $\ln x \to -\infty$ ». La puissance écrase le logarithme, toujours : c'est le contenu même de la croissance comparée, et il faut la citer par son nom, pas la subir.
      - id: q1b
        stem: |-
          **1- b)** (0,75 pt) Montrer que : $\displaystyle\lim_{x\to+\infty} f_n(x) = -\infty$ et $\displaystyle\lim_{x\to+\infty} \dfrac{f_n(x)}{x} = -\infty$ puis interpréter graphiquement le résultat obtenu.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Deux limites et une interprétation, pour trois quarts de point — et c'est l'interprétation qu'on oublie. Les deux limites, elles, se font par **factorisation par le terme dominant**, la technique standard dès qu'on a une différence de deux quantités qui explosent.

          **La première limite.** On factorise par $x$ :

          $$f_n(x) = x - x^n \ln x = x\left(1 - x^{n-1}\ln x\right)$$

          Comme $n \geq 2$, on a $n - 1 \geq 1$, donc $x^{n-1} \to +\infty$ et $\ln x \to +\infty$ : leur produit tend vers $+\infty$, et

          $$1 - x^{n-1}\ln x \longrightarrow -\infty$$

          Le facteur $x$ tendant vers $+\infty$, le produit donne :

          $$\boxed{\lim_{x \to +\infty} f_n(x) = -\infty}$$

          **La seconde limite.** Elle est en réalité déjà faite — la parenthèse ci-dessus **est** le quotient :

          $$\frac{f_n(x)}{x} = 1 - x^{n-1}\ln x
          \qquad\Longrightarrow\qquad
          \boxed{\lim_{x \to +\infty} \frac{f_n(x)}{x} = -\infty}$$

          C'est le bénéfice de la factorisation : une seule écriture donne les deux réponses.

          **L'interprétation graphique, et c'est le troisième geste.** La conjonction « $f_n(x) \to -\infty$ **et** $\dfrac{f_n(x)}{x} \to -\infty$ » porte un nom précis dans le cours :

          > $(C_n)$ admet, au voisinage de $+\infty$, une **branche parabolique de direction l'axe des ordonnées** $(Oy)$.

          **Ce que cela veut dire concrètement.** La courbe plonge vers le bas, et de plus en plus **raide** : sa pente moyenne $\dfrac{f_n(x)}{x}$ n'est pas seulement négative, elle diverge. La courbe se redresse donc vers la verticale — elle finit par descendre presque parallèlement à l'axe des ordonnées.

          **Le piège nommé de cette question.** Confondre les trois cas de branche infinie. Si $\dfrac{f_n(x)}{x}$ tendait vers une constante finie $a$, il faudrait examiner $f_n(x) - ax$ pour trancher entre asymptote oblique et branche parabolique de direction $y = ax$. Ici le quotient **diverge**, ce qui élimine ces deux cas d'un coup et impose la direction $(Oy)$. Le second piège, symétrique : dire « direction $(Ox)$ », qui correspondrait au cas où le quotient tend vers $0$ — c'est-à-dire exactement l'inverse de ce qui se passe.
      - id: q1c
        stem: |-
          **1- c)** (0,5 pt) Montrer que $f_n$ est dérivable à droite en $0$ et que son nombre dérivé à droite en $0$ est égal à $1$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** En $0$, la fonction n'est définie que par un prolongement : aucune formule de dérivation ne s'y applique. Il faut **revenir à la définition** — le taux d'accroissement — et montrer qu'il admet une limite finie.

          **Le taux d'accroissement.** Comme $f_n(0) = 0$ :

          $$\frac{f_n(x) - f_n(0)}{x - 0} = \frac{f_n(x)}{x} = \frac{x - x^n\ln x}{x} = 1 - x^{n-1}\ln x$$

          On retrouve exactement l'expression de la question 1-b, mais on va la faire tendre vers $0^+$ cette fois, et non vers $+\infty$.

          **La limite.** Comme $n \geq 2$, on a $n - 1 \geq 1$, donc la croissance comparée s'applique :

          $$\lim_{x \to 0^+} x^{n-1}\ln x = 0$$

          D'où :

          $$\lim_{x \to 0^+} \frac{f_n(x) - f_n(0)}{x - 0} = 1 - 0 = 1$$

          Cette limite est **finie**. Par définition, $f_n$ est donc **dérivable à droite en $0$**, et

          $$\boxed{f_{n,d}'(0) = 1}$$

          **Où l'hypothèse $n \geq 2$ devient indispensable, et il faut le voir.** Pour $n = 1$, le terme $x^{n-1}\ln x$ deviendrait $x^0 \ln x = \ln x$, qui tend vers $-\infty$ : le taux d'accroissement divergerait et $f_1$ ne serait **pas** dérivable en $0$. L'énoncé écarte $n = 1$ dès la première ligne précisément pour cette raison. Un candidat qui remarque ce point montre qu'il a lu l'énoncé et pas seulement les questions.

          **La lecture géométrique.** $(C_n)$ admet à l'origine une **demi-tangente de pente $1$**, c'est-à-dire dirigée par la droite $y = x$. Toutes les courbes de la famille partent donc de $O$ dans la même direction, quelle que soit la valeur de $n$ — un fait que la question 2 va compléter en montrant comment elles se séparent ensuite.
      - id: q1d
        stem: |-
          **1- d)** (0,5 pt) Montrer que $f_n$ est dérivable sur $]0,+\infty[$ et que : $\forall x \in\, ]0, +\infty[$ ; $f_n'(x) = 1 - x^{n-1} - n\,x^{n-1}\ln x$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Une question « montrer que » : le résultat est donné, donc les points sont dans le chemin — justifier la dérivabilité, puis dériver proprement le produit $x^n \ln x$.

          **La dérivabilité, en une phrase.** Sur $]0, +\infty[$, la fonction $x \mapsto x$ est dérivable, $x \mapsto x^n$ l'est aussi, et $x \mapsto \ln x$ l'est sur cet intervalle. $f_n$ est donc dérivable comme **différence** d'une fonction dérivable et d'un **produit** de fonctions dérivables.

          **La dérivée du produit, terme par terme.** On applique $(uv)' = u'v + uv'$ avec $u(x) = x^n$ et $v(x) = \ln x$ :

          $$\left(x^n \ln x\right)' = n\,x^{n-1}\ln x + x^n \cdot \frac{1}{x}$$

          Le second terme se simplifie : $\dfrac{x^n}{x} = x^{n-1}$. Donc

          $$\left(x^n \ln x\right)' = n\,x^{n-1}\ln x + x^{n-1}$$

          **On reporte, en surveillant le signe moins.** $f_n(x) = x - x^n\ln x$, donc le produit est **soustrait** et sa dérivée l'est aussi, en entier :

          $$f_n'(x) = 1 - \left(n\,x^{n-1}\ln x + x^{n-1}\right)$$

          $$\boxed{\ f_n'(x) = 1 - x^{n-1} - n\,x^{n-1}\ln x\ }$$

          C'est exactement l'expression imprimée par l'énoncé.

          **Le contrôle gratuit, à faire systématiquement.** Évalue en $x = 1$ : $f_n'(1) = 1 - 1 - n \times 1 \times 0 = 0$. La dérivée s'annule en $1$ — ce qui est cohérent avec la question suivante, qui va montrer que $f_n$ croît avant $1$ et décroît après. Le contrôle passe.

          **Le piège nommé de cette question.** Distribuer le signe moins sur un seul des deux termes de la dérivée du produit, et écrire $1 - n x^{n-1}\ln x + x^{n-1}$. La parenthèse entière change de signe : les deux termes sont soustraits. C'est l'erreur la plus banale de tout le programme, et ici elle est détectable en trois secondes par le contrôle en $x = 1$, qui ne donnerait plus $0$.
      - id: q1e
        stem: |-
          **1- e)** (0,5 pt) Montrer que $f_n$ est strictement croissante sur $[0;1]$ et strictement décroissante sur $[1,+\infty[$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Une étude de signe, mais l'expression $1 - x^{n-1} - n x^{n-1}\ln x$ n'est pas factorisée et ne se prête pas à un tableau de signes naïf. Le geste qui débloque : **mettre $x^{n-1}$ en facteur dans les deux derniers termes**, ce qui isole un « $1$ » d'un côté et un produit de l'autre, puis comparer.

          **La réécriture.**

          $$f_n'(x) = 1 - x^{n-1}\left(1 + n\ln x\right)$$

          Le signe de $f_n'(x)$ se ramène donc à la comparaison du produit $P(x) = x^{n-1}(1 + n\ln x)$ avec $1$ :

          $$f_n'(x) > 0 \iff P(x) < 1
          \qquad\text{et}\qquad
          f_n'(x) < 0 \iff P(x) > 1$$

          **Sur $]1, +\infty[$ : c'est immédiat.** Pour $x > 1$ :
          — $x^{n-1} > 1$, car $n - 1 \geq 1$ et $x > 1$ ;
          — $\ln x > 0$, donc $1 + n\ln x > 1$.

          Deux facteurs strictement supérieurs à $1$ : leur produit l'est aussi, $P(x) > 1$, donc $f_n'(x) < 0$. **$f_n$ est strictement décroissante sur $[1, +\infty[$.**

          **Sur $]0, 1[$ : deux cas, et il faut les écrire tous les deux.** Pour $0 < x < 1$, on a $\ln x < 0$, donc $1 + n\ln x < 1$. Deux situations selon le signe de ce facteur.

          *Cas 1 — $1 + n\ln x \leq 0$.* Alors $P(x) = x^{n-1}(1 + n\ln x) \leq 0 < 1$, puisque $x^{n-1} > 0$. Donc $f_n'(x) > 0$.

          *Cas 2 — $0 < 1 + n\ln x < 1$.* Alors $P(x)$ est le produit de deux nombres strictement compris entre $0$ et $1$ — car $0 < x^{n-1} < 1$ pour $0 < x < 1$ — donc $P(x) < 1$. Donc $f_n'(x) > 0$.

          Dans les deux cas $f_n'(x) > 0$ : **$f_n$ est strictement croissante sur $]0, 1]$.**

          **Le raccord en $0$.** La question 1-c a établi que $f_n$ est dérivable à droite en $0$ avec $f_{n,d}'(0) = 1 > 0$. La croissance stricte s'étend donc à l'intervalle fermé $[0, 1]$, comme l'énoncé le demande.

          **Le portrait complet, à avoir en tête pour la suite.** $f_n(0) = 0$, la fonction croît jusqu'à $x = 1$ où elle vaut $f_n(1) = 1 - 1^n\ln 1 = 1$, puis décroît vers $-\infty$. Le point $(1\,;\,1)$ est donc le **maximum global**, et il est commun à toutes les courbes de la famille. C'est ce sommet partagé que la question 2 va exploiter, et c'est parce que la fonction décroît strictement de $1$ à $-\infty$ après $x = 1$ qu'elle traverse $0$ une fois et une seule — l'objet de la question 3.

          **Le piège nommé de cette question.** Vouloir étudier le signe de $f_n'$ en cherchant ses racines par un calcul. L'équation $x^{n-1}(1 + n\ln x) = 1$ n'a pas de solution explicite ; la seule voie est la **comparaison** menée ci-dessus. Reconnaître qu'une équation n'est pas résoluble et contourner par des inégalités est un réflexe d'analyse, pas un aveu d'échec.
      - id: q2a
        part: "2 — Les courbes de la famille s'emboîtent"
        stem: |-
          **2- a)** (0,5 pt) Montrer que pour tout entier $n \geq 2$, on a : $\forall x \in [0, +\infty[$ ; $f_{n+1}(x) \leq f_n(x)$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Comparer deux fonctions, c'est **étudier le signe de leur différence**. Ici la différence se calcule et se factorise entièrement — il n'y a aucune astuce à trouver, seulement à mener le calcul jusqu'au bout.

          **La différence, développée.** Pour $x > 0$ :

          $$f_n(x) - f_{n+1}(x) = \left(x - x^n\ln x\right) - \left(x - x^{n+1}\ln x\right)$$

          Les deux termes $x$ s'annulent :

          $$f_n(x) - f_{n+1}(x) = x^{n+1}\ln x - x^n\ln x$$

          **La factorisation.** On met $x^n \ln x$ en facteur :

          $$f_n(x) - f_{n+1}(x) = x^n \ln x\,(x - 1)$$

          **Le signe, facteur par facteur.** $x^n > 0$ pour $x > 0$ : il ne joue aucun rôle. Reste le produit $\ln x \cdot (x-1)$, et c'est là qu'est l'observation :

          — si $0 < x < 1$ : $\ln x < 0$ **et** $x - 1 < 0$ — deux négatifs, produit **positif** ;
          — si $x = 1$ : les deux facteurs sont nuls, produit **nul** ;
          — si $x > 1$ : $\ln x > 0$ **et** $x - 1 > 0$ — deux positifs, produit **positif**.

          Dans tous les cas, $\ln x\,(x-1) \geq 0$. Ce n'est pas un hasard : $\ln$ et $x \mapsto x-1$ s'annulent au **même point** $x = 1$ et sont toutes deux croissantes, donc elles ont partout le même signe.

          **Le cas $x = 0$, à ne pas oublier.** L'énoncé demande l'inégalité sur $[0, +\infty[$, borne incluse. En $0$ : $f_n(0) = f_{n+1}(0) = 0$, donc la différence est nulle et l'inégalité large est vérifiée.

          **Conclusion.** Pour tout $x \in [0, +\infty[$ :

          $$f_n(x) - f_{n+1}(x) \geq 0
          \qquad\Longrightarrow\qquad
          \boxed{f_{n+1}(x) \leq f_n(x)}$$

          avec égalité **exactement** en $x = 0$ et $x = 1$.

          **Le piège nommé de cette question.** Se contenter d'écrire « $x^{n+1} \leq x^n$ pour $x \leq 1$ donc… ». L'inégalité entre les puissances change de sens en $x = 1$, et le facteur $\ln x$ change de signe au même endroit : les deux renversements se compensent, ce qui est **exactement** le contenu du résultat. Une démonstration qui ne traite qu'un côté de $1$ passe à côté de la moitié de l'énoncé.
      - id: q2b
        stem: |-
          **2- b)** (0,25 pt) En déduire la position relative des deux courbes $(C_n)$ et $(C_{n+1})$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** « Position relative » est la traduction géométrique de l'inégalité qu'on vient d'établir. Une réponse complète dit **qui est au-dessus**, sur quel domaine, et **où les courbes se touchent**.

          **La lecture.** $f_{n+1}(x) \leq f_n(x)$ pour tout $x \in [0, +\infty[$, donc :

          > $(C_{n+1})$ est **au-dessous** de $(C_n)$ sur tout l'intervalle $[0, +\infty[$.

          **Les points de contact, qui font la précision de la réponse.** L'égalité a lieu exactement en $x = 0$ et $x = 1$. Les deux courbes se **coupent** donc en deux points, et ces points sont les mêmes pour toute la famille :

          $$O(0\,;\,0) \qquad\text{et}\qquad A(1\,;\,1)$$

          puisque $f_n(0) = 0$ et $f_n(1) = 1$ pour tout $n$.

          **Le portrait d'ensemble, qui vaut d'être formulé.** Toutes les courbes de la famille passent par les deux mêmes points $O$ et $A$, partent de $O$ avec la **même** demi-tangente de pente $1$ (question 1-c), atteignent leur maximum commun en $A$, et s'ordonnent entre elles : plus $n$ grandit, plus la courbe est basse. La famille $(C_n)$ forme donc un **faisceau de courbes emboîtées**, épinglées en deux points fixes et s'écrasant vers le bas à mesure que $n$ croît.

          **Ce que cela annonce pour la question 3.** Si les courbes s'enfoncent quand $n$ grandit, alors leur point de traversée de l'axe des abscisses après $x = 1$ se rapproche de $1$. C'est exactement la décroissance de la suite $(\alpha_n)$ que la question 3-d va démontrer, et sa limite $\ell = 1$ que la question 4 va établir. Voir le dessin avant de faire les calculs rend toute la fin de l'exercice lisible.
      - id: q3a
        part: "3 — La suite (αₙ) des zéros dans ]1 ; 2["
        stem: |-
          **3- a)** (0,5 pt) Montrer que pour tout $n \geq 2$, il existe un unique réel $\alpha_n \in\, ]1;2[$ tel que : $f_n(\alpha_n) = 0$. *(On prendra $\ln 2 = 0{,}7$.)*
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** « Il existe un unique » : deux affirmations, deux outils — le **théorème des valeurs intermédiaires** pour l'existence, la **stricte monotonie** pour l'unicité. C'est le schéma le plus rentable du programme d'analyse, et il se rédige toujours de la même façon : les trois hypothèses, puis la conclusion.

          **Continuité.** $f_n$ est dérivable sur $]0, +\infty[$ (question 1-d), donc continue sur $[1, 2]$.

          **Stricte monotonie.** La question 1-e a établi que $f_n$ est **strictement décroissante** sur $[1, +\infty[$, donc en particulier sur $[1, 2]$.

          **Les valeurs aux bornes, et c'est ici que sert l'indication $\ln 2 = 0{,}7$.**

          En $x = 1$ : $f_n(1) = 1 - 1^n\ln 1 = 1 - 0 = \mathbf{1 > 0}$.

          En $x = 2$ :

          $$f_n(2) = 2 - 2^n\ln 2 = 2 - 0{,}7 \times 2^n$$

          Or $n \geq 2$, donc $2^n \geq 4$, donc $0{,}7 \times 2^n \geq 2{,}8$. D'où :

          $$f_n(2) \leq 2 - 2{,}8 = -0{,}8 < 0$$

          La majoration vaut pour **tout** $n \geq 2$, et elle est même très large : plus $n$ grandit, plus $f_n(2)$ est négatif.

          **La conclusion.** $f_n$ est continue et strictement décroissante sur $[1, 2]$, avec $f_n(1) > 0 > f_n(2)$. Elle réalise donc une **bijection** de $[1, 2]$ sur $[f_n(2),\, 1]$, intervalle qui contient $0$. L'équation $f_n(x) = 0$ admet par conséquent **une solution et une seule** dans $]1, 2[$ — on la note $\alpha_n$.

          **Pourquoi l'énoncé fournit $\ln 2 = 0{,}7$.** La calculatrice est interdite. Sans cette valeur, il serait impossible de décider du signe de $2 - 2^n \ln 2$ — c'est-à-dire impossible d'appliquer le TVI. L'indication n'est pas un confort, c'est une **donnée de l'exercice**, et il faut l'utiliser explicitement.

          **Le piège nommé de cette question.** Invoquer le TVI et s'arrêter là. Le TVI donne l'**existence**, jamais l'unicité — c'est la stricte décroissance qui interdit une seconde traversée. Le second piège : traiter le cas $n = 2$ et écrire « de même pour les autres ». La majoration $2^n \geq 4$ vaut pour tout $n \geq 2$ d'un seul coup ; il faut l'écrire ainsi, en une ligne valable pour tous les rangs.
        steps:
          - math: 'f_n(1) = 1 - 1^n \ln 1 = 1 > 0'
            note: "Valeur au bord gauche : c'est le maximum global de f_n, etabli a la question 1-e."
          - math: 'f_n(2) = 2 - 2^n \ln 2 = 2 - 0{,}7 \times 2^n'
            note: "L'enonce impose ln 2 = 0,7 : la calculatrice est interdite, cette valeur est une DONNEE."
          - math: 'n \geq 2 \Longrightarrow 2^n \geq 4 \Longrightarrow 0{,}7 \times 2^n \geq 2{,}8 \Longrightarrow f_n(2) \leq -0{,}8 < 0'
            note: "Majoration valable pour TOUS les rangs d'un seul coup, pas rang par rang."
          - math: 'f_n \text{ continue et strictement décroissante sur } [1,2],\quad f_n(1) > 0 > f_n(2)'
            note: "TVI pour l'existence, stricte monotonie pour l'unicite : les deux sont necessaires."
          - math: '\boxed{\exists !\,\alpha_n \in\, ]1;2[\ ;\ f_n(\alpha_n) = 0}'
            note: "Bijection de [1,2] sur un intervalle contenant 0."
      - id: q3b
        stem: |-
          **3- b)** (0,25 pt) Vérifier que $(\forall n \geq 2)$ $\alpha_{n+1}^{\,n} \ln \alpha_{n+1} = 1$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Lis bien les indices : la relation porte sur $\alpha_{n+1}$, pas sur $\alpha_n$, et l'exposant est $n$, pas $n+1$. C'est un décalage volontaire, et c'est ce qui rendra la question 3-c possible. On part donc de la définition de $\alpha_{n+1}$ et on ajuste l'exposant.

          **Le point de départ.** Par définition, $\alpha_{n+1}$ est le zéro de $f_{n+1}$ dans $]1,2[$ :

          $$f_{n+1}(\alpha_{n+1}) = 0
          \qquad\text{c'est-à-dire}\qquad
          \alpha_{n+1} - \alpha_{n+1}^{\,n+1}\ln \alpha_{n+1} = 0$$

          soit, en isolant :

          $$\alpha_{n+1}^{\,n+1}\ln \alpha_{n+1} = \alpha_{n+1}$$

          **La division, qu'il faut justifier.** $\alpha_{n+1} \in\, ]1, 2[$, donc $\alpha_{n+1} > 1 > 0$ : la division par $\alpha_{n+1}$ est licite. En divisant les deux membres :

          $$\frac{\alpha_{n+1}^{\,n+1}}{\alpha_{n+1}}\,\ln\alpha_{n+1} = 1
          \qquad\Longrightarrow\qquad
          \boxed{\ \alpha_{n+1}^{\,n}\ln\alpha_{n+1} = 1\ }$$

          en utilisant $\dfrac{x^{n+1}}{x} = x^n$.

          **La lecture, et pourquoi cette identité est utile.** Le facteur $\alpha_{n+1}^{\,n}\ln\alpha_{n+1}$ est **exactement** le terme qui apparaît dans $f_n$ évaluée en $\alpha_{n+1}$ — c'est-à-dire dans une fonction d'indice $n$ évaluée au zéro du rang **suivant**. La question 3-c va s'en servir immédiatement. Tout l'intérêt du décalage d'indices est là : il permet de comparer deux rangs consécutifs, ce qui est le seul moyen d'établir la monotonie d'une suite définie implicitement.

          **Le piège nommé de cette question.** Confondre $\alpha_n$ et $\alpha_{n+1}$ et écrire $\alpha_n^{\,n}\ln\alpha_n = 1$. Cette relation-là est vraie aussi — elle sert d'ailleurs à la question 4-b, sous la forme $\alpha_n^{\,n-1}\ln\alpha_n = 1$ — mais ce n'est pas celle qu'on demande, et elle ne mène nulle part pour la question 3-c.
      - id: q3c
        stem: |-
          **3- c)** (0,25 pt) En déduire que pour tout $n \geq 2$, $f_n(\alpha_{n+1}) = \alpha_{n+1} - 1$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** « En déduire » : la question précédente suffit, il n'y a qu'à substituer. C'est un quart de point qui ne coûte que deux lignes — à condition d'avoir vu que le terme à remplacer est déjà tout prêt.

          **La substitution.** On écrit $f_n$ évaluée en $\alpha_{n+1}$, en appliquant la définition de $f_n$ :

          $$f_n(\alpha_{n+1}) = \alpha_{n+1} - \alpha_{n+1}^{\,n}\ln\alpha_{n+1}$$

          Le terme soustrait est exactement celui dont la question 3-b vient d'établir qu'il vaut $1$ :

          $$\boxed{\ f_n(\alpha_{n+1}) = \alpha_{n+1} - 1\ }$$

          **Le renseignement capital, à extraire tout de suite.** $\alpha_{n+1} \in\, ]1, 2[$, donc $\alpha_{n+1} - 1 > 0$, et par conséquent :

          $$f_n(\alpha_{n+1}) > 0$$

          Autrement dit : **la fonction du rang $n$ est encore strictement positive au zéro du rang $n+1$.** C'est exactement l'information dont la question 3-d a besoin, et c'est pour la produire que les questions 3-b et 3-c existent. Une chaîne de trois questions à un quart de point chacune peut porter tout le reste d'un exercice.
      - id: q3d
        stem: |-
          **3- d)** (0,5 pt) Montrer que la suite $(\alpha_n)_{n\geq 2}$ ainsi définie est strictement décroissante.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** $\alpha_n$ n'est donné par aucune formule : il est défini **implicitement**, comme le zéro d'une fonction. Il est donc impossible de comparer $\alpha_{n+1}$ et $\alpha_n$ par le calcul. La technique — celle de toutes les suites définies implicitement — consiste à évaluer **une seule et même fonction** aux deux points, puis à remonter aux abscisses par la monotonie.

          **Les deux valeurs, comparées.** On évalue $f_n$ aux deux points :

          $$f_n(\alpha_n) = 0 \qquad\text{(définition de } \alpha_n\text{)}$$
          $$f_n(\alpha_{n+1}) = \alpha_{n+1} - 1 > 0 \qquad\text{(question 3-c)}$$

          Donc :

          $$f_n(\alpha_{n+1}) > f_n(\alpha_n)$$

          **Le passage aux abscisses, et c'est là que le sens s'inverse.** Les deux points $\alpha_n$ et $\alpha_{n+1}$ appartiennent à $]1, 2[$, intervalle sur lequel $f_n$ est **strictement décroissante** (question 1-e). Une fonction strictement décroissante **renverse** l'ordre : si les images sont dans un sens, les antécédents sont dans l'autre.

          $$f_n(\alpha_{n+1}) > f_n(\alpha_n)
          \qquad\Longrightarrow\qquad
          \alpha_{n+1} < \alpha_n$$

          La suite $(\alpha_n)_{n\geq 2}$ est donc **strictement décroissante**.

          **La vérification par le dessin, qui rend le résultat évident.** La question 2 a montré que $(C_{n+1})$ est **sous** $(C_n)$. Après $x = 1$ les deux courbes descendent ; celle du rang $n+1$ étant plus basse, elle traverse l'axe des abscisses **plus tôt**. Son zéro $\alpha_{n+1}$ est donc à gauche de $\alpha_n$. Le calcul et la géométrie disent la même chose.

          **Le piège nommé de cette question.** Écrire $f_n(\alpha_n) = 0 = f_{n+1}(\alpha_{n+1})$ et croire en tirer quelque chose. C'est une comparaison entre **deux fonctions différentes** évaluées en deux points différents : elle ne dit rien du tout. Il faut impérativement une seule fonction — ici $f_n$ — évaluée aux deux points, pour que la monotonie ait prise. Le second piège : oublier que la fonction **décroît** et conclure $\alpha_{n+1} > \alpha_n$, ce qui retourne le résultat.
      - id: q3e
        stem: |-
          **3- e)** (0,25 pt) En déduire que la suite $(\alpha_n)_{n\geq 2}$ est convergente.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Le théorème de la limite monotone : une suite **décroissante et minorée** converge. Il ne reste qu'à fournir le minorant, et il est sous les yeux depuis la question 3-a.

          **Les deux ingrédients.**
          — La suite est **strictement décroissante** (question 3-d).
          — Elle est **minorée par $1$** : par construction, $\alpha_n \in\, ]1, 2[$ pour tout $n \geq 2$, donc $\alpha_n > 1$.

          **La conclusion.** Une suite décroissante et minorée est convergente. Donc $(\alpha_n)_{n \geq 2}$ **converge**, et sa limite $\ell$ vérifie $\ell \geq 1$.

          **Ce qu'on n'a PAS démontré, et qu'il ne faut pas croire acquis.** Le théorème donne l'existence de la limite, jamais sa valeur. Rien ici ne dit que $\ell = 1$ : la suite pourrait converger vers $1{,}3$ tout en restant au-dessus de $1$. C'est tout l'objet de la question 4, qui procédera par l'absurde.

          **Le piège nommé de cette question.** Conclure directement $\ell = 1$ « puisque la suite décroît et est minorée par $1$ ». Une suite décroissante minorée par $1$ converge vers sa **borne inférieure**, qui peut être n'importe quel réel supérieur ou égal à $1$ — pas nécessairement le minorant qu'on a choisi. Un minorant n'est pas une limite.
      - id: q4a
        part: "4 — La limite ℓ, obtenue par l'absurde"
        stem: |-
          **4- a)** (0,25 pt) On pose $\ell = \displaystyle\lim_{n\to+\infty} \alpha_n$. Montrer que : $1 \leq \ell \leq 2$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Un encadrement, obtenu par passage à la limite. La seule chose à savoir, et elle est décisive pour la suite : le passage à la limite **affaiblit** toujours une inégalité stricte.

          **L'encadrement de départ.** Par construction, pour tout $n \geq 2$ :

          $$1 < \alpha_n < 2$$

          **Le passage à la limite.** Les inégalités strictes deviennent larges :

          $$\boxed{\,1 \leq \ell \leq 2\,}$$

          **Pourquoi l'énoncé écrit des inégalités LARGES, et pourquoi c'est essentiel.** On ne peut pas conclure $\ell > 1$ : le passage à la limite ne conserve pas la stricte inégalité. Le contre-exemple qui le prouve : $1 + \frac{1}{n} > 1$ pour tout $n$, et pourtant la limite vaut exactement $1$.

          Ce détail n'est pas une subtilité de rédaction — c'est **le pivot de tout l'exercice**. Si l'on pouvait affirmer $\ell > 1$, la question 4-d n'aurait aucun sens : c'est précisément parce que la valeur $\ell = 1$ reste possible que la démonstration par l'absurde des questions 4-c et 4-d va pouvoir la désigner comme la seule.

          **Le contraste avec le problème miroir de 2023.** Dans le problème d'analyse du rattrapage 2023, la suite était **croissante** et l'on démontrait $1 < \ell \leq e$ — l'inégalité stricte du côté gauche s'obtenait par minoration par le **premier terme**, pas par passage à la limite. Ici la suite décroît, ce chemin n'existe pas, et l'encadrement reste large des deux côtés. Même schéma, orientation inversée.
      - id: q4b
        stem: |-
          **4- b)** (0,5 pt) Montrer que pour tout $n \geq 2$, $n - 1 = -\dfrac{\ln(\ln(\alpha_n))}{\ln(\alpha_n)}$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** On veut faire apparaître $n$ **tout seul** d'un côté de l'égalité. L'idée : $n$ est un exposant dans la relation qui définit $\alpha_n$, et le moyen de faire descendre un exposant est le **logarithme**.

          **Le point de départ.** Par définition de $\alpha_n$ :

          $$f_n(\alpha_n) = 0
          \qquad\Longrightarrow\qquad
          \alpha_n - \alpha_n^{\,n}\ln\alpha_n = 0
          \qquad\Longrightarrow\qquad
          \alpha_n^{\,n}\ln\alpha_n = \alpha_n$$

          On divise par $\alpha_n$, licite car $\alpha_n > 1 > 0$ :

          $$\alpha_n^{\,n-1}\ln\alpha_n = 1$$

          **La forme utile.** On isole la puissance :

          $$\alpha_n^{\,n-1} = \frac{1}{\ln\alpha_n}$$

          la division étant licite car $\alpha_n > 1$ donne $\ln\alpha_n > 0$, donc non nul.

          **Le passage au logarithme.** Les deux membres sont strictement positifs, on peut donc leur appliquer $\ln$ :

          $$\ln\!\left(\alpha_n^{\,n-1}\right) = \ln\!\left(\frac{1}{\ln\alpha_n}\right)$$

          À gauche, la propriété du logarithme d'une puissance fait descendre l'exposant — c'est exactement ce qu'on cherchait. À droite, $\ln\frac{1}{a} = -\ln a$ :

          $$(n-1)\,\ln\alpha_n = -\ln\!\left(\ln\alpha_n\right)$$

          **La division finale.** $\ln\alpha_n \neq 0$ car $\alpha_n > 1$ :

          $$\boxed{\ n - 1 = -\frac{\ln\!\left(\ln\alpha_n\right)}{\ln\alpha_n}\ }$$

          **Ce que cette identité vaut, et pourquoi elle est l'arme de la question 4-d.** Le membre de gauche est $n - 1$, qui tend vers $+\infty$ **de façon certaine**. Le membre de droite ne dépend que de $\alpha_n$. On dispose donc d'une contrainte : quelle que soit la limite $\ell$, le membre de droite est **obligé** de diverger vers $+\infty$. Toute hypothèse sur $\ell$ qui le ferait converger sera fausse.

          **Le piège nommé de cette question.** Appliquer $\ln$ sans vérifier la stricte positivité des deux membres. Ici tout va bien parce que $\alpha_n > 1$ — donc $\ln\alpha_n > 0$, donc $\ln(\ln\alpha_n)$ existe — mais c'est une conséquence de l'appartenance de $\alpha_n$ à $]1,2[$, pas une évidence. Le double logarithme est une composée fragile : elle exige que l'argument intérieur soit strictement positif, ce qui exclut tout $\alpha_n \leq 1$.
      - id: q4c
        stem: |-
          **4- c)** (0,25 pt) On suppose que $\ell > 1$. Calculer $\displaystyle\lim_{n\to+\infty} \dfrac{\ln(\ln(\alpha_n))}{\ln(\alpha_n)}$ en fonction de $\ell$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Une hypothèse est **posée** — $\ell > 1$ — et on en tire les conséquences. On n'affirme rien sur la vraie valeur de $\ell$ : on construit la branche que la question suivante fera exploser. C'est la première moitié d'un raisonnement par l'absurde.

          **Sous l'hypothèse $\ell > 1$.** La suite $\alpha_n$ converge vers $\ell$, et $\ln$ est continue sur $]0, +\infty[$, donc en $\ell$ :

          $$\ln\alpha_n \longrightarrow \ln\ell$$

          Comme $\ell > 1$, on a $\ln\ell > 0$ **strictement**. On peut donc composer une seconde fois par $\ln$, continue en $\ln\ell > 0$ :

          $$\ln\!\left(\ln\alpha_n\right) \longrightarrow \ln\!\left(\ln\ell\right)$$

          **Le quotient.** Le dénominateur tend vers $\ln\ell \neq 0$ : le quotient de deux suites convergentes, dont le dénominateur a une limite non nulle, converge vers le quotient des limites.

          $$\boxed{\ \lim_{n \to +\infty} \frac{\ln\!\left(\ln\alpha_n\right)}{\ln\alpha_n} = \frac{\ln\!\left(\ln\ell\right)}{\ln\ell}\ }$$

          **L'information capitale, à retenir pour la question suivante.** Cette limite est un nombre **fini**. Sa valeur exacte importe peu — ce qui compte, et qui va tout trancher, c'est qu'elle ne soit ni $+\infty$ ni $-\infty$.

          **Là où l'hypothèse est indispensable.** Si $\ell$ valait $1$, alors $\ln\ell = 0$ : le dénominateur tendrait vers $0$ et le double logarithme $\ln(\ln\alpha_n)$ vers $-\infty$. Le quotient deviendrait une forme indéterminée $\frac{-\infty}{0^+}$, et rien de ce qui précède ne s'appliquerait. L'énoncé écarte ce cas par hypothèse **précisément parce que c'est celui qui est vrai** — et la question 4-d va le montrer par élimination.
      - id: q4d
        stem: |-
          **4- d)** (0,5 pt) En déduire la valeur de la limite $\ell$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Deux questions ont préparé deux affirmations sur la **même** quantité. Il ne reste qu'à les confronter — c'est un raisonnement **par l'absurde**, et il se rédige en trois temps : on suppose, on dérive la contradiction, on conclut.

          **Ce que dit la question 4-b, sans aucune hypothèse.** Pour tout $n \geq 2$ :

          $$\frac{\ln\!\left(\ln\alpha_n\right)}{\ln\alpha_n} = -(n-1)$$

          Le membre de droite tend vers $-\infty$ quand $n \to +\infty$, de façon certaine. Donc le quotient **diverge vers $-\infty$** :

          $$\lim_{n \to +\infty} \frac{\ln\!\left(\ln\alpha_n\right)}{\ln\alpha_n} = -\infty$$

          **La confrontation.** Supposons, par l'absurde, que $\ell > 1$. La question 4-c affirme alors que ce même quotient converge vers $\dfrac{\ln(\ln\ell)}{\ln\ell}$, un nombre **fini**. Une suite ne peut pas à la fois diverger vers $-\infty$ et converger vers un réel : **contradiction**.

          **La conclusion.** L'hypothèse $\ell > 1$ est fausse, donc $\ell \leq 1$. Or la question 4-a a établi $\ell \geq 1$. Les deux inégalités ne laissent qu'une possibilité :

          $$\boxed{\ \ell = 1\ }$$

          **Le contrôle de cohérence, qui referme l'exercice.** Avec $\ell = 1$, on a $\ln\alpha_n \to 0^+$, donc $\ln(\ln\alpha_n) \to -\infty$ : le numérateur plonge vers $-\infty$ pendant que le dénominateur tend vers $0^+$. Le quotient tend bien vers $-\infty$, exactement ce que la relation $-(n-1)$ exige. Aucune contradiction ne subsiste, et l'on voit du même coup pourquoi la question 4-c devait exclure ce cas : c'est celui où le calcul direct est indéterminé.

          **La lecture d'ensemble, qui donne son sens à tout l'exercice.** La question 2 avait montré que les courbes s'emboîtent en s'enfonçant vers le bas quand $n$ grandit, tout en restant épinglées au point $(1\,;\,1)$. Plus $n$ croît, plus la courbe descend vite après ce point, et plus son zéro se rapproche de $1$. Le résultat $\ell = 1$ était donc lisible sur le dessin dès la question 2-b ; les questions 3 et 4 n'ont fait que le rendre rigoureux. C'est le contrôle numérique du vérificateur qui l'illustre : $\alpha_2 \approx 1{,}76$, $\alpha_{10} \approx 1{,}21$, $\alpha_{200} \approx 1{,}02$ — la descente vers $1$ est lente mais inexorable.

          **Le piège nommé de cette question.** Conclure $\ell = 1$ directement de la question 4-a, en disant « la suite décroît et est minorée par $1$, donc elle tend vers $1$ ». C'est faux : une suite décroissante minorée par $1$ peut converger vers n'importe quelle valeur supérieure ou égale à $1$. Toute la mécanique des questions 4-b, 4-c et 4-d n'existe que pour écarter ces autres valeurs — sans elle, on n'a rien démontré.
        steps:
          - math: '\frac{\ln(\ln \alpha_n)}{\ln \alpha_n} = -(n-1) \xrightarrow[n \to +\infty]{} -\infty'
            note: "Question 4-b, valable sans aucune hypothese : le membre de droite diverge, donc le quotient aussi."
          - math: '\text{Si } \ell > 1 : \quad \frac{\ln(\ln \alpha_n)}{\ln \alpha_n} \longrightarrow \frac{\ln(\ln \ell)}{\ln \ell} \in \mathbb{R}'
            note: "Question 4-c : sous cette hypothese la meme suite CONVERGE vers un reel fini."
          - math: '-\infty \neq \text{limite finie} \Longrightarrow \ell \leq 1'
            note: "Contradiction : une suite ne peut pas diverger et converger. L'hypothese ell > 1 tombe."
          - math: '1 \leq \ell \quad \text{(question 4-a)} \quad\text{et}\quad \ell \leq 1'
            note: "Les deux encadrements se referment sur une seule valeur."
          - math: '\boxed{\ell = 1}'
            note: "Controle numerique : alpha_2 = 1,76 ; alpha_10 = 1,21 ; alpha_200 = 1,02. La descente est lente mais sure."

  # ── bk-2023-n-x1 — 2023 N, Exercice 1 : g(x)=ln(1+x)/x encadree, f=g.e^-x,
  #    et un point fixe itere u_(n+1) = (1/3)f(u_n) qui converge vers alpha ──
  # Voir NOTE ÉDITORIALE — CARTE DES DOMAINES, SCOPE NOTE ADDITIONNELLE et
  # FLAG DE JUMELAGE en tete de fichier.
  - id: bk-2023-n-x1
    title: "Problème d'analyse : encadrement de ln(1 + x), la fonction f = g·e⁻ˣ, existence d'un point fixe et convergence géométrique d'une suite itérée vers lui"
    source:
      year: 2023
      session: normale
      filiere: "SM"
      exercise_label: "Exercice 1 — Analyse (7,75 points)"
    bareme_total: 7.75
    duration_min: 47
    sourcing:
      status: sourced
      note: >-
        2023 session normale — examen national Mathématiques, filière
        Sciences Mathématiques (A) et (B), option française (BIOF), 4 h,
        coefficient 9, code sujet NS 24F, AlloSchool element/142490
        (course-436/upload-85316), pages 2 et 3 sur 5 (consignes en page 1).
        Barème de l'exercice : 7,75 points, annoncé en page 1 (carte des
        composantes : Ex1 = 7,75 ; Ex2 = 2,25 ; Ex3 = 3,5 ; Ex4 = 3 ; Ex5 =
        3,5 ; somme = 20). Recompte question par question sur l'énoncé
        transcrit : Partie I = 0,5(1-a) + 0,5(1-b) + 0,5(2) = 1,5 ; Partie II
        = 0,5(1) + 0,25(2-a) + 0,25(2-b) + 0,5(2-c) + 0,75(3) + 0,5(4-a) +
        0,25(4-b) + 0,25(5-a) + 0,75(5-b) = 4,0 ; Partie III = 0,5(1) +
        0,5(2-a) + 0,5(2-b) + 0,5(2-c) + 0,25(2-d) = 2,25 ; total 1,5 + 4,0 +
        2,25 = 7,75, conforme à la carte de la page 1. ÉCART SIGNALÉ SUR LE
        COMPTE DE LIGNES (pas sur le total) : ce recompte, fait ligne par
        ligne contre l'énoncé transcrit, dénombre 17 items de barème (3 pour
        la Partie I, 9 pour la Partie II, 5 pour la Partie III) — exactement
        le compte que le fichier source donne lui-même dans sa table « Ce qui
        a été contrôlé, point par point » (« Barèmes de marge, ex. 1 (17
        relevés : 3 + 9 + 5) »). La consigne de conversion annonçait « 13
        barèmes de marge » pour cet exercice ; aucun découpage de l'énoncé
        transcrit ne redonne 13 (3+9+5=17, et aucune autre partition des neuf
        valeurs imprimées de la Partie II ne le fait non plus). Le total,
        lui, est triple confirmé — carte de la page 1, recompte du
        vérificateur adversarial dans le fichier source, et recompte
        indépendant ici — et vaut sans ambiguïté 7,75. Écart de comptage
        rapporté à l'orchestrateur ; n'affecte ni bareme_total ni aucune
        valeur transcrite. Transcription vérifiée par une passe adversariale
        indépendante (2026-08-27, docs/sujets/_incoming/maths-sm-2023-n.md,
        statut « vérifié ») : re-fetch de element/142490, extraction des cinq
        images depuis le HTML (course-436/upload-85316/0001 à 0005-big.jpg,
        1240×1754), lecture des pages 2 et 3 SUR L'IMAGE — jamais sur le
        texte transcrit —, recadrages ×4 à ×20 sur chaque point litigieux
        (glyphes cassés, exposant de R+, colonne des barèmes), et
        re-dérivation mathématique complète de chaque « montrer que » de
        l'énoncé. Résultat de la passe, à retenir : AUCUN défaut trouvé dans
        le corps de l'exercice — la seule correction, hors énoncé, portait
        sur le compte des glyphes cassés lus ≤ (cinq annoncés par une
        première lecture, six recomptés ensuite ; l'énumération, elle, était
        déjà juste). NOTE DE LECTURE (mojibake, portée sur CET exercice) :
        deux des six occurrences du glyphe cassé « ,, » lu ≤ tombent ici
        (Partie III, q2-b et q2-c) — glyphe adjugé au pixel (double virgule
        basse « „ », posée sur la ligne de base, sans valeur informative,
        nettement distincte du point-virgule imprimé sur la même ligne),
        lecture forcée par l'égalité au rang n=0 de la q2-c : $|u_0-\alpha| =
        |\beta-\alpha| = \frac{1}{2^0}|\beta-\alpha|$ est une égalité, qu'un
        $<$ strict rendrait fausse. L'autre glyphe cassé concerné est
        « $\beta \in$ | |$^{+}$ » (Partie III, tête de q2), confirmé
        $\mathbb{R}^{+}$ (exposant « + » seul au zoom ×20, sans astérisque —
        contrôle de robustesse : l'astérisque, lui, s'imprime correctement
        ailleurs dans le même scan) et non $\mathbb{R}^{+*}$ : $\beta = 0$
        est donc un cas admis, cohérent avec la question III-2-a qui demande
        $u_n \geq 0$ et non $u_n > 0$. Aucune valeur numérique n'est touchée
        par ces deux familles de glyphes cassés. Aucune figure n'est imprimée
        dans ce sujet : la question 5-b de la Partie II demande au candidat
        de CONSTRUIRE la courbe, sans lui en fournir aucune — absence
        confirmée par le vérificateur sur les cinq pages du scan, pas
        supposée. CONTRE-CALCUL INDÉPENDANT, refait pour cette conversion et
        non recopié du fichier source : chaque encadrement, identité,
        dérivée, limite et étude de signe des 17 questions a été redérivé
        séparément (voir le `reasoning` de chaque question) ; aucun écart
        trouvé avec la re-dérivation du vérificateur adversarial — en
        particulier I-1a/I-1b (l'encadrement s'intègre exactement), II-2b/2c
        (l'identité télescope, $f'_d(0)=-\frac32$), II-3 (l'expression de
        $f'$ retrouvée à l'identique), II-4a (les deux écarts valent
        $x^2(2x+3)$ et $x^3$, tous deux strictement positifs pour $x>0$), et
        III-1/2 (existence-unicité de $\alpha$ par TVI + stricte monotonie,
        puis majoration géométrique par le TAF). FILIÈRE — POINT CRITIQUE :
        `source.filiere` doit valoir « SM », impérativement. Les identifiants
        `bk-2023-n-x1` et `bk-2023-n-x2` existent déjà dans le corpus, mais
        portés par l'épreuve SExp 2023 normale (code NS 22F,
        element/137482) sous content/maths/geometrie-espace/bank.yaml et
        content/maths/nombres-complexes-1/bank.yaml. Ce n'est pas un
        conflit : les identifiants ne sont uniques qu'à l'intérieur d'un
        `bank.yaml`, et web/src/lib/examens.ts (l. 81) groupe les épreuves
        sur `source.{filiere, year, session}`, pas sur l'id seul — mais
        c'est bien `filiere` qui sépare les deux épreuves de 2023 normale
        dans cet assemblage ; le renseigner faux fusionnerait deux épreuves
        distinctes en une, et le total sauterait au-dessus de 20. AJOUT AU
        CORPUS : avec l'ajout jumeau de bk-2023-n-x2 (Exercice 2) dans
        content/maths/calcul-integral/bank.yaml, cette conversion fait
        passer l'épreuve SM 2023 normale de 10,00/20 (les trois exercices
        d'algèbre — nombres complexes, arithmétique, structures algébriques
        — déjà en banque) à 20,00/20, complète, dans Examens blancs.
    intro: |-
      **Problème d'analyse (examen national, Sciences Mathématiques, session normale 2023 — Exercice 1, 7,75 points).**

      *L'épreuve dure 4 heures et comporte cinq exercices indépendants, traitables dans l'ordre choisi par le candidat. Cet exercice à lui seul pèse près de 40 % du barème. L'usage de la calculatrice n'est pas autorisé.*

      **Partie I.** On encadre $\dfrac{1}{1+t}$ entre deux expressions simples, puis on intègre cet encadrement sur $[0,x]$ pour obtenir un encadrement de $\ln(1+x)$ — dont on tire une première limite.

      **Partie II.** On construit $f(x) = g(x)\,e^{-x}$ à partir de $g(x) = \dfrac{\ln(1+x)}{x}$, avec $f(0)=1$ posé par prolongement, et on étudie $f$ en entier sur $[0,+\infty[$ : limite en $+\infty$, continuité et dérivabilité à droite en $0$, dérivée sur $]0,+\infty[$, encadrement de cette dérivée, tableau de variations, courbe $(C)$.

      **Partie III.** On montre que l'équation $f(x)=3x$ admet une unique solution $\alpha$ dans $]0,+\infty[$, puis on construit une suite $(u_n)$ par $u_0=\beta \in \mathbb{R}^{+}$ et $u_{n+1} = \dfrac13 f(u_n)$, et on démontre qu'elle converge vers $\alpha$ — avec une vitesse de convergence explicite, géométrique de raison $\dfrac12$.

      *Aucune figure n'accompagne ce sujet : la question 5-b de la Partie II demande au candidat de construire la courbe lui-même, sans lui en fournir aucune. La carte des domaines, pour savoir quoi réviser avant : cette leçon (fonction-logarithme) porte l'ossature — les encadrements, la propriété du logarithme, la limite de référence $\dfrac{\ln x}{x}\to 0$ — mais Partie I-2 et Partie II-1/2-a empruntent aux limites et à la continuité, Partie II-2-c à II-5 à la dérivabilité et à l'étude de fonctions (dont le facteur $e^{-x}$ mobilise aussi la fonction exponentielle), Partie III-1 au théorème des valeurs intermédiaires (dérivabilité), et Partie III-2 aux suites numériques — avec, pour la seule majoration géométrique de la question 2-b, le théorème des accroissements finis, énoncé et appliqué au moment précis où il sert, jamais supposé acquis. Le fil qui traverse tout le problème : chaque limite délicate se résout en encadrant d'abord, jamais en devinant.*
    questions:
      - id: q1a
        part: "Partie I — 1. Encadrer 1/(1+t), puis ln(1 + x)"
        stem: |-
          **1- a)** (0,5 pt) Montrer que : $\forall t \in [0, +\infty[\ ;\quad \dfrac{4}{(2+t)^2} \le \dfrac{1}{1+t} \le \dfrac{1}{2}\left(1 + \dfrac{1}{(1+t)^2}\right)$
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Deux inégalités à établir, et elles ne demandent ni dérivée ni étude de fonction : les trois membres sont des fractions, donc le réflexe est de comparer par équivalence, en multipliant par des quantités qu'on sait positives. C'est la question la plus « gratuite » de tout le problème — mais elle porte tout le reste, comme on le verra dès la question suivante.

          **L'inégalité de gauche.** Pour $t \geq 0$, les trois quantités $(2+t)^2$, $1+t$ et leur produit sont strictement positives : multiplier les deux membres par $(2+t)^2(1+t)$ conserve le sens de l'inégalité.

          $$\frac{4}{(2+t)^2} \le \frac{1}{1+t} \iff 4(1+t) \le (2+t)^2$$

          Développe le membre de droite : $(2+t)^2 = 4+4t+t^2$. L'inégalité devient $4+4t \le 4+4t+t^2$, c'est-à-dire $0 \le t^2$ — vraie pour tout réel $t$, en particulier sur $[0,+\infty[$, avec égalité seulement en $t=0$.

          **L'inégalité de droite, avec un changement de regard.** Pose $u = \dfrac{1}{1+t}$. Comme $t \in [0,+\infty[$, on a $1+t \in [1,+\infty[$, donc $u \in\ ]0,1]$. L'inégalité à montrer devient, en remplaçant :

          $$u \le \frac12\left(1+u^2\right) \iff 2u \le 1+u^2 \iff 0 \le u^2-2u+1 \iff 0 \le (1-u)^2$$

          — vraie pour tout réel $u$, donc en particulier sur $]0,1]$, avec égalité seulement en $u=1$, c'est-à-dire $t=0$.

          **Conclusion.** Les deux inégalités tiennent sur $[0,+\infty[$ tout entier, chacune devenant une égalité exactement en $t=0$ — et nulle part ailleurs. Retiens ce détail : il va resservir en Partie II pour justifier une borne « atteinte », pas seulement approchée.

          **Le piège nommé de cette question.** Vouloir dériver une fonction $t \mapsto \dfrac{1}{1+t} - \dfrac{4}{(2+t)^2}$ pour en étudier le signe. C'est possible, mais beaucoup plus long qu'une simple mise au même dénominateur ou qu'un changement de variable bien choisi — et sur une question à $0{,}5$ point, le temps gagné ici finance les questions plus lourdes de la Partie II.
        steps:
          - math: '\dfrac{4}{(2+t)^2} \le \dfrac{1}{1+t} \iff 4(1+t) \le (2+t)^2 \iff 0 \le t^2'
            note: "Multiplication par (2+t) au carre fois (1+t), toutes deux strictement positives : le sens de l'inegalite est conserve."
          - math: 'u = \dfrac{1}{1+t} \in\ ]0,1] \quad\text{et}\quad \dfrac{1}{1+t} \le \dfrac12\left(1+\dfrac{1}{(1+t)^2}\right) \iff 0 \le (1-u)^2'
            note: "Changement de variable u = 1/(1+t) : l'inegalite se ramene a un carre, toujours positif ou nul."
          - math: '\forall t \in [0,+\infty[\ ;\quad \dfrac{4}{(2+t)^2} \le \dfrac{1}{1+t} \le \dfrac12\left(1+\dfrac{1}{(1+t)^2}\right)'
            note: "Les deux inegalites tiennent sur tout [0, plus infini[, egalite aux deux bouts seulement en t = 0."
      - id: q1b
        stem: |-
          **1- b)** (0,5 pt) En déduire que : $\forall x \in [0, +\infty[\ ;\quad \dfrac{2x}{2+x} \le \ln(1+x) \le \dfrac{1}{2}\left(\dfrac{x^2+2x}{1+x}\right)$
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** « En déduire » interdit de repartir de zéro : il faut **intégrer** l'encadrement de la question précédente. Le signal est dans la forme même du résultat demandé — $\ln(1+x)$ apparaît, et $\ln(1+x) = \displaystyle\int_0^x \dfrac{dt}{1+t}$ est exactement le terme central de la question 1-a, intégré entre $0$ et $x$.

          **La condition à vérifier avant d'intégrer.** Les bornes doivent être dans l'ordre : ici $0 \le x$ pour $x \in [0,+\infty[$, ce qui est acquis. On peut donc intégrer les trois membres de l'encadrement de 1-a sur $[0,x]$, terme à terme.

          **Le terme de gauche.** Une primitive de $\dfrac{4}{(2+t)^2}$ est $-\dfrac{4}{2+t}$ :

          $$\int_0^x \frac{4}{(2+t)^2}\,dt = \left[-\frac{4}{2+t}\right]_0^x = -\frac{4}{2+x} + 2 = \frac{2(2+x)-4}{2+x} = \frac{2x}{2+x}$$

          **Le terme du milieu.** C'est la définition même de $\ln$ :

          $$\int_0^x \frac{dt}{1+t} = \bigl[\ln(1+t)\bigr]_0^x = \ln(1+x) - \ln(1) = \ln(1+x)$$

          **Le terme de droite.** On sépare la somme :

          $$\int_0^x \frac12\left(1+\frac{1}{(1+t)^2}\right)dt = \frac12\left[t - \frac{1}{1+t}\right]_0^x = \frac12\left(x - \frac{1}{1+x} + 1\right) = \frac12 \cdot \frac{x(1+x) + (1+x) - 1}{1+x} = \frac12\cdot\frac{x^2+2x}{1+x}$$

          **Conclusion.** L'intégration de l'encadrement de 1-a donne exactement l'encadrement demandé :

          $$\boxed{\forall x \in [0,+\infty[\ ;\quad \frac{2x}{2+x} \le \ln(1+x) \le \frac12\cdot\frac{x^2+2x}{1+x}}$$

          **Le contrôle qui rassure.** En $x=0$ : les trois membres valent $0$ — cohérent, puisque l'intégrale sur $[0,0]$ est nulle. Et l'encadrement de 1-a était taillé pour produire, une fois intégré, exactement cette forme-ci — c'est pour cela qu'il fallait la démontrer sous cette forme précise et pas une autre.

          **Le piège nommé de cette question.** Refaire l'étude de signe d'une différence, en oubliant que l'énoncé vient de fournir, une question plus tôt, exactement l'outil à intégrer. « En déduire » n'est jamais une invitation à repartir de zéro.
        steps:
          - math: '\int_0^x \frac{4}{(2+t)^2}\,dt = \left[-\frac{4}{2+t}\right]_0^x = \frac{2x}{2+x}'
            note: "Primitive de 4/(2+t) au carre : -4/(2+t). Evaluee entre 0 et x."
          - math: '\int_0^x \frac{dt}{1+t} = \bigl[\ln(1+t)\bigr]_0^x = \ln(1+x)'
            note: "Definition meme du logarithme comme primitive de 1/(1+t) qui s'annule en 0."
          - math: '\int_0^x \frac12\left(1+\frac{1}{(1+t)^2}\right)dt = \frac12\left[t-\frac{1}{1+t}\right]_0^x = \frac12\cdot\frac{x^2+2x}{1+x}'
            note: "On integre la somme terme a terme, puis on met au meme denominateur."
          - math: '\forall x \in [0,+\infty[\ ;\quad \frac{2x}{2+x} \le \ln(1+x) \le \frac12\cdot\frac{x^2+2x}{1+x}'
            note: "Integration de l'encadrement de 1-a entre 0 et x, bornes dans l'ordre 0 <= x."
      - id: q2
        part: "Partie I — 2. Une premiere limite, tiree de l'encadrement"
        stem: |-
          **2)** (0,5 pt) Soit $g$ la fonction numérique de la variable réelle $x$ définie sur $]0, +\infty[$ par $g(x) = \dfrac{\ln(1+x)}{x}$. Montrer que : $\displaystyle\lim_{\substack{x \to 0 \\ x > 0}} \frac{g(x) - 1}{x} = \frac{-1}{2}$
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Regarde ce qui se passe si tu essaies de calculer cette limite directement : $g(x) \to 1$ quand $x \to 0^+$ (limite classique de $\ln(1+x)/x$), donc $g(x)-1 \to 0$, et le dénominateur $x \to 0$ aussi. C'est une **forme indéterminée $\frac{0}{0}$** — exactement le genre de limite que l'encadrement de la question 1-b a été construit pour résoudre, sans jamais poser de dérivée.

          **On divise l'encadrement de 1-b par $x$**, strictement positif ici, ce qui conserve le sens des inégalités :

          $$\frac{2x}{2+x} \le \ln(1+x) \le \frac12\cdot\frac{x^2+2x}{1+x}
          \quad\Longrightarrow\quad
          \frac{2}{2+x} \le \frac{\ln(1+x)}{x} \le \frac12\cdot\frac{x+2}{1+x}$$

          c'est-à-dire $\dfrac{2}{2+x} \le g(x) \le \dfrac12\cdot\dfrac{x+2}{1+x}$.

          **On soustrait $1$ à chaque membre**, pour faire apparaître $g(x)-1$ :

          $$\frac{2}{2+x}-1 = \frac{2-(2+x)}{2+x} = \frac{-x}{2+x}
          \qquad\text{et}\qquad
          \frac12\cdot\frac{x+2}{1+x}-1 = \frac{(x+2)-2(1+x)}{2(1+x)} = \frac{-x}{2(1+x)}$$

          donc $\dfrac{-x}{2+x} \le g(x)-1 \le \dfrac{-x}{2(1+x)}$.

          **On divise par $x>0$** une dernière fois, ce qui conserve toujours le sens :

          $$\boxed{-\frac{1}{2+x} \le \frac{g(x)-1}{x} \le -\frac{1}{2(1+x)}}$$

          **Le passage à la limite.** Quand $x \to 0^+$ : la borne de gauche $-\dfrac{1}{2+x} \to -\dfrac12$, et la borne de droite $-\dfrac{1}{2(1+x)} \to -\dfrac12$. Les deux bornes convergent vers la **même** valeur : le théorème des gendarmes s'applique et force le milieu à converger aussi :

          $$\lim_{\substack{x \to 0\\ x>0}} \frac{g(x)-1}{x} = -\frac12$$

          **Ce qui est réellement établi, et qu'il faut garder pour la Partie II.** Ce quotient est le taux d'accroissement de $g$ entre $0$ et $x$, **si l'on prolonge $g$ par $g(0)=1$** (ce que sa limite en $0$ autorise). Cette limite dit donc que $g$, ainsi prolongée, serait dérivable en $0$ avec un « nombre dérivé » de $-\dfrac12$ — un résultat qui va resservir tel quel dans la question II-2-c.

          **Le piège nommé de cette question.** Essayer une limite « à vue » du type $\ln(1+x) \approx x - \frac{x^2}{2}$ (développement limité). C'est correct mathématiquement mais **hors programme de 2ᵉ Bac** et hors de ce que le barème attend : la voie imposée par l'énoncé — encadrer, diviser, gendarmes — ne demande aucun outil au-delà du cours, et c'est elle qui rapporte les points.
        steps:
          - math: '\frac{2x}{2+x} \le \ln(1+x) \le \frac12\cdot\frac{x^2+2x}{1+x} \ \Big/\ x \ \Longrightarrow\ \frac{2}{2+x} \le g(x) \le \frac12\cdot\frac{x+2}{1+x}'
            note: "Encadrement de la question 1-b divise par x, positif : le sens des inegalites est conserve."
          - math: '\frac{2}{2+x}-1 = \frac{-x}{2+x} \qquad\text{et}\qquad \frac12\cdot\frac{x+2}{1+x}-1 = \frac{-x}{2(1+x)}'
            note: "On soustrait 1 aux deux bornes pour faire apparaitre g(x) moins 1."
          - math: '-\frac{1}{2+x} \le \frac{g(x)-1}{x} \le -\frac{1}{2(1+x)}'
            note: "Division par x, positif, une derniere fois."
          - math: '\lim_{x\to 0^+} -\frac{1}{2+x} = -\frac12 \quad\text{et}\quad \lim_{x\to 0^+} -\frac{1}{2(1+x)} = -\frac12 \ \Longrightarrow\ \lim_{x\to 0^+}\frac{g(x)-1}{x} = -\frac12'
            note: "Les deux bornes convergent vers la meme valeur : theoreme des gendarmes."
      - id: q3
        part: "Partie II — 1. La fonction f = g fois e puissance moins x, limite en +infini"
        stem: |-
          **1)** (0,5 pt) Soit $f$ la fonction numérique de la variable réelle $x$ définie sur $[0, +\infty[$ par $f(0) = 1$ et $\forall x \in\, ]0, +\infty[\ ;\ f(x) = g(x)\,e^{-x}$. On note $(C)$ sa courbe représentative. Calculer $\displaystyle\lim_{x \to +\infty} f(x)$ puis interpréter graphiquement le résultat obtenu.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** $f$ est un produit de deux facteurs, $g(x)$ et $e^{-x}$ : le réflexe est de regarder la limite de chacun séparément avant de les combiner.

          **La limite de $g(x)$ en $+\infty$.** Pose $t = 1+x$ : quand $x \to +\infty$, $t \to +\infty$ aussi, et

          $$g(x) = \frac{\ln(1+x)}{x} = \frac{\ln t}{t-1} = \frac{\ln t}{t}\cdot\frac{t}{t-1}$$

          Le premier facteur, $\dfrac{\ln t}{t}$, tend vers $0$ : c'est **la** limite de référence du chapitre (R4 de cette leçon), celle qui dit que $\ln$ grossit indéfiniment mais infiniment plus lentement que $t$. Le second facteur, $\dfrac{t}{t-1}$, tend vers $1$. Le produit tend donc vers $0 \times 1 = 0$ :

          $$\lim_{x\to+\infty} g(x) = 0$$

          **La limite de $e^{-x}$ en $+\infty$.** C'est une limite de référence de la fonction exponentielle (domaine « fonction-exponentielle », hors du corps de cette leçon) : $e^{-x} = \dfrac{1}{e^x}$, et $e^x \to +\infty$, donc $e^{-x} \to 0$.

          **Le produit.** Les deux facteurs tendent tous deux vers $0$ : ce n'est **pas** une forme indéterminée — $0 \times 0$ se calcule directement, sans détour, contrairement à $0 \times \infty$.

          $$\boxed{\lim_{x\to+\infty} f(x) = 0 \times 0 = 0}$$

          **L'interprétation graphique.** $f(x)$ tend vers une constante finie ($0$) quand $x \to +\infty$ : la courbe $(C)$ admet la droite d'équation $y=0$, c'est-à-dire **l'axe des abscisses**, comme **asymptote horizontale** au voisinage de $+\infty$.

          **Le piège nommé de cette question.** Écrire « $0 \times 0$, forme indéterminée » par réflexe, en confondant avec $0\times\infty$. Un produit de deux facteurs qui tendent chacun vers $0$ tend vers $0$ — il n'y a ici strictement aucune indétermination, et vouloir en chercher une fait perdre du temps sur une question à $0{,}5$ point.
        steps:
          - math: 'g(x) = \frac{\ln(1+x)}{x} = \frac{\ln t}{t}\cdot\frac{t}{t-1} \qquad (t = 1+x,\ t\to+\infty)'
            note: "Changement de variable t = 1+x pour retomber sur la limite de reference ln(t)/t."
          - math: '\lim_{t\to+\infty}\frac{\ln t}{t} = 0 \ (\text{R4 de cette lecon}), \qquad \lim_{t\to+\infty}\frac{t}{t-1}=1 \ \Longrightarrow\ \lim_{x\to+\infty} g(x) = 0'
            note: "Limite de reference du chapitre, pas un emprunt : R4 de fonction-logarithme."
          - math: '\lim_{x\to+\infty} e^{-x} = 0 \quad (\text{fonction exponentielle})'
            note: "e puissance moins x est l'inverse de e puissance x, qui tend vers plus l'infini."
          - math: '\lim_{x\to+\infty} f(x) = \lim_{x\to+\infty} g(x)\,e^{-x} = 0\times 0 = 0 \quad (\text{aucune indetermination})'
            note: "Produit de deux facteurs tendant chacun vers 0 : resultat direct, pas de forme 0 fois l'infini."
          - math: '(C) \text{ admet la droite } y=0 \text{ pour asymptote horizontale en } +\infty'
            note: "Limite finie (0) en plus l'infini : lecture graphique directe."
      - id: q4a
        part: "Partie II — 2. Continuite et derivabilite a droite en 0"
        stem: |-
          **2- a)** (0,25 pt) Montrer que $f$ est continue à droite en $0$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Continue à droite en $0$ signifie $\displaystyle\lim_{x\to 0^+} f(x) = f(0) = 1$. On dispose déjà, de la question I-1-b (divisée par $x$ comme en I-2), d'un encadrement de $g(x)$ valable pour tout $x>0$ : $\dfrac{2}{2+x} \le g(x) \le \dfrac12\cdot\dfrac{x+2}{1+x}$.

          **La limite de $g$ en $0^+$, par gendarmes.** Quand $x\to 0^+$ : la borne de gauche $\dfrac{2}{2+x}\to \dfrac22=1$, la borne de droite $\dfrac12\cdot\dfrac{x+2}{1+x}\to \dfrac12\cdot 2 = 1$. Les deux bornes convergent vers $1$ :

          $$\lim_{x\to 0^+} g(x) = 1$$

          **La limite de $e^{-x}$ en $0^+$.** $\exp$ est continue en $0$ (propriété de la fonction exponentielle), donc $e^{-x} \to e^0 = 1$.

          **Le produit.** Deux facteurs convergents, produit des limites, aucune indétermination :

          $$\lim_{x\to 0^+} f(x) = \lim_{x\to 0^+} g(x)\cdot e^{-x} = 1\times 1 = 1 = f(0)$$

          $f$ est donc continue à droite en $0$.

          **Une seconde route, plus rapide, qui réutilise la question I-2.** La question I-2 a montré que $\dfrac{g(x)-1}{x} \to -\dfrac12$, une limite **finie**. Donc $g(x)-1 = x \cdot \dfrac{g(x)-1}{x} \to 0 \times \left(-\dfrac12\right) = 0$, c'est-à-dire $g(x)\to 1$ directement — sans repasser par l'encadrement. Les deux voies sont valables ; la seconde montre que rien, dans ce problème, n'est isolé.

          **Le piège nommé de cette question.** Invoquer « $g$ est continue comme quotient de fonctions continues » sans remarquer que $g$ n'est **pas définie en $0$** : son domaine est $]0,+\infty[$. Ce qu'on montre ici n'est pas la continuité de $g$ en un point de son domaine, mais l'existence d'une limite qui permet de **prolonger** $f$ par continuité — c'est tout le sens du $f(0)=1$ posé par l'énoncé.
        steps:
          - math: '\frac{2}{2+x} \le g(x) \le \frac12\cdot\frac{x+2}{1+x} \ \xrightarrow[x\to 0^+]{} \ 1 \le \lim g(x) \le 1 \ \Longrightarrow\ \lim_{x\to 0^+} g(x) = 1'
            note: "Meme encadrement qu'a la question I-2, gendarmes en 0."
          - math: '\lim_{x\to 0^+} e^{-x} = e^{0} = 1 \quad (\text{continuite de exp en 0})'
            note: "Fonction exponentielle continue partout, en particulier en 0."
          - math: '\lim_{x\to 0^+} f(x) = 1\times 1 = 1 = f(0) \ \Longrightarrow\ f \text{ continue a droite en } 0'
            note: "Produit de deux limites finies, aucune indetermination."
      - id: q4b
        stem: |-
          **2- b)** (0,25 pt) Vérifier que : $\forall x \in\, ]0, +\infty[\ ;\quad \dfrac{f(x)-1}{x} = \left(\dfrac{e^{-x}-1}{x}\right)g(x) + \left(\dfrac{g(x)-1}{x}\right)$
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Une identité algébrique pure, sans limite ni inégalité : elle sert à **préparer** la question 2-c, en cassant le taux d'accroissement de $f$ en deux morceaux dont chacun a déjà une limite connue.

          **Le développement du membre de droite.** Mets tout au même dénominateur $x$ :

          $$\left(\frac{e^{-x}-1}{x}\right)g(x) + \frac{g(x)-1}{x} = \frac{(e^{-x}-1)g(x) + g(x)-1}{x}$$

          **Le développement du numérateur.** Distribue $(e^{-x}-1)g(x) = e^{-x}g(x) - g(x)$ :

          $$(e^{-x}-1)g(x) + g(x) - 1 = e^{-x}g(x) - g(x) + g(x) - 1 = e^{-x}g(x) - 1$$

          Le terme $-g(x)$ et le terme $+g(x)$ s'annulent exactement — c'est un **télescopage**, et c'est le cœur de l'identité.

          **La conclusion.** Il reste $e^{-x}g(x) - 1$, et par définition $f(x) = g(x)e^{-x}$ pour $x>0$ :

          $$\frac{(e^{-x}-1)g(x) + g(x)-1}{x} = \frac{e^{-x}g(x) - 1}{x} = \frac{f(x)-1}{x}$$

          $$\boxed{\forall x \in\, ]0,+\infty[\ ;\quad \frac{f(x)-1}{x} = \left(\frac{e^{-x}-1}{x}\right)g(x) + \frac{g(x)-1}{x}}$$

          **Pourquoi cette décomposition précisément.** Le taux d'accroissement de $f$ en $0$ n'est calculable ni par une formule directe (pas de règle de dérivation d'un produit-composé en un point où l'un des facteurs n'est défini que par prolongement), ni par un DL hors-programme. La décomposer en deux morceaux, dont chacun est **une limite déjà établie** ailleurs dans le problème (l'un une limite de référence de l'exponentielle, l'autre le résultat de la question I-2), est exactement ce qui rend la question 2-c immédiate.

          **Le piège nommé de cette question.** Partir du membre de gauche $\dfrac{f(x)-1}{x}$ et essayer de le « scinder » sans passer par le développement du membre de droite : c'est plus risqué, parce que rien n'indique a priori **où** couper. Partir du membre le plus compliqué (ici la somme de droite) et le simplifier est presque toujours plus sûr que l'inverse.
        steps:
          - math: '\left(\frac{e^{-x}-1}{x}\right)g(x) + \frac{g(x)-1}{x} = \frac{(e^{-x}-1)g(x) + g(x) - 1}{x}'
            note: "Meme denominateur x pour les deux termes."
          - math: '(e^{-x}-1)g(x) + g(x) - 1 = e^{-x}g(x) - g(x) + g(x) - 1 = e^{-x}g(x) - 1'
            note: "Telescopage : le moins g(x) et le plus g(x) s'annulent."
          - math: '\frac{e^{-x}g(x)-1}{x} = \frac{f(x)-1}{x} \quad (\text{car } f(x)=g(x)e^{-x} \text{ pour } x>0)'
            note: "On reconnait f(x) dans e puissance moins x fois g(x), par definition de f."
      - id: q4c
        stem: |-
          **2- c)** (0,5 pt) En déduire que $f$ est dérivable à droite en $0$ et déterminer $f'_d(0)$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** $f$ dérivable à droite en $0$ signifie que $\displaystyle\lim_{x\to 0^+} \dfrac{f(x)-f(0)}{x} = \lim_{x\to 0^+}\dfrac{f(x)-1}{x}$ existe et est finie. Grâce à l'identité de la question 2-b, il suffit de calculer la limite de **chaque morceau** de la somme.

          **Le premier morceau : $\dfrac{e^{-x}-1}{x}$.** Quand $x\to 0$, $e^{-x}-1 \to 0$ et $x\to 0$ : c'est une **forme indéterminée $\frac00$**, résolue par une limite de référence de la fonction exponentielle. En posant $h=-x$ :

          $$\frac{e^{-x}-1}{x} = \frac{e^h-1}{-h} = -\frac{e^h-1}{h} \xrightarrow[h\to 0]{} -1$$

          car $\dfrac{e^h-1}{h}\to 1$ quand $h\to 0$ (limite de référence, domaine fonction-exponentielle). Donc $\dfrac{e^{-x}-1}{x} \to -1$.

          **Le second morceau : $g(x)$.** Établi à la question 2-a : $g(x) \to 1$ quand $x\to 0^+$.

          **Le troisième morceau : $\dfrac{g(x)-1}{x}$.** C'est **exactement** le résultat de la question I-2 : $\to -\dfrac12$.

          **On assemble, par produit puis somme de limites finies :**

          $$\lim_{x\to 0^+} \frac{f(x)-1}{x} = \left(\lim \frac{e^{-x}-1}{x}\right)\cdot\left(\lim g(x)\right) + \lim\frac{g(x)-1}{x} = (-1)\times 1 + \left(-\frac12\right) = -\frac32$$

          Cette limite existe et est finie : $f$ est donc **dérivable à droite en $0$**, et

          $$\boxed{f'_d(0) = -\frac32}$$

          **La cohérence à vérifier, et qui vaut d'être notée.** Cette valeur, $-\dfrac32$, va réapparaître comme la borne **atteinte** de l'encadrement de $f'$ établi à la question 4-b : ce n'est pas une coïncidence, c'est le même objet regardé depuis deux questions différentes — le nombre dérivé en $0$ prolonge continûment la dérivée sur $]0,+\infty[$.

          **Le piège nommé de cette question.** Oublier qu'un des trois facteurs de l'identité de 2-b est un **produit** de limites ($\frac{e^{-x}-1}{x}$ fois $g(x)$) et l'autre une **somme** — mélanger les deux opérations dans le calcul final change le résultat. Relis l'identité de 2-b terme à terme avant d'assembler.
        steps:
          - math: '\frac{e^{-x}-1}{x} \xrightarrow[x\to 0]{} -1 \quad (\text{forme } \tfrac00,\ \text{limite de reference } \tfrac{e^h-1}{h}\to 1)'
            note: "Changement de variable h = -x, ramene a la limite de reference de exp en 0."
          - math: '\lim_{x\to 0^+} g(x) = 1 \ (\text{question 2-a}), \qquad \lim_{x\to 0^+}\frac{g(x)-1}{x} = -\frac12 \ (\text{question I-2})'
            note: "Deux limites deja etablies plus haut dans le probleme, reutilisees telles quelles."
          - math: '\lim_{x\to 0^+}\frac{f(x)-1}{x} = (-1)\times 1 + \left(-\frac12\right) = -\frac32'
            note: "Identite de la question 2-b : produit des deux premieres limites, plus la troisieme."
          - math: '\boxed{f \text{ derivable a droite en } 0, \quad f''_d(0) = -\frac32}'
            note: "Limite finie du taux d'accroissement : la derivabilite a droite est acquise, avec ce nombre derive."
      - id: q5
        part: "Partie III — 3. Derivabilite sur ]0, plus infini[ et formule de f'"
        stem: |-
          **3)** (0,75 pt) Montrer que $f$ est dérivable sur $]0, +\infty[$ puis que : $\forall x \in\, ]0, +\infty[\ ;\quad f'(x) = \dfrac{x - (1+x)^2\ln(1+x)}{x^2(1+x)}\,e^{-x}$
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Trois quarts de point pour deux tâches : justifier la dérivabilité, puis dériver et **factoriser jusqu'à la forme imprimée** — cette dernière étape n'est pas cosmétique, c'est elle qui rendra l'étude de signe de la question 4-a possible.

          **La dérivabilité, en une phrase.** Sur $]0,+\infty[$ : $g(x) = \dfrac{\ln(1+x)}{x}$ est un quotient de deux fonctions dérivables (numérateur $\ln(1+x)$, dérivable car $1+x>0$ ; dénominateur $x$, dérivable, non nul), donc $g$ y est dérivable ; $x \mapsto e^{-x}$ est dérivable sur $\mathbb{R}$. $f = g \cdot e^{-x}$ est le **produit** de deux fonctions dérivables sur $]0,+\infty[$ : elle y est donc dérivable.

          **La dérivée de $g$, d'abord.** Avec $u=\ln(1+x)$, $u'=\dfrac{1}{1+x}$, et $v=x$, $v'=1$ :

          $$g'(x) = \frac{u'v-uv'}{v^2} = \dfrac{\dfrac{x}{1+x} - \ln(1+x)}{x^2}$$

          **La dérivée de $f = g\cdot e^{-x}$, par la règle du produit.**

          $$f'(x) = g'(x)e^{-x} + g(x)\cdot(-e^{-x}) = e^{-x}\bigl(g'(x)-g(x)\bigr)$$

          **Le calcul de $g'(x)-g(x)$, sur le même dénominateur $x^2$.**

          $$g'(x) - g(x) = \frac{\dfrac{x}{1+x}-\ln(1+x)}{x^2} - \frac{\ln(1+x)}{x} = \frac{\dfrac{x}{1+x}-\ln(1+x) - x\ln(1+x)}{x^2}$$

          Regroupe les deux termes en $\ln(1+x)$ : $-\ln(1+x) - x\ln(1+x) = -(1+x)\ln(1+x)$ :

          $$g'(x)-g(x) = \frac{\dfrac{x}{1+x} - (1+x)\ln(1+x)}{x^2}$$

          **La mise au même dénominateur final**, en multipliant le numérateur et le dénominateur par $(1+x)$ :

          $$g'(x)-g(x) = \frac{x - (1+x)^2\ln(1+x)}{x^2(1+x)}$$

          **La conclusion.**

          $$\boxed{f'(x) = e^{-x}\bigl(g'(x)-g(x)\bigr) = \frac{x-(1+x)^2\ln(1+x)}{x^2(1+x)}\,e^{-x}}$$

          C'est exactement l'expression imprimée par l'énoncé.

          **Le piège nommé de cette question.** Oublier le signe moins dans $f'=g'e^{-x} - g\,e^{-x}$ (dérivée de $e^{-x}$, qui vaut $-e^{-x}$ et non $e^{-x}$) — l'erreur la plus fréquente sur une dérivée de produit avec exponentielle décroissante. Le second piège : s'arrêter à la somme $g'(x)e^{-x}-g(x)e^{-x}$ sans la factoriser par $e^{-x}$ puis réduire au même dénominateur — la forme non réduite est juste, mais ce n'est pas celle que demande l'énoncé, et une question « montrer que » exige la forme imposée.
        steps:
          - math: 'g''(x) = \frac{\frac{x}{1+x}-\ln(1+x)}{x^2} \qquad (\text{quotient}, u=\ln(1+x),\ v=x)'
            note: "Regle du quotient appliquee a g = ln(1+x) sur x."
          - math: 'f''(x) = g''(x)e^{-x} + g(x)\left(-e^{-x}\right) = e^{-x}\bigl(g''(x)-g(x)\bigr)'
            note: "Regle du produit pour f = g fois e puissance moins x ; derivee de e puissance moins x = moins e puissance moins x."
          - math: 'g''(x)-g(x) = \frac{\frac{x}{1+x}-\ln(1+x)}{x^2} - \frac{\ln(1+x)}{x} = \frac{\frac{x}{1+x}-(1+x)\ln(1+x)}{x^2}'
            note: "Meme denominateur x au carre ; les deux termes en ln(1+x) se regroupent."
          - math: 'g''(x)-g(x) = \frac{x-(1+x)^2\ln(1+x)}{x^2(1+x)}'
            note: "On multiplie numerateur et denominateur par (1+x) pour eliminer la fraction imbriquee."
          - math: '\boxed{f''(x) = \frac{x-(1+x)^2\ln(1+x)}{x^2(1+x)}\,e^{-x}}'
            note: "Forme exactement imprimee par l'enonce, prete pour l'etude de signe de la question 4."
      - id: q6a
        part: "Partie II — 4. Encadrement de f', entre -3/2 et 0"
        stem: |-
          **4- a)** (0,5 pt) Montrer que : $\forall x \in\, ]0, +\infty[\ ;\quad -\dfrac{3}{2} < \dfrac{x - (1+x)^2\ln(1+x)}{x^2(1+x)} < 0$
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Note $\varphi(x) = \dfrac{x-(1+x)^2\ln(1+x)}{x^2(1+x)}$, le facteur entre parenthèses de la question 3. Deux inégalités à établir, et **chacune sort d'une seule des deux bornes** de l'encadrement I-1-b — c'est pour cela que I-1-b avait été démontré à double sens.

          **La majoration $\varphi(x)<0$.** Comme $x^2(1+x)>0$ pour $x>0$, il suffit de montrer $x-(1+x)^2\ln(1+x) < 0$, c'est-à-dire $\ln(1+x) > \dfrac{x}{(1+x)^2}$. La borne **gauche** de I-1-b donne $\ln(1+x) \ge \dfrac{2x}{2+x}$ ; il suffit donc de comparer $\dfrac{2x}{2+x}$ à $\dfrac{x}{(1+x)^2}$ :

          $$\frac{2x}{2+x} - \frac{x}{(1+x)^2} = \frac{2x(1+x)^2 - x(2+x)}{(2+x)(1+x)^2} = \frac{x\bigl[2(1+x)^2-(2+x)\bigr]}{(2+x)(1+x)^2}$$

          Développe le crochet : $2(1+2x+x^2)-(2+x) = 2+4x+2x^2-2-x = 3x+2x^2 = x(2x+3)$. Donc :

          $$\frac{2x}{2+x} - \frac{x}{(1+x)^2} = \frac{x^2(2x+3)}{(2+x)(1+x)^2} > 0 \quad (x>0)$$

          tous les facteurs étant strictement positifs. Donc $\dfrac{2x}{2+x} > \dfrac{x}{(1+x)^2}$, et comme $\ln(1+x) \ge \dfrac{2x}{2+x}$ : $\ln(1+x) > \dfrac{x}{(1+x)^2}$, **strictement**. D'où $\varphi(x)<0$.

          **La minoration $\varphi(x)>-\dfrac32$.** Il faut $x-(1+x)^2\ln(1+x) > -\dfrac32 x^2(1+x)$, c'est-à-dire $(1+x)^2\ln(1+x) < x + \dfrac32 x^2(1+x)$. La borne **droite** de I-1-b donne $\ln(1+x) \le \dfrac12\cdot\dfrac{x^2+2x}{1+x}$, donc $(1+x)^2\ln(1+x) \le (1+x)\cdot\dfrac{x(x+2)}{2}$. Reste à comparer ce majorant à $x+\dfrac32 x^2(1+x)$ :

          $$x+\frac32 x^2(1+x) - \frac{x(x+2)(1+x)}{2} = x\left[1 + \frac32 x(1+x) - \frac{(x+2)(1+x)}{2}\right]$$

          Développe le crochet sur dénominateur $2$ : $2 + 3x(1+x) - (x+2)(1+x) = (1+x)\bigl[3x-(x+2)\bigr]+2 = (1+x)(2x-2)+2$. Or $(1+x)(2x-2) = 2x^2-2 = 2(x^2-1)$, donc le crochet vaut $\dfrac{2(x^2-1)+2}{2} = x^2$. Donc :

          $$x+\frac32 x^2(1+x) - \frac{x(x+2)(1+x)}{2} = x\times x^2 = x^3 > 0 \quad (x>0)$$

          Donc $(1+x)^2\ln(1+x) \le \dfrac{x(x+2)(1+x)}{2} < x+\dfrac32 x^2(1+x)$, l'écart de $x^3$ rendant l'inégalité finale **strictement** vraie même si la première étape n'était que large. D'où $\varphi(x) > -\dfrac32$.

          **Conclusion.**

          $$\boxed{\forall x\in\,]0,+\infty[\ ;\quad -\frac32 < \varphi(x) < 0}$$

          **Le piège nommé de cette question.** Utiliser la mauvaise borne de I-1-b pour chaque sens : c'est la borne **gauche** (minorant de $\ln(1+x)$) qui produit la **majoration** de $\varphi$ ($\varphi<0$), et c'est la borne **droite** (majorant de $\ln(1+x)$) qui produit la **minoration** de $\varphi$ ($\varphi>-\frac32$) — l'appariement est inversé par rapport à l'intuition naïve « minorant sert à minorer », et s'y perdre annule tout le calcul qui suit.
        steps:
          - math: '\frac{2x}{2+x} - \frac{x}{(1+x)^2} = \frac{x^2(2x+3)}{(2+x)(1+x)^2} > 0 \ (x>0) \ \Longrightarrow\ \ln(1+x) > \frac{x}{(1+x)^2} \ \Longrightarrow\ \varphi(x) < 0'
            note: "Borne GAUCHE de I-1-b : produit la majoration de phi."
          - math: '(1+x)^2\ln(1+x) \le \frac{x(x+2)(1+x)}{2} \quad (\text{borne DROITE de I-1-b})'
            note: "On applique la borne droite de I-1-b, multipliee par (1+x) au carre."
          - math: 'x+\frac32 x^2(1+x) - \frac{x(x+2)(1+x)}{2} = x^3 > 0 \ (x>0) \ \Longrightarrow\ (1+x)^2\ln(1+x) < x+\frac32 x^2(1+x)'
            note: "Ecart calcule exactement : x au cube, strictement positif pour x > 0."
          - math: '\boxed{-\frac32 < \varphi(x) < 0} \qquad \varphi(x) = \frac{x-(1+x)^2\ln(1+x)}{x^2(1+x)}'
            note: "Les deux inegalites reunies, chacune tiree d'une borne differente de I-1-b."
      - id: q6b
        stem: |-
          **4- b)** (0,25 pt) En déduire que : $\forall x \in\, ]0, +\infty[\ ;\quad -\dfrac{3}{2} < f'(x) < 0$
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** On a $f'(x) = \varphi(x)\,e^{-x}$ (question 3), et on connaît l'encadrement de $\varphi(x)$ (question 4-a) : il reste à multiplier par $e^{-x}$, en faisant attention à l'effet d'une multiplication par un facteur **entre $0$ et $1$** sur un nombre négatif.

          **L'encadrement de $e^{-x}$.** Pour $x>0$ : $-x<0$, donc $e^{-x} < e^0=1$ ; et $e^{-x}>0$ toujours. Donc $0 < e^{-x} < 1$.

          **L'effet sur un négatif.** $\varphi(x) < 0$ (question 4-a). Multiplier un nombre strictement négatif par un facteur **strictement compris entre $0$ et $1$** le rapproche de $0$, donc l'**augmente** :

          $$\varphi(x) < \varphi(x)\cdot e^{-x} < \varphi(x)\cdot 0 = 0$$

          (la première inégalité vient de $e^{-x}<1$ appliqué à $\varphi(x)<0$ — multiplier une inégalité par un facteur positif conserve son sens, donc $\varphi(x)\times 1 > \varphi(x)\times e^{-x}$ ; la seconde vient de $\varphi(x)<0$ et $e^{-x}>0$, produit négatif).

          **On combine avec $\varphi(x)>-\dfrac32$ (question 4-a) :**

          $$-\frac32 < \varphi(x) < \varphi(x)e^{-x} < 0$$

          Et $\varphi(x)e^{-x} = f'(x)$ :

          $$\boxed{\forall x\in\,]0,+\infty[\ ;\quad -\frac32 < f'(x) < 0}$$

          **La lecture, avant même le tableau de variations.** $f'(x)$ est **strictement négative partout** sur $]0,+\infty[$ : $f$ y est donc strictement décroissante, sans aucun point où la dérivée s'annule — le tableau de variations de la question 5-a n'aura donc qu'un seul sens de variation à décrire.

          **Le piège nommé de cette question.** Écrire $\varphi(x)e^{-x} < \varphi(x)$ au lieu de $>$, en oubliant que multiplier un négatif par un nombre plus petit que $1$ (mais positif) l'**augmente**, ne le diminue pas. Teste sur un exemple pour t'en convaincre : $-4 \times 0{,}5 = -2$, et $-2 > -4$.
        steps:
          - math: '0 < e^{-x} < 1 \quad (x>0)'
            note: "e puissance moins x est toujours strictement positif, et strictement inferieur a 1 pour x positif."
          - math: '\varphi(x) < 0 \ \text{et}\ 0<e^{-x}<1 \ \Longrightarrow\ \varphi(x) < \varphi(x)e^{-x} < 0'
            note: "Multiplier un negatif par un facteur entre 0 et 1 le rapproche de 0 : l'inegalite s'inverse par rapport a l'intuition naive."
          - math: '-\frac32 < \varphi(x) < \varphi(x)e^{-x} < 0 \ \Longrightarrow\ \boxed{-\frac32 < f''(x) < 0}'
            note: "On combine avec la borne -3/2 de la question 4-a, et phi(x) fois e puissance moins x vaut f'(x) par la question 3."
      - id: q7a
        part: "Partie II — 5. Tableau de variations et construction de (C)"
        stem: |-
          **5- a)** (0,25 pt) Dresser le tableau de variations de $f$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Un quart de point, parce que tout le travail est déjà fait : il reste à **assembler** les résultats des questions précédentes en un tableau.

          **Ce qu'on assemble.**
          — $f$ est continue sur $[0,+\infty[$ (continue à droite en $0$, question 2-a ; continue sur $]0,+\infty[$ comme dérivable, question 3) ;
          — $f'(x)<0$ pour tout $x>0$ (question 4-b) : $f$ est **strictement décroissante** sur $]0,+\infty[$, et donc, par continuité en $0$, sur $[0,+\infty[$ tout entier ;
          — $f(0)=1$ (donné) ;
          — $\displaystyle\lim_{x\to+\infty} f(x) = 0$ (question 1), limite jamais atteinte (asymptote).

          **Le tableau.**

          | $x$ | $0$ | | $+\infty$ |
          |---|---|---|---|
          | $f'(x)$ | | $-$ | |
          | $f(x)$ | $1$ | $\searrow$ | $0$ |

          $f$ décroît strictement de $1$ (valeur atteinte en $0$) vers $0$ (limite, jamais atteinte) sur tout $[0,+\infty[$.

          **Le piège nommé de cette question.** Chercher un extremum local ou un point où $f'$ s'annulerait : la question 4-b a établi $f'(x)<0$ **strictement partout** sur $]0,+\infty[$, sans exception — il n'y a ni palier ni changement de sens à chercher, et en inventer un serait contredire ce qui vient d'être démontré.
        steps:
          - math: 'f \text{ continue sur } [0,+\infty[, \quad f''(x) < 0 \ \forall x>0 \ (\text{question 4-b})'
            note: "Continuite (question 2-a et 3) plus signe de la derivee (question 4-b)."
          - math: 'f(0) = 1 \quad (\text{donne}), \qquad \lim_{x\to+\infty} f(x) = 0 \quad (\text{question 1})'
            note: "Les deux valeurs aux bornes du tableau."
          - math: 'f \text{ strictement decroissante sur } [0,+\infty[, \text{ de } 1 \text{ (atteint) a } 0 \text{ (limite, jamais atteinte)}'
            note: "Synthese : un seul sens de variation, pas de point stationnaire."
      - id: q7b
        stem: |-
          **5- b)** (0,75 pt) Construire la courbe $(C)$ en faisant apparaître la demi-tangente à droite au point d'abscisse $0$. (On prendra $\|\vec{i}\| = 2\ \text{cm}$.)
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Aucune figure n'est fournie par l'énoncé — c'est au candidat de la construire, à partir des seules données déjà établies dans les questions précédentes. La question ne demande donc pas un nouveau calcul, mais de **rassembler** les ingrédients dans le bon ordre et de les placer correctement sur le repère.

          **Les ingrédients, un par un, chacun déjà démontré.**

          1. **Le point de départ.** $f(0)=1$ : la courbe part du point $(0\,;\,1)$ sur l'axe des ordonnées.
          2. **La demi-tangente à droite en $0$.** $f'_d(0) = -\dfrac32$ (question 2-c) : au point $(0\,;\,1)$, la courbe part avec une pente de $-\dfrac32$ — une demi-tangente **oblique** (ni verticale ni horizontale), assez raide, orientée vers le bas et vers la droite.
          3. **Le sens de variation.** $f$ est strictement décroissante sur tout $[0,+\infty[$ (question 5-a) : la courbe descend sans jamais remonter ni se stabiliser en un palier.
          4. **Le comportement en $+\infty$.** $f(x) \to 0$ (question 1) : la courbe s'approche de l'axe des abscisses sans jamais le toucher — une **asymptote horizontale**, approchée par valeurs strictement positives (puisque $f$ décroît vers $0$ sans jamais l'atteindre, $f(x)>0$ pour tout $x$ fini).
          5. **L'échelle.** $\|\vec{i}\|=2\ \text{cm}$ : chaque unité en abscisse occupe $2\ \text{cm}$ sur le papier — à respecter pour que la pente $-\dfrac32$ de la demi-tangente soit dessinée avec le bon angle visuel.

          **La forme d'ensemble à tracer.** Une courbe qui part de $(0\,;\,1)$ avec une tangente de pente $-\dfrac32$, descend strictement et continûment, se rapproche de plus en plus lentement de l'axe des abscisses à mesure que $x$ grandit, sans jamais le franchir ni le toucher — la concavité n'étant pas demandée par l'énoncé, aucune inflexion n'est à chercher ni à justifier.

          **Le piège nommé de cette question.** Tracer une tangente **horizontale** en $0$, par réflexe (beaucoup de courbes de ce type démarrent avec une tangente horizontale) : ici $f'_d(0)=-\dfrac32 \ne 0$, la demi-tangente est clairement inclinée, et l'omettre ou la dessiner plate fait perdre l'essentiel des $0{,}75$ point de cette question. Second piège : faire toucher la courbe à l'axe des abscisses en un point fini — la question 1 a établi une limite, jamais une valeur atteinte.
        steps:
          - math: '(0\,;\,1) \quad (\text{point de depart, } f(0)=1)'
            note: "Premier ingredient : la valeur en 0, deja etablie."
          - math: 'f''_d(0) = -\frac32 \quad (\text{question 2-c, pente de la demi-tangente a droite en 0})'
            note: "Deuxieme ingredient : la pente de depart, oblique et raide."
          - math: 'f \text{ strictement decroissante sur } [0,+\infty[ \quad (\text{question 5-a})'
            note: "Troisieme ingredient : un seul sens de variation, sans palier ni remontee."
          - math: 'f(x) \to 0^{+} \text{ quand } x\to+\infty \quad (\text{question 1, asymptote } y=0)'
            note: "Quatrieme ingredient : la courbe s'approche de l'axe des abscisses sans jamais le toucher."
      - id: q8
        part: "Partie III — 1. Existence et unicite de alpha, solution de f(x) = 3x"
        stem: |-
          **1)** (0,5 pt) Montrer que l'équation d'inconnue $x$ : $f(x) = 3x$, admet une unique solution $\alpha$ dans $]0, +\infty[$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** « Il existe une unique » : deux affirmations, deux outils différents. L'existence vient du **théorème des valeurs intermédiaires**, l'unicité de la **stricte monotonie**. Aucun des deux ne remplace l'autre — un candidat qui n'en invoque qu'un perd la moitié du barème.

          **On introduit une fonction auxiliaire.** Pose $\psi(x) = f(x) - 3x$ sur $[0,+\infty[$ : résoudre $f(x)=3x$ revient à résoudre $\psi(x)=0$.

          **Hypothèse 1 : la continuité, vérifiée séparément.** $f$ est continue sur $[0,+\infty[$ (établi aux questions 2-a et 3), et $x\mapsto 3x$ est continue partout. $\psi$, différence de deux fonctions continues, est **continue sur $[0,+\infty[$**.

          **Hypothèse 2 : la stricte monotonie, vérifiée séparément.** Sur $]0,+\infty[$, $\psi'(x) = f'(x) - 3$. Or $f'(x) < 0$ (question 4-b), donc $f'(x) - 3 < 0 - 3 = -3 < 0$. $\psi$ est donc **strictement décroissante** sur $]0,+\infty[$, et par continuité en $0$, sur $[0,+\infty[$ tout entier.

          **Les valeurs qui encadrent le zéro cherché.** $\psi(0) = f(0) - 0 = 1 > 0$. Et $\displaystyle\lim_{x\to+\infty}\psi(x) = \lim_{x\to+\infty}\bigl(f(x)-3x\bigr) = 0 - \infty = -\infty$ (une quantité qui reste bornée, $f(x)\to 0$, moins une quantité qui explose, $3x\to+\infty$ : pas d'indétermination, le résultat est direct).

          **La conclusion, par le corollaire du théorème des valeurs intermédiaires (continuité + stricte monotonie $\Rightarrow$ bijection sur l'intervalle image — domaine « dérivabilité et étude de fonctions », hors du corps de cette leçon).** $\psi$ continue et strictement décroissante sur $[0,+\infty[$ réalise une bijection de $[0,+\infty[$ sur $\psi\bigl([0,+\infty[\bigr) = \left]\displaystyle\lim_{+\infty}\psi,\ \psi(0)\right] = \bigl]-\infty,\,1\bigr]$.

          Or $0 \in\, ]-\infty,1]$ : il existe donc un **unique** $x_0 \in [0,+\infty[$ tel que $\psi(x_0)=0$. Comme $\psi(0)=1\ne 0$, ce $x_0$ n'est pas $0$ : il appartient à $]0,+\infty[$. Note-le $\alpha$.

          $$\boxed{\text{L'équation } f(x)=3x \text{ admet une unique solution } \alpha \text{ dans } ]0,+\infty[}$$

          **Le piège nommé de cette question.** Invoquer le TVI et s'arrêter là. Le TVI donne l'**existence**, jamais l'unicité — une fonction continue peut valoir $0$ en plusieurs points. C'est la stricte décroissance qui interdit un second passage par $0$. Les deux arguments sont complémentaires, et la rédaction doit les faire apparaître **séparément**, comme ci-dessus.
        steps:
          - math: '\psi(x) = f(x) - 3x \quad \text{sur } [0,+\infty[, \qquad \psi \text{ continue (f continue + } 3x \text{ continue)}'
            note: "Fonction auxiliaire. Hypothese 1 verifiee : continuite."
          - math: '\psi''(x) = f''(x) - 3 < 0 - 3 = -3 < 0 \quad (x>0) \ \Longrightarrow\ \psi \text{ strictement decroissante sur } [0,+\infty['
            note: "Hypothese 2 verifiee : stricte monotonie, via le signe de f' etabli a la question 4-b."
          - math: '\psi(0) = 1 > 0, \qquad \lim_{x\to+\infty}\psi(x) = 0-\infty = -\infty'
            note: "Valeurs aux bornes : borne finie moins la droite qui explose, pas d'indetermination."
          - math: '\psi \text{ bijection de } [0,+\infty[ \text{ sur } \,]-\infty,1] \ni 0 \ \Longrightarrow\ \exists!\, \alpha \in\,]0,+\infty[,\ \psi(\alpha)=0'
            note: "Corollaire du TVI (continuite + stricte monotonie), hors socle de cette lecon. Alpha different de 0 car psi(0) = 1."
      - id: q9a
        part: "Partie III — 2. La suite (u_n), point fixe itere de F = f sur 3"
        stem: |-
          **2- a)** (0,5 pt) Soit $\beta \in \mathbb{R}^{+}$ et $(u_n)_{n \in \mathbb{N}}$ la suite numérique définie par $u_0 = \beta$ et $\forall n \in \mathbb{N}\ ;\ u_{n+1} = \dfrac{1}{3}\,f(u_n)$. Montrer que : $\forall n \in \mathbb{N}\ ;\ u_n \ge 0$
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Une propriété qui porte sur **tous les rangs $n$**, définie par une relation qui construit chaque terme à partir du précédent : c'est le signal exact d'une démonstration par **récurrence**, avec initialisation et hérédité écrites séparément.

          **Le fait préalable, à établir une fois pour toutes.** Sur $[0,+\infty[$, $f$ est strictement décroissante (question 5-a) de $f(0)=1$ vers une limite de $0$ **jamais atteinte** : donc $f(x) > 0$ pour **tout** $x\ge 0$ fini.

          **Initialisation ($n=0$).** $u_0=\beta$, et $\beta \in \mathbb{R}^{+}$ signifie $\beta \ge 0$ par définition de $\mathbb{R}^{+}$ (borne $0$ incluse). Donc $u_0 \ge 0$ : la propriété est vraie au rang $0$.

          **Hérédité.** Suppose $u_n \ge 0$ pour un rang $n$ fixé (hypothèse de récurrence). Comme $u_n \ge 0$, $u_n$ appartient au domaine $[0,+\infty[$ de $f$, et le fait préalable donne $f(u_n) > 0$. Donc :

          $$u_{n+1} = \frac13 f(u_n) > 0 \ge 0$$

          La propriété est vraie au rang $n+1$.

          **Conclusion.** Par le principe de récurrence, initialisation et hérédité étant toutes deux établies :

          $$\boxed{\forall n \in \mathbb{N}\ ;\quad u_n \ge 0}$$

          **Pourquoi $\mathbb{R}^{+}$ et non $\mathbb{R}^{+*}$ compte ici.** Le glyphe cassé du scan a été adjugé $\mathbb{R}^{+}$ (exposant « + » seul, $\beta=0$ admis) — précisément parce que l'initialisation de cette récurrence n'a besoin **que** de $\beta \ge 0$, jamais de $\beta > 0$ strictement. Le cas limite $\beta=0$ fonctionne sans accroc : $u_1 = \frac13 f(0) = \frac13 > 0$, et la récurrence repart normalement.

          **Le piège nommé de cette question.** Écrire « $f(u_n) \ge 0$ donc $u_{n+1}\ge 0$ » sans avoir d'abord établi que $f$ est **strictement positive** (pas seulement positive ou nulle) sur tout son domaine — ce fait n'est pas immédiat, il vient du tableau de variations complet de la question 5-a, et doit être cité explicitement.
        steps:
          - math: 'f \text{ strictement decroissante sur } [0,+\infty[ \text{, de } f(0)=1 \text{ vers une limite } 0 \text{ jamais atteinte} \ \Longrightarrow\ f(x)>0 \ \forall x\ge 0'
            note: "Fait prealable, tire du tableau de variations complet (question 5-a)."
          - math: '\text{Initialisation : } u_0 = \beta \ge 0 \quad (\beta \in \mathbb{R}^{+})'
            note: "R plus inclut 0 : c'est pourquoi le glyphe casse a ete adjuge R plus et non R plus etoile."
          - math: '\text{Heredite : } u_n \ge 0 \ \Longrightarrow\ f(u_n) > 0 \ \Longrightarrow\ u_{n+1} = \frac13 f(u_n) > 0 \ge 0'
            note: "u_n dans le domaine de f (positif), donc f(u_n) strictement positif par le fait prealable."
          - math: '\boxed{\forall n \in \mathbb{N}\ ;\ u_n \ge 0} \quad (\text{recurrence, initialisation + heredite})'
            note: "Principe de recurrence : les deux etapes etablies separement suffisent pour tout rang n."
      - id: q9b
        stem: |-
          **2- b)** (0,5 pt) Montrer que : $\forall n \in \mathbb{N}\ ;\ |u_{n+1} - \alpha| \le \dfrac{1}{2}|u_n - \alpha|$
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** Note $F(x) = \dfrac13 f(x)$. Remarque d'abord un fait décisif : $\alpha$ vérifie $f(\alpha)=3\alpha$ (question 1), c'est-à-dire $F(\alpha) = \dfrac13 f(\alpha) = \alpha$ — **$\alpha$ est le point fixe de $F$**, exactement le nombre que la suite $u_{n+1}=F(u_n)$ cherche à atteindre. La question demande de majorer l'écart $|u_{n+1}-\alpha| = |F(u_n)-F(\alpha)|$ : c'est une comparaison de $F$ entre deux points, et l'outil pour cela — hors du corps de cette leçon — est le **théorème des accroissements finis**.

          **Le théorème des accroissements finis (TAF), énoncé ici car il n'appartient pas au socle R0–R6 de cette leçon (domaine « dérivabilité et étude de fonctions »).** Si $h$ est continue sur un intervalle fermé $[p,q]$ et dérivable sur l'intervalle ouvert $]p,q[$, alors il existe $c \in\, ]p,q[$ tel que $h(q)-h(p) = h'(c)(q-p)$.

          **Les hypothèses, vérifiées pour $F=\frac13 f$.** $F$ est continue sur $[0,+\infty[$ (car $f$ l'est, questions 2-a et 3) et dérivable sur $]0,+\infty[$ avec $F'(x)=\frac13 f'(x)$ (car $f$ l'est, question 3). Les deux points $u_n \ge 0$ (question 2-a) et $\alpha>0$ (question 1) sont dans $[0,+\infty[$ ; l'intervalle ouvert entre eux — qu'il s'agisse de $]u_n,\alpha[$ ou $]\alpha,u_n[$ selon leur ordre — est inclus dans $]0,+\infty[$ où $F$ est dérivable, sauf si $u_n=0$, auquel cas l'intervalle ouvert $]0,\alpha[$ reste inclus dans le domaine de dérivabilité : le TAF s'applique dans tous les cas.

          **L'application.** Il existe $c$, strictement compris entre $u_n$ et $\alpha$ (donc $c>0$), tel que :

          $$F(u_n) - F(\alpha) = F'(c)\,(u_n-\alpha)$$

          **La borne sur $F'(c)$.** Comme $c>0$, la question 4-b donne $-\dfrac32 < f'(c) < 0$, donc $|f'(c)| < \dfrac32$, et :

          $$|F'(c)| = \left|\frac13 f'(c)\right| = \frac13|f'(c)| < \frac13\times\frac32 = \frac12$$

          **On prend la valeur absolue de l'égalité du TAF :**

          $$|F(u_n)-F(\alpha)| = |F'(c)|\cdot|u_n-\alpha| < \frac12|u_n-\alpha| \le \frac12|u_n-\alpha|$$

          Or $F(u_n) = u_{n+1}$ (définition de la suite) et $F(\alpha)=\alpha$ (point fixe, établi ci-dessus) :

          $$\boxed{\forall n\in\mathbb{N}\ ;\quad |u_{n+1}-\alpha| \le \frac12|u_n-\alpha|}$$

          **Ce que cette inégalité raconte.** Chaque itération de la récurrence divise l'écart à $\alpha$ par (au moins) $2$ — c'est un argument de **contraction**, la méthode générale pour montrer qu'une suite définie par $u_{n+1}=F(u_n)$ converge vers le point fixe de $F$, dès que $|F'|$ reste majoré par une constante strictement inférieure à $1$.

          **Le piège nommé de cette question.** Appliquer le TAF sans vérifier ses deux hypothèses (continuité sur le fermé, dérivabilité sur l'ouvert) — ici elles sont vraies, mais elles ne le sont que parce que les questions 2-a et 3 les ont établies plus haut ; les admettre sans les citer prive la démonstration de son fondement.
        steps:
          - math: 'F(x) = \frac13 f(x), \qquad F(\alpha) = \frac13 f(\alpha) = \frac13(3\alpha) = \alpha \quad (\text{question 1 : } f(\alpha)=3\alpha)'
            note: "Alpha est le point fixe de F : c'est ce qui relie la question 1 a celle-ci."
          - math: '\exists\, c \text{ entre } u_n \text{ et } \alpha \ (c>0), \quad F(u_n)-F(\alpha) = F''(c)(u_n-\alpha) \quad (\text{TAF, F continue sur le ferme, derivable sur l ouvert})'
            note: "Theoreme des accroissements finis, hors socle de cette lecon (domaine derivabilite), enonce et applique ici."
          - math: '-\frac32 < f''(c) < 0 \ (\text{question 4-b, car } c>0) \ \Longrightarrow\ |F''(c)| = \frac13|f''(c)| < \frac12'
            note: "Borne sur f' reutilisee de la question 4-b, divisee par 3."
          - math: '|u_{n+1}-\alpha| = |F(u_n)-F(\alpha)| = |F''(c)|\,|u_n-\alpha| < \frac12|u_n-\alpha| \le \boxed{\frac12|u_n-\alpha|}'
            note: "Valeur absolue de l'egalite du TAF, puis la borne sur F' de c."
      - id: q9c
        stem: |-
          **2- c)** (0,5 pt) Montrer par récurrence que : $\forall n \in \mathbb{N}\ ;\ |u_n - \alpha| \le \dfrac{1}{2^n}|\beta - \alpha|$
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** L'énoncé demande explicitement une récurrence : la question 2-b a établi le passage d'un rang au suivant (diviser l'écart par $2$), il reste à l'enchaîner $n$ fois, proprement, avec initialisation et hérédité séparées.

          **Initialisation ($n=0$).** Le membre de gauche : $|u_0-\alpha| = |\beta-\alpha|$ (car $u_0=\beta$). Le membre de droite : $\dfrac{1}{2^0}|\beta-\alpha| = 1\times|\beta-\alpha| = |\beta-\alpha|$. Les deux membres sont **égaux** :

          $$|u_0-\alpha| = |\beta-\alpha| = \frac{1}{2^0}|\beta-\alpha|$$

          L'inégalité $\le$ est donc vraie au rang $0$ — avec égalité. **C'est précisément ce fait qui a permis d'adjuger le glyphe cassé du scan à $\le$ et non $<$** : un $<$ strict rendrait cette égalité fausse au rang initial.

          **Hérédité.** Suppose $|u_n-\alpha| \le \dfrac{1}{2^n}|\beta-\alpha|$ pour un rang $n$ fixé (hypothèse de récurrence, notée HR). Montre que la propriété passe au rang $n+1$.

          De la question 2-b : $|u_{n+1}-\alpha| \le \dfrac12|u_n-\alpha|$. En appliquant HR au membre de droite, et en multipliant l'inégalité HR par $\dfrac12>0$ (ce qui conserve son sens) :

          $$\frac12|u_n-\alpha| \le \frac12\cdot\frac{1}{2^n}|\beta-\alpha| = \frac{1}{2^{n+1}}|\beta-\alpha|$$

          En chaînant les deux inégalités :

          $$|u_{n+1}-\alpha| \le \frac12|u_n-\alpha| \le \frac{1}{2^{n+1}}|\beta-\alpha|$$

          La propriété est donc vraie au rang $n+1$.

          **Conclusion.** Par le principe de récurrence, initialisation (avec égalité au rang $0$) et hérédité étant toutes deux établies :

          $$\boxed{\forall n\in\mathbb{N}\ ;\quad |u_n-\alpha| \le \frac{1}{2^n}|\beta-\alpha|}$$

          **Le piège nommé de cette question.** Appliquer la question 2-b directement à $|u_n-\alpha|$ au lieu de $|u_{n+1}-\alpha|$ — confondre le rang qu'on **suppose** connu (HR, au rang $n$) et celui qu'on **cherche** à établir (au rang $n+1$) fait s'effondrer toute la chaîne d'inégalités. Il faut toujours partir de $|u_{n+1}-\alpha|$, lui appliquer 2-b, puis **seulement ensuite** invoquer HR sur le $|u_n-\alpha|$ qui apparaît.
        steps:
          - math: '\text{Initialisation : } |u_0-\alpha| = |\beta-\alpha| = \frac{1}{2^0}|\beta-\alpha| \quad (\text{egalite au rang } 0)'
            note: "u_0 = beta par definition. Egalite exacte au rang 0 : c'est elle qui force le symbole a etre <= et non <."
          - math: '\text{HR : } |u_n-\alpha| \le \frac{1}{2^n}|\beta-\alpha| \quad \text{(hypothese, rang } n\text{)}'
            note: "Ce qu'on suppose connu, a un rang n fixe quelconque."
          - math: '|u_{n+1}-\alpha| \le \frac12|u_n-\alpha| \ (\text{question 2-b}) \ \le\ \frac12\cdot\frac{1}{2^n}|\beta-\alpha| \ (\text{HR}) \ =\ \frac{1}{2^{n+1}}|\beta-\alpha|'
            note: "Heredite : question 2-b d'abord, puis HR appliquee au terme qui en resulte."
          - math: '\boxed{\forall n \in \mathbb{N}\ ;\ |u_n-\alpha| \le \frac{1}{2^n}|\beta-\alpha|} \quad (\text{recurrence complete})'
            note: "Initialisation (avec egalite) et heredite etablies separement : la recurrence est complete."
      - id: q9d
        stem: |-
          **2- d)** (0,25 pt) En déduire que la suite $(u_n)_{n \in \mathbb{N}}$ converge vers $\alpha$.
        reasoning: |-
          **Ce qu'on cherche et pourquoi ce geste.** On dispose d'un encadrement de $|u_n-\alpha|$ (question 2-c) ; il reste à en tirer la convergence — **par encadrement**, jamais en affirmant que « la majoration décroît donc ça converge » sans le dire proprement.

          **L'encadrement de départ.** Une valeur absolue est toujours positive ou nulle, donc pour tout $n$ :

          $$0 \le |u_n-\alpha| \le \frac{1}{2^n}|\beta-\alpha|$$

          **La limite de la borne de droite.** $|\beta-\alpha|$ est une constante (indépendante de $n$), et $\dfrac{1}{2^n} \to 0$ quand $n\to+\infty$ — limite de référence d'une suite géométrique de raison $q=\dfrac12$, avec $|q|<1$ (chapitre « Suites numériques »). Donc :

          $$\lim_{n\to+\infty} \frac{1}{2^n}|\beta-\alpha| = 0 \times |\beta-\alpha| = 0$$

          **Le théorème des gendarmes.** La borne de gauche (la suite constante $0$) et la borne de droite tendent toutes deux vers $0$ : le théorème des gendarmes force le milieu à converger aussi, vers cette même valeur :

          $$\lim_{n\to+\infty} |u_n-\alpha| = 0$$

          **La conclusion.** $|u_n-\alpha| \to 0$ signifie, par définition même de la limite d'une suite, que $u_n \to \alpha$ :

          $$\boxed{\lim_{n\to+\infty} u_n = \alpha}$$

          **Ce que tout le problème vient de démontrer, relu d'ensemble.** La suite $(u_n)$ ne se contente pas de converger : elle converge **géométriquement vite**, avec un écart à $\alpha$ divisé par au moins $2$ à chaque étape — c'est la signature d'une **itération de point fixe** (méthode de Picard) appliquée à $F=\frac13 f$, rendue possible par le fait que $|F'|<\frac12<1$ partout (question 2-b). Quel que soit le point de départ $\beta \ge 0$ choisi, la suite retombe toujours sur le même $\alpha$ — celui, et le seul, que la question 1 a isolé par TVI et stricte monotonie.

          **Le piège nommé de cette question.** Conclure « la suite est décroissante et minorée, donc elle converge » : rien dans ce problème n'établit que $(u_n)$ est monotone — elle pourrait très bien osciller autour de $\alpha$ en s'en rapprochant. C'est l'encadrement de $|u_n-\alpha|$ par le théorème des gendarmes, et lui seul, qui donne à la fois la convergence et l'identité de la limite en une seule étape.
        steps:
          - math: '0 \le |u_n-\alpha| \le \frac{1}{2^n}|\beta-\alpha| \quad (\text{question 2-c, valeur absolue toujours} \ge 0)'
            note: "Encadrement de depart : la valeur absolue est bornee des deux cotes."
          - math: '\lim_{n\to+\infty} \frac{1}{2^n} = 0 \ (|q|<1) \ \Longrightarrow\ \lim_{n\to+\infty} \frac{1}{2^n}|\beta-\alpha| = 0'
            note: "Suite geometrique de raison 1/2 : limite de reference du chapitre Suites numeriques."
          - math: '\lim_{n\to+\infty} |u_n-\alpha| = 0 \quad (\text{theoreme des gendarmes})'
            note: "Les deux bornes (0 et 1/2 puissance n fois |beta-alpha|) tendent vers 0 : le milieu aussi."
          - math: '\boxed{\lim_{n\to+\infty} u_n = \alpha}'
            note: "Par definition : le taux d'accroissement de u_n a alpha qui tend vers 0 signifie que u_n tend vers alpha."
