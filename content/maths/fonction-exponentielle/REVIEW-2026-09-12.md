# fonction-exponentielle — revue vague 1 (2026-09-12)

Deux critiques (`bac-fidelity-critic`, `pedagogy-critic`).

## Ce qui est sain — recompté, à ne pas re-dépenser

La critique fidelity a recalculé **les 31 items**, **les deux sommets de bout en
bout** (r-bac 2022 : $(e^{x/2}-1)^2-1 = e^{x/2}(e^{x/2}-2)$, $f(\ln4)=\ln4$,
$f''=\frac12 e^{x/2}g(x)$, $(f^{-1})'(\ln4)=\frac{1}{1+2\ln4}$ ; r-variation :
$f'=\frac{2e^x}{(e^x+1)^2}$, inflexion en $(0,0)$, $(f^{-1})'(0)=2$), **les six
barèmes** et les mathématiques des sujets 2019/2020/2025 re-dérivées depuis le
relevé. **Aucune faute d'arithmétique.** Aucun barème inventé — et les trois
cartes dont le relevé de la notion ne porte pas le chiffre le disent
explicitement et pointent le fichier `_incoming` qui le porte : cette
divulgation est exemplaire, la garder.

Les deux critiques ont recompté `coverage_summary` **indépendamment** depuis les
tags bruts de distracteurs : les 13 comptes, `total_items: 31` et les 9 seaux de
`ramp_coverage` sont **justes**. Et sur les 88 citations « chapitre N » de la
notion, une seule est fausse (F8) — les 87 autres sont vérifiées correctes, y
compris celles qui visent les six leçons à numérotation décalée.

## Appliqué — 11 corrections objectives

| # | Où | Défaut | Correction |
|---|---|---|---|
| — | `exercises.yaml` ×19, `bank.yaml` ×14 | **33 notes de pas dont la syntaxe TeX arrivait NUE chez l'élève.** `<Note>` rend par ReactMarkdown + remarkMath : sans `$…$`, « `e^{x/2}` » sort littéralement, accolades comprises. Vérifié en rendant la chaîne dans le vrai pipeline | maths délimitées en `$…$` — aucun contenu mathématique modifié |
| F8 | `bank.yaml` ×2 | **deux fautes dans la même phrase** : « (croissance stricte de exp, **chapitre 2**) ». Le chapitre 2 **diffère** explicitement ce résultat (« on y reviendra au chapitre 5 ») ; et la croissance stricte ne donne **pas** $\lim_{-\infty}e^x=0$ — une fonction strictement croissante minorée peut tendre vers n'importe quel $\ell\ge0$ | « (limite de référence, chapitre 5) » |
| F3 | `lesson.md` | la liste « ces problèmes mobilisent **trois** outils que la leçon n'enseigne pas » est fausse dans les deux sens : **aucun des deux exercices ne contient d'intégrale** (l'IPP n'y sert jamais), et elle **omet** l'outil qu'ils exigent vraiment — le théorème de la bijection et $(f^{-1})'$, que la banque de la notion signale pourtant elle-même | « des outils » + quatrième entrée nommant la bijection et $(f^{-1})'$, avec la leçon où ils vivent |
| F13 | `exercises.yaml` | la description de $(C_g)$ se dit « transcrite **fidèlement** du scan » mais **durcit** une lecture que le relevé donne hésitante : il écrit « $x\in[-5;\approx0{,}5]$ » et referme sur « lecture d'échelle à confirmer » ; les deux sont tombés. Or $g(0{,}5)=5e^{0{,}25}-4{,}5=1{,}92$ et $g$ n'atteint $4$ que vers $x\approx0{,}87$ : un bord droit **exactement** à $0{,}5$ et une ordonnée de bord de $+4$ ne tiennent pas ensemble | le `≈` et la réserve restaurés, avec le chiffre qui le montre |
| F14 | `exercises.yaml`, `bank.yaml` | la note dit « son **carré diminué de 1** » — c'est $(e^{x/2})^2-1$ ; l'objet est $(e^{x/2}-1)^2$. Le `math` du même pas et le `reasoning` sont justes | « $(e^{x/2}-1)^2$ » / « son écart à 1, élevé au carré » |
| F18 | `lesson.md` | le récapitulatif **encadré** — celui que l'élève photographie — donne $(e^a)^n=e^{na}$ **sans quantificateur** ; le corps ne l'établit que pour $n\in\mathbb{N}$ puis l'étend à $\mathbb{Z}$ | $(n \in \mathbb{Z})$ ajouté |
| F20 | `lesson.md` ×2 | « **Une** primitive … $=e^{x^2}+C$ » confond une primitive et la famille — la leçon le dit pourtant juste 18 lignes plus haut | « Les primitives … sont » |
| F16 | `bank.yaml` | « au chapitre 6 **du chapitre** » — mot dupliqué par une réécriture antérieure | « de cette leçon » |
| F9 | `items.yaml` | l'en-tête affirme « le plancher n'est **PAS** atteint » — le contraire de l'état publié 1 840 lignes plus bas (`floor_met: true`). Recompté : c'est la **table** qui a raison | en-tête aligné sur l'état réel |
| F10 | `items.yaml` | **deux méthodes de comptage** déclarées dans le même fichier. Sous la périmée (« BY PRIMARY »), **cinq familles sur treize** passeraient sous le plancher et `floor_met: true` serait faux | la méthode abandonnée est signalée comme telle |
| F11 | `items.yaml` | « neuf places, **trois** items » — quatre ont été ajoutés (EXP-28…31), comme le pied de fichier le dit | « quatre items » |

`validate-content --strict` : 0 failure.

## Déféré — research-lead / propriétaire (cadre)

- **F1, bloquant : la filière n'est déclarée nulle part**, et les fichiers se
  contredisent — `lesson.md` dit « 2 Bac **SM** » deux fois, `checkpoints.yaml`
  dit « **Sciences Expérimentales** ». Aucun champ machine. Les deux cadres
  donnent des cibles **différentes** pour ce chapitre.
- **F2, bloquant en SM** : le savoir-faire « Étudier $a^x$ et $x^\alpha$ »
  n'est enseigné **nulle part** — zéro occurrence dans les cinq fichiers. Le
  cadre le signale déjà comme lacune connue, mais c'est un tiers du savoir-faire
  SM déclaré du chapitre.
- **F4** : le sommet est un problème **SExp** de 8,5 pts (3 h, coef 7) alors que
  la banque tient quatre exercices **SM** de 10 pts (Rolle/TAF, IPP, Riemann).

## Déféré — pédagogie / items

- **Quatre lignes de grille non réclamées** : `inegalite-reference-erreur`,
  `primitive-exp-erreur`, `resolution-exp-erreur`,
  `exp-valeur-negative-impossible` — aucune rupture en prose, aucun commit,
  aucune délégation écrite (il n'existe pas de spec pour cette notion).
- **R1, R6, R7 enseignent un modèle sans aucun commit.** Une seule des treize
  misconceptions est réellement **rompue** (R2, « teste ce modèle avec a=b=0 ») ;
  ailleurs le motif retombe sur « on nomme l'erreur, on énonce la vérité ».
- **Deux fuites de réponse au-dessus de la porte d'essai** : l'intro de
  `r-variation` répond à q1, q2 et q6 (asymptotes horizontales $y=1$ et $y=-1$,
  bornée, $f^{-1}$ sur $]-1,1[$) — dans l'exercice dont tout l'objet est de
  vérifier que l'élève sait courir seul ; et l'intro de `r-bac` annonce les
  quatre parties, dont « la Partie D exploite tout cela pour la réciproque »
  alors que q7a demande de **montrer** que $f$ admet une réciproque.
- **q5b imprime sa propre réponse** : la question demande de lire le signe de $g$
  sur la courbe, et la description textuelle de la courbe **donne** le signe.
  Cause racine : aucun asset $(C_g)$ n'existe, la figure a été remplacée par de
  la prose. → diagram-author.
- **Falaise R7→R8** : le dernier exemple travaillé est une inéquation en trois
  lignes, l'objet suivant est un problème national de 17 questions. Aucun
  barreau moyen ni difficile entre les deux.
- **Zéro ancrage réel** dans 471 lignes (aucune population, décroissance,
  refroidissement) — la notion dont la propriété célèbre est la *croissance* ne
  montre jamais une croissance.
- **F5/F6** : niveau 3 à ~6 % contre 20 % (SM), et les **six gestes les plus
  fréquents du relevé de la notion** (position relative, branche infinie,
  inflexion, réciproque, suite $u_{n+1}=f(u_n)$, aire) ont **zéro item** au banc
  de fin — or le modèle apprenant est bâti sur ce banc seul.
- **F17/F19** : « exp ne restreint **jamais** le domaine » omet que les
  *exposants* doivent être définis ($e^{1/x}=e^x$ exige $x\neq0$) ; et
  « $f'\ge0$ nul en un point isolé ⟹ strictement croissante » est utilisé comme
  charnière de q7a et q8a sans que la leçon énonce jamais le théorème.
- **F7** : aucun champ `habilete` sur les 31 items — le ratio du cadre reste
  inauditable par machine.
